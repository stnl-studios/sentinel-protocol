SCOPE: unit
UNIT: <unit-slug>
UNIT CLASS: app | api | mobile | worker | package | bff | admin | TBD
LAST UPDATED: YYYYMMDD

# Context

## 1. Resumo da unit
Explique em poucas linhas o que esta unit entrega e por que ela existe.

## 2. Escopo da unit
Liste o que pertence claramente a esta unit.

- `<escopo local 1>`
- `<escopo local 2>`

## 3. Fora de escopo da unit
Liste o que fica em outras units ou fora do repo.

- `<fora de escopo local 1>`
- `<fora de escopo local 2>`

## 4. Localização e fronteira
- Path raiz: `<path>`
- Entradas principais: `<entrypoints>`
- Saídas / boundaries: `<integrações ou dependências relevantes>`

## 5. Responsabilidades principais
- `<responsabilidade 1>`
- `<responsabilidade 2>`
- `<responsabilidade 3>`

## 6. Dependências relevantes
Liste apenas dependências estruturais que ajudam a entender a unit.

- `<dependência 1>`
- `<dependência 2>`

## 7. Visão local de arquitetura
Descreva apenas o suficiente para orientar leitura e atuação nesta unit.

## 8. Pontos de atenção
- `<risco ou limitação local>`
- `<particularidade relevante>`

## 9. Fontes canônicas relacionadas
- `docs/core/CONTEXT.md`
- `docs/units/<unit-slug>/RULES.md`
- `docs/units/<unit-slug>/STATE.md`
- `docs/units/<unit-slug>/TESTING.md`
- `docs/units/<unit-slug>/CONTRACTS.md` quando existir
- `docs/units/<unit-slug>/UI_KIT.md` quando a unit for visual

## 10. TBDs locais
Registrar apenas pendências que afetem o entendimento desta unit.

- `TBD-001` — `<lacuna local>`
- `TBD-002` — `<pergunta local>`
