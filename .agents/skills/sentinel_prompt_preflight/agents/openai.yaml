interface:
  display_name: "Sentinel Prompt Preflight"
  short_description: "Normaliza pedido bruto em prompt governado; faz apenas sanity check por existencia; retorna um unico bloco Markdown."
policy:
  allow_implicit_invocation: true
