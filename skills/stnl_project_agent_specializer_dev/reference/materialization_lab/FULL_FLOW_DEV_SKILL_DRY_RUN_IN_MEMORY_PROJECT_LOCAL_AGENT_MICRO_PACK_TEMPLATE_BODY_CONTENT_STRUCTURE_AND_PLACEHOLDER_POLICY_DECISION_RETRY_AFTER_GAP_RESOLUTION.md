# Template Body Content Structure And Placeholder Policy Decision Retry After Gap Resolution

## Status

`ACTIVE_BODY_CONTENT_STRUCTURE_AND_PLACEHOLDER_POLICY_RETRY_AFTER_GAP_RESOLUTION_DECISION_DOCUMENT`

Posture:

`TEMPLATE_BODY_CONTENT_STRUCTURE_AND_PLACEHOLDER_POLICY_DEFINED_AFTER_GAP_RESOLUTION_TEMPLATE_WRITE_STILL_BLOCKED`

## Phase

`MATERIALIZATION_FULL_FLOW_DEV_SKILL_DRY_RUN_IN_MEMORY_PROJECT_LOCAL_AGENT_MICRO_PACK_TEMPLATE_BODY_CONTENT_STRUCTURE_AND_PLACEHOLDER_POLICY_DECISION_RETRY_AFTER_GAP_RESOLUTION`

Documentary macro flow completed: `VALIDATE -> DECIDE -> DEFINE BODY STRUCTURE -> DEFINE PLACEHOLDER POLICY -> AUDIT -> CLOSE`.

## Verdict

`EXCELLENT_PASS_WITH_BODY_CONTENT_STRUCTURE_AND_PLACEHOLDER_POLICY_DEFINED_AFTER_GAP_RESOLUTION_TEMPLATE_WRITE_STILL_BLOCKED`

The three required prerequisite decisions exist with their exact expected `EXCELLENT_PASS` verdicts. They provide sufficient positive authority to define body content structure and placeholder policy without creating final bodies, filling values, reading templates, or authorizing template write.

## Objective

Close the previously blocked body content structure and placeholder policy decision at documentary level. Define deterministic, concise, no-copy, value-free, Target-independent policies for future TOML and Markdown body drafting while preserving every materialization and write block.

## Scope

- Exactly this decision document under `skills/stnl_project_agent_specializer_dev/reference/materialization_lab/`.
- Explicit-path reads of the required allowlisted decisions and contracts only.
- Documentary body structure and placeholder policy for the canonical 52 conceptual candidates.
- Preservation of matrix, support, trace, anti-bloat, Target, and Aggregator boundaries.

No template, candidate output, parent directory, rendered artifact, executable mechanism, Target real, GitHub resource, productive skill, or host-installed skill is in scope.

## Non-goals

This phase does not create final bodies or template files; fill placeholder values; infer Target facts; create the 52 candidates or their parents; create `template_basis/**`; render or materialize outputs; create fixtures, snapshots, schemas, parsers, renderers, validators, checkers, meters, materializers, runners, or tests; finalize support physically; define a trace schema; enforce numeric thresholds; alter the Aggregator; create a tenth check; or perform branch, commit, or PR operations.

## Inherited baseline

- Architecture: `PROJECT_LOCAL_AGENT_MICRO_PACK_MATRIX`.
- Exactly 12 canonical agents, 24 agent artifacts, and 2 target-level artifacts.
- Exactly 52 conceptual candidates: `24 + 2 + 26 + 0 trace`.
- Exact allowlist and body-basis records are documentary authorities, not write permission.
- Placeholder inventory is closed at 57 names across `shared`, `agent`, `shard`, `trace`, `platform`, and `target`.
- Placeholder syntax is `{{namespace.name}}`; `{{AGENT_BODY}}` is forbidden by the later canonical authorities governing these 52 candidates.
- Support remains bounded and non-agent; trace remains audit-only with zero template files.
- Anti-bloat is qualitative; numeric thresholds remain deferred.
- The official Aggregator remains closed at exactly nine child checks.

## Relation to canonical inputs decision

The canonical inputs decision exists with verdict `EXCELLENT_PASS_WITH_CANONICAL_FORMAT_BODY_STRUCTURE_NOTICE_PLACEMENT_AND_ESCAPING_INPUTS_DEFINED_TEMPLATE_WRITE_STILL_BLOCKED`. It supplies positive authority for TOML shapes, six Markdown families, generated notice inputs, placeholder contexts, escaping, safety, and obligation-to-slot mappings. It supplies no final prose, value, Target fact, or write permission.

## Relation to canonical specification retry

The canonical specification retry exists with verdict `EXCELLENT_PASS_WITH_TOML_MARKDOWN_NOTICE_PLACEHOLDER_PLACEMENT_AND_ESCAPING_CANONICAL_SPECIFICATION_DEFINED_AFTER_CANONICAL_INPUTS_TEMPLATE_WRITE_STILL_BLOCKED`. It fixes keys, tables, headings, slots, ordering, notice, placement, and escaping. This retry adopts that specification without extending it and without converting it into a schema or renderer.

## Relation to gap resolution retry

The gap resolution retry exists with verdict `EXCELLENT_PASS_WITH_BODY_CANONICAL_SOURCE_AND_TOML_PLACEHOLDER_PLACEMENT_GAP_RESOLUTION_DEFINED_AFTER_CANONICAL_SPECIFICATION_TEMPLATE_WRITE_STILL_BLOCKED`. It defines the canonical source as the intersection of candidate identity, body-basis, canonical inputs, canonical specification, placeholder records, support, trace, anti-bloat, matrix, and contracts. It closes the gaps that previously made this decision unsafe.

## Relation to blocked body content structure decision

The older decision remains historically `BLOCKED_WITHOUT_BODY_CONTENT_STRUCTURE_AND_PLACEHOLDER_POLICY`. Its block was valid because complete TOML/Markdown structure, generated notice, placeholder placement, escaping, and clean source authority were absent, and unauthorized template reads contaminated that execution. This clean retry neither converts the old record retroactively nor reuses contaminated content. It relies only on the three later passing authorities and allowed inherited records.

## Relation to exact allowlist decision

The exact 52-entry allowlist remains documentary. Candidate identity selects family and body-basis, but does not supply payload, values, parent-directory permission, or write permission. All candidates remain conceptual and create-only for a separately authorized future phase.

## Relation to body-basis records

The `BB-*`, `TR-*`, and `O-*` records supply semantic purpose, obligations, exclusions, transformations, family linkage, disposition, and audit traceability. They are transformed into concise semantic units and may never be copied as final body text. Candidate identity selects one body-basis; the body-basis selects the closed family structure.

## Relation to placeholder inventory and syntax

The closed inventory contains 57 names: `shared` 9, `agent` 11, `shard` 13, `trace` 10, `platform` 6, and `target` 8. The nominal form is lowercase `{{namespace.name}}` with dot-separated segments and underscores within segments. Unknown, unused, nested, malformed, multiline, unbalanced, or non-uniquely bound tokens fail closed. The legacy rendering contract cannot override later canonical placement; `{{AGENT_BODY}}` aborts for these 52 candidates.

## Relation to support layer

`_shared`, minimal START, MANIFEST activation control, and selectively loaded temperature shards remain auxiliary responsibilities, not agents or a third matrix. Shared rules are referenced, not duplicated. This decision defines content policy only; it does not finalize physical support layout or write support files.

## Relation to trace audit closeout

Trace is audit-only, conceptual, non-payload, non-report, and zero-file. `TR-*` records may support auditability outside body payload. No `trace.*` token, trace template, trace schema, source mirror, or persisted trace artifact is authorized.

## Relation to anti-bloat posture

Entrypoints stay small; START stays minimal; MANIFEST holds bounded inventory and routing; common rules remain in `_shared`; shards and `/docs` are on demand. Load-all behavior, duplicated shared rules, body monoliths, source mirrors, embedded shard bodies, kernel/Profile copies, and `/docs` dumps remain forbidden. Numeric thresholds remain deferred.

## Relation to Aggregator boundary

The official Aggregator remains exactly these nine ordered child checks:

1. `scripts/materialization_lab/check-static.mjs`
2. `scripts/materialization_lab/check-source-inventory.mjs`
3. `scripts/materialization_lab/check-template-coverage.mjs`
4. `scripts/materialization_lab/check-fixture-boundary.mjs`
5. `scripts/materialization_lab/check-lazy-load-fixtures.mjs`
6. `scripts/materialization_lab/check-project-scenarios.mjs`
7. `scripts/materialization_lab/check-render-context.mjs`
8. `scripts/materialization_lab/check-dry-run-plan.mjs`
9. `scripts/materialization_lab/check-fixture-render-dry-run-integration.mjs`

This document is not a check. The wrapper is not a tenth check. No checker is created, registered, reordered, removed, or changed.

## Canonical invariants

- The 12 canonical agents and their order remain unchanged.
- The 24 agent artifacts are exactly 12 GitHub/Copilot entrypoints plus 12 Codex entrypoints.
- `.codex/config.toml` and `AGENTS.md` are target-level artifacts, not agents.
- The 26 support candidates are not agents or a third matrix.
- `.sentinel/agents/**` is support, not a platform matrix.
- Placeholders, template families, notices, `_shared`, shards, and trace are not agents.
- Trace contributes zero physical candidates.
- Every body policy is documentary, no-copy, value-free, and Target-independent.
- No decision in this document is current write permission.

## Matrix 12/24/2 preservation

| Element | Count | Preserved decision |
|---|---:|---|
| Canonical agents | 12 | Exact canonical ordered set |
| Agent artifacts | 24 | 12 GitHub Markdown + 12 Codex TOML |
| Target-level artifacts | 2 | Codex config + AGENTS guide |
| Support candidates | 26 | Non-agent, bounded support/control |
| Trace template files | 0 | Audit-only |

The composition remains exactly `52 = 24 + 2 + 26 + 0 trace`.

## Body content structure retry posture

`TEMPLATE_BODY_CONTENT_STRUCTURE_AND_PLACEHOLDER_POLICY_DEFINED_AFTER_GAP_RESOLUTION_TEMPLATE_WRITE_STILL_BLOCKED`.

The retry defines structure and policy only. Final bodies, final prose, values, Target filling, template creation, and materialization remain blocked.

## Decision model

`BASIS_READY` means inherited evidence is sufficient; `BODY_POLICY_DEFINED` means this decision closes a body or placeholder policy; `DEFERRED` means a later expressly authorized phase is required; `BLOCKED` means the operation remains prohibited. These values are documentary only and are not an executable gate, schema, checker, or validator.

## Decision result

| Criterion | Status | Documentary result |
|---|---|---|
| 1. Canonical inputs decision exists | BASIS_READY | Explicit required path exists. |
| 2. Canonical inputs has expected `EXCELLENT_PASS` | BASIS_READY | Exact verdict confirmed. |
| 3. Canonical specification retry exists | BASIS_READY | Explicit required path exists. |
| 4. Canonical specification has expected `EXCELLENT_PASS` | BASIS_READY | Exact verdict confirmed. |
| 5. Gap resolution retry exists | BASIS_READY | Explicit required path exists. |
| 6. Gap resolution has expected `EXCELLENT_PASS` | BASIS_READY | Exact verdict confirmed. |
| 7. Previous body content structure block understood | BASIS_READY | Missing authority and historical contamination understood. |
| 8. Read hygiene preserved | BASIS_READY | Explicit allowlisted paths only. |
| 9. Exact allowlist remains documentary | BASIS_READY | Identity only; no write authority. |
| 10. 52 candidates remain conceptual | BASIS_READY | No candidate created or inspected. |
| 11. Body-basis records available | BASIS_READY | Semantic records remain authoritative. |
| 12. Body content structure authority defined | BODY_POLICY_DEFINED | Three passing prerequisites authorize this retry. |
| 13. Body content structure policy defined | BODY_POLICY_DEFINED | Deterministic family and obligation mapping closed. |
| 14. TOML body content policy defined | BODY_POLICY_DEFINED | Entrypoint and config policy closed. |
| 15. Markdown body content policy defined | BODY_POLICY_DEFINED | Six fixed families closed. |
| 16. Generated notice body policy defined | BODY_POLICY_DEFINED | Wording, cardinality, and placement closed. |
| 17. Placeholder policy defined | BODY_POLICY_DEFINED | Presence and failure behavior closed. |
| 18. Required placeholders policy defined | BODY_POLICY_DEFINED | Required placements must be present exactly as specified. |
| 19. Optional placeholders policy defined | BODY_POLICY_DEFINED | Omission leaves no residue. |
| 20. Deferred placeholders policy defined | BODY_POLICY_DEFINED | Deferred slots preserve tokens without resolution. |
| 21. Forbidden placeholders policy defined | BODY_POLICY_DEFINED | Forbidden presence aborts. |
| 22. Placeholder absence and failure policy defined | BODY_POLICY_DEFINED | Missing, duplicate, unknown, or malformed cases fail closed. |
| 23. Placeholder values remain blocked | BLOCKED | No value is authorized. |
| 24. Target-dependent filling remains blocked | BLOCKED | No Target fact may be read or inferred. |
| 25. Body drafting boundary defined | BODY_POLICY_DEFINED | Concise semantic transformation only. |
| 26. Trace remains audit-only | BASIS_READY | Trace is not payload. |
| 27. Trace template files remain zero | BASIS_READY | Zero physical trace files. |
| 28. No-copy preserved | BASIS_READY | No source mirror or literal body copy. |
| 29. Anti-bloat preserved | BASIS_READY | Small, bounded, lazy-load posture. |
| 30. Create-only readiness criteria defined | BODY_POLICY_DEFINED | Future necessary conditions are explicit. |
| 31. Mutation remains blocked | BLOCKED | No overwrite, merge, repair, or update. |
| 32. No templates read | BASIS_READY | No template path accessed. |
| 33. No host-installed skills read | BASIS_READY | Host skills remained outside scope. |
| 34. No productive skill read | BASIS_READY | Productive skill remained outside scope. |
| 35. No templates created | BASIS_READY | Only this document is created. |
| 36. No rendered outputs | BASIS_READY | None created. |
| 37. No fixtures | BASIS_READY | None created. |
| 38. No snapshots | BASIS_READY | None created. |
| 39. No parser, renderer, or schema | BASIS_READY | No executable mechanism created. |
| 40. No validator, checker, or materializer | BASIS_READY | No enforcement or writer created. |
| 41. Matrix `12/24/2` preserved | BASIS_READY | Cardinalities unchanged. |
| 42. `52 = 24 + 2 + 26 + 0 trace` preserved | BASIS_READY | Composition unchanged. |
| 43. Aggregator 9-check boundary preserved | BASIS_READY | No change or tenth check. |
| 44. Target real remains blocked | BLOCKED | No Target access authorized. |
| 45. GitHub remains blocked | BLOCKED | No GitHub access authorized. |
| 46. Current phase writes limited to one document | BASIS_READY | Exactly this path is authorized. |

The table is documentary only. It is not a schema, checker, validator, template, or executable gate.

## Read hygiene and contamination prevention

Only the 16 primary required paths were read, each explicitly. No broad search, recursive listing, template read/list/search, host-skill read, productive-skill read, Target access, or GitHub access occurred. `__MACOSX` and `.DS_Store` were ignored. Historical contamination remains a recorded historical fact; its content was discarded and not reopened or used as evidence.

## Body content structure authority

This phase is authorized to define body content structure and placeholder policy because canonical inputs, canonical specification, and gap resolution all passed with the required verdicts. Its authority is limited to documentary structure and policy. It cannot create a final body, create a template file, fill a value, use Target facts, produce rendered output, or create an executable mechanism.

## Body content structure policy

For a future body draft:

1. Candidate identity selects the body-basis record.
2. Body-basis selects the family and semantic obligations.
3. Gap resolution defines the canonical source intersection.
4. Canonical specification fixes headings, keys, slots, notice, placement, and escaping.
5. Each obligation becomes one concise semantic body unit in exactly one compatible slot.
6. Each exclusion becomes explicit forbidden content.
7. Role semantics are transformed, not copied.
8. Shared rules are referenced, not duplicated.
9. Trace remains audit-only and outside the payload.
10. Temperature remains reference or activation metadata, never embedded shard content.

Every body must be small, structured, no-copy, non-monolithic, and bounded by its family. An obligation without one compatible closed slot aborts; it does not create a heading, key, or slot.

## TOML body content policy

For each Codex entrypoint, the future document has no tables. After the mandatory line-1 notice and one empty line, keys are ordered `name`, `description`, optional `model`, optional `model_reasoning_effort`, optional `sandbox_mode`, then `developer_instructions`. Optional keys are omitted unless separately value-authorized. `name` contains only `{{agent.display_name}}`; `description` contains only `{{agent.canonical_role}}`. `developer_instructions` is the only multiline basic string and contains exactly seven tokens, one per logical line, in this order: `{{agent.agent_id}}`, `{{agent.canonical_role}}`, `{{agent.activation_policy_reference}}`, `{{agent.start_sequence}}`, `{{shared.routing_relation}}`, `{{shared.global_reading_boundary}}`, `{{agent.stop_conditions}}`.

For target config, there are no top-level keys and exactly 12 `agents.<literal-agent-id>` tables in canonical agent order. Each table contains only `description` then `config_file`. Their complete values are `{{agent.canonical_role}}` and deferred `{{target.entrypoint_path}}`, bound by `(literal agent ID, placeholder)` without fallback or carry-over.

Only allowlisted tokens may occur in allowlisted string values. No final value or Target-dependent filling is performed. No TOML file is created.

## Markdown body content policy

The six fixed families have exactly one H1 and the following ordered H2 headings:

- GitHub platform entrypoint: `# Agent`; Mission; Activation; Responsibilities; Boundaries; Routing and support; Completion.
- Target-level AGENTS guide: `# Agent system`; Purpose; Canonical agents; Routing; Shared support; Selective loading; Documentation access; Boundaries.
- Shared START: `# Shared start`; Purpose; Initial sequence; Shared references; Reading boundary; Failure handling.
- Shared MANIFEST: `# Shared manifest`; Purpose; Inventory; Activation rules; Dependencies; Do not load; Missing item behavior.
- Per-agent START: `# Agent start`; Identity; Activation; Initial reads; Sequence; Shared support; Stop conditions.
- Per-agent MANIFEST: `# Agent manifest`; Identity and role; Inventory; Activation triggers; Dependencies; Do not load; Output and handoff; Trace expectations; Documentation access.

Each slot has one narrow purpose. Prose is concise semantic paraphrase; lists are bounded and atomic. Ordered sequences use `1.` and other lists use `-`. H3, dynamic or optional headings, nested lists, and tables are forbidden under the canonical specification. No source mirror, body monolith, or rendered file is created.

## Generated notice body policy

Every future generated Markdown file has exactly one line-1 notice `<!-- Generated file. Do not edit directly. -->`. Every future generated TOML file has exactly one line-1 notice `# Generated file. Do not edit directly.`. The notice is followed by exactly one empty line. It is mandatory but is not body payload; it contains no placeholder, Target fact, provenance payload, or source boilerplate and does not authorize rendering or writing.

## Placeholder policy

The policy classifies every family/namespace placement as required, optional, deferred, or forbidden:

| Family | `shared` | `agent` | `shard` | `trace` | `platform` | `target` |
|---|---|---|---|---|---|---|
| GitHub entrypoint | OPTIONAL | REQUIRED | FORBIDDEN | FORBIDDEN | REQUIRED | FORBIDDEN |
| Target AGENTS | REQUIRED | FORBIDDEN | OPTIONAL | FORBIDDEN | OPTIONAL | DEFERRED |
| Shared START | REQUIRED | FORBIDDEN | OPTIONAL | FORBIDDEN | FORBIDDEN | FORBIDDEN |
| Shared MANIFEST | REQUIRED | FORBIDDEN | OPTIONAL | FORBIDDEN | FORBIDDEN | FORBIDDEN |
| Per-agent START | OPTIONAL | REQUIRED | OPTIONAL | FORBIDDEN | FORBIDDEN | FORBIDDEN |
| Per-agent MANIFEST | OPTIONAL | REQUIRED | REQUIRED | FORBIDDEN | FORBIDDEN | DEFERRED |
| Codex entrypoint TOML | REQUIRED | REQUIRED | FORBIDDEN | FORBIDDEN | FORBIDDEN | FORBIDDEN |
| Codex target config TOML | FORBIDDEN | REQUIRED | FORBIDDEN | FORBIDDEN | FORBIDDEN | DEFERRED |

Namespace eligibility never grants automatic placement: the identifier purpose must match the exact allowlisted slot. Tokens may occur only in allowlisted Markdown prose/list items or TOML string values. They never occur in physical paths, filenames, headings, keys, table names, notices, comments, structural markers, links, images, HTML, code fences, front matter, or table cells.

## Required placeholders policy

Every required identifier must appear in its appropriate future structure, in the exact allowlisted slot and cardinality. Missing required, duplicated singular, displaced, or non-uniquely bound required tokens abort. Codex entrypoints and target config must follow the exact token sets and table-scoped binding defined in the TOML policy. Required does not authorize value resolution.

## Optional placeholders policy

An optional placeholder may appear only in a matching optional slot and may be omitted without blank line, empty item, comment, marker, default, or other residue. Presence outside its allowlisted slot or duplication beyond slot cardinality aborts. Optional does not mean inferable and does not authorize a value.

## Deferred placeholders policy

A deferred placeholder may appear only in a slot expressly classified deferred. It remains unresolved and byte-preserved until a separate value and Target authority exists. It cannot be replaced with blank, default, observed path, inferred fact, or sample. A deferred requirement that must be resolved to produce a valid future file blocks that write.

## Forbidden placeholders policy

Any placeholder in a forbidden family, namespace, slot, or structural context aborts. `trace.*` has no placement in the 52 bodies. Unknown, renamed, unused, nested, malformed, multiline, delimiter-unbalanced, monolithic, path-structural, or unsafe tokens abort. `{{AGENT_BODY}}` always aborts for this matrix.

## Placeholder absence and failure policy

- Missing required token: abort.
- Omitted optional token: remove the optional unit without residue.
- Deferred token outside a deferred slot or requiring current resolution: abort.
- Present forbidden token: abort.
- Duplicate singular token or ambiguous binding: abort.
- Unknown, unused, malformed, nested, multiline, or unbalanced token: abort.
- Placeholder-provided newline, structure, key, heading, notice, or path: abort.
- Any need to read a forbidden source to classify or resolve a token: abort.

All failures are fail-closed. No coercion, fallback, silent normalization, or invented default is allowed.

## Placeholder value policy

All 57 placeholder values remain unfilled and blocked. Inventory provenance classes such as static-documentary, derived-documentary, and future-target-dependent describe future authority requirements, not values. Escaping applies only after a value is separately authorized: Markdown and TOML context rules from the canonical specification remain normative. This phase performs no resolution, interpolation, serialization, or value validation.

## Body drafting boundary

A separately authorized future body drafting phase may transform obligations into concise prose; place already allowlisted placeholders; preserve notices, headings, keys, slots, ordering, no-copy, and anti-bloat; and represent exclusions as explicit boundaries.

It must not invent headings, keys, slots, placeholder names, values, defaults, or Target facts; copy source bodies; read templates, host skills, or the productive skill; create rendered outputs; or write template files without separate create-only authorization. It must abort if an obligation cannot fit one closed slot.

## Body family coverage

| Family | Body structure status | Placeholder policy status | Final body status | Write status | Still blocked |
|---|---|---|---|---|---|
| GitHub platform entrypoint | BODY_POLICY_DEFINED | BODY_POLICY_DEFINED | BLOCKED_FOR_FINAL_BODY | BLOCKED_FOR_WRITE | Prose, values, write |
| Codex platform entrypoint | BODY_POLICY_DEFINED | BODY_POLICY_DEFINED | BLOCKED_FOR_FINAL_BODY | BLOCKED_FOR_WRITE | Values, write |
| Codex target config | BODY_POLICY_DEFINED | BODY_POLICY_DEFINED | BLOCKED_FOR_FINAL_BODY | BLOCKED_FOR_WRITE | Values, Target filling, write |
| Target-level AGENTS guide | BODY_POLICY_DEFINED | BODY_POLICY_DEFINED | BLOCKED_FOR_FINAL_BODY | BLOCKED_FOR_WRITE | Prose, values, write |
| Shared START | BODY_POLICY_DEFINED | BODY_POLICY_DEFINED | BLOCKED_FOR_FINAL_BODY | BLOCKED_FOR_WRITE | Prose, values, write |
| Shared MANIFEST | BODY_POLICY_DEFINED | BODY_POLICY_DEFINED | BLOCKED_FOR_FINAL_BODY | BLOCKED_FOR_WRITE | Prose, values, write |
| Per-agent START | BODY_POLICY_DEFINED | BODY_POLICY_DEFINED | BLOCKED_FOR_FINAL_BODY | BLOCKED_FOR_WRITE | Prose, values, write |
| Per-agent MANIFEST | BODY_POLICY_DEFINED | BODY_POLICY_DEFINED | BLOCKED_FOR_FINAL_BODY | BLOCKED_FOR_WRITE | Prose, values, write |
| Temperature support | BODY_POLICY_DEFINED | BODY_POLICY_DEFINED | NOT_APPLICABLE | BLOCKED_FOR_WRITE | Physical finalization; references only |
| Trace support | AUDIT_ONLY_ZERO_FILES | NOT_APPLICABLE | NOT_APPLICABLE | AUDIT_ONLY_ZERO_FILES | Schema and materialization remain blocked |

## Trace support posture

Trace remains an audit relationship across conceptual candidates, never body payload. It has no template path, placeholder placement, physical file, final schema, source mirror, report, or runtime behavior. Static `Trace expectations` prose may state no-copy/audit-only boundaries, but cannot contain `trace.*` or copied source content.

## No-copy boundary

Future body text must be a concise semantic transformation of explicit obligations. Existing templates, historical bodies, kernels, Senior Agent Profiles, `/docs`, host skills, the productive skill, Target, GitHub, and contaminated historical content are forbidden body sources. References remain references; source content is never mirrored.

## Anti-bloat boundary

One section serves one purpose. Shared rules stay shared; agent files contain role-specific deltas and references. START does not embed MANIFEST; MANIFEST does not embed shard bodies; AGENTS does not concatenate role bodies; entrypoints do not reproduce support packs. Load-all, repo-wide content ingestion, source mirrors, monolithic placeholders, and heavy agents remain forbidden.

## Target dependency boundary

Conceptual Target shapes constrain structure but provide no observed fact. Target-dependent tokens remain deferred in their exact slots. Real paths, ownership, collisions, tools, stack, commands, capabilities, platform behavior, documentation maps, configuration values, and runtime facts cannot be read, inferred, or filled in this phase.

## Create-vs-mutate decision

Mutation remains blocked. Current create is limited to this decision document. Any future template phase must be separately authorized as create-only and all-or-nothing; preexistence of any candidate aborts before content read. Overwrite, append, merge, repair, rename, normalization, delete-and-recreate, cleanup, and mixed create/mutate are forbidden.

## Future create-only write readiness criteria

A future create-only template write may be considered only after:

- canonical inputs retain their required `EXCELLENT_PASS`;
- canonical specification retry retains its required `EXCELLENT_PASS`;
- gap resolution retry retains its required `EXCELLENT_PASS`;
- this body content structure retry retains its required `EXCELLENT_PASS`;
- the exact 52-entry allowlist remains valid;
- all 52 paths are confirmed absent without content reads;
- parent-directory creation is explicitly authorized;
- every body is derivable without guessing, copying, or forbidden reads;
- every placeholder is classified and correctly placed;
- no placeholder value or Target-dependent filling is required;
- zero trace template files remain preserved; and
- create-only, all-or-nothing execution is possible.

These are necessary readiness conditions, not current authorization.

## Abort criteria for future template write

A future write must abort if it would need to invent body source, body structure, TOML key/table/order/type/comment/escaping, Markdown heading/slot/prose/list/table, notice, placement, or placeholder name; use a token in a physical path, filename, TOML key/table, Markdown heading, or other forbidden context; fill a value; infer Target data; use `{{AGENT_BODY}}` or another monolithic token; read/copy a template, host skill, productive skill, kernel, Senior Agent Profile, or `/docs`; mutate an existing file; create rendered output, fixture, snapshot, parser, renderer, schema, validator, checker, meter, materializer, runner, or test; change the Aggregator or add a tenth check; access Target real or GitHub; alter `12/24/2`; treat support as a third matrix or templates as agents; create trace files; or materialize Target artifacts.

## What is decided now

- Positive authority for this clean retry.
- Canonical body structure policy for TOML and six Markdown families.
- Mandatory generated notice policy.
- Required, optional, deferred, forbidden, absence, duplication, and failure placeholder policies.
- Value-free drafting, source, no-copy, anti-bloat, trace, Target, and create-only boundaries.
- Family coverage and future readiness/abort criteria.

## What remains blocked

The 52 template implementations; template creation, write, or mutation; parent directories; materialization; final bodies and prose; rendered outputs; fixtures; snapshots; placeholder values; Target-dependent filling or mapping; support-layer physical finalization; trace final schema/files; source mirror; `/docs` dump; checker, validator, meter, parser, renderer, schema, materializer, runner, or test; threshold enforcement; Target real; GitHub; productive skill; host-installed skills; Aggregator change; and tenth check remain blocked.

## What cannot be finalized yet

Final body wording, final placeholder values, Target-dependent bindings, optional model/effort/sandbox values, Target paths and documentation maps, physical support layout, ownership and collision handling, create-only atomicity implementation, trace schema, rendered/fixture/snapshot strategy, executable validation, numeric anti-bloat thresholds, runtime integration, and post-write Target validation cannot be finalized here.

## Forbidden inferences

Do not infer from templates, filesystem adjacency, filename, conceptual path, generic TOML/Markdown convention, host skill, productive skill, Target, GitHub, historical contamination, legacy flat token, missing value, platform recollection, kernel/Profile wording, or `/docs`. Do not promote body-basis prose into literal payload, namespace eligibility into automatic placement, specification into executable schema, or allowlist into write permission.

## Allowed future decision paths

The immediate allowed path is the separately authorized create-only implementation retry identified below, provided it revalidates all readiness gates and still requires no values or Target filling. Post-write audit may follow only after a successful authorized write. Any need for values, Target facts, mutation, rendering, fixtures, snapshots, mechanisms, physical support finalization, trace schema, or numeric thresholds requires a distinct explicit phase.

## Recommended safe next phase

`MATERIALIZATION_FULL_FLOW_DEV_SKILL_DRY_RUN_IN_MEMORY_PROJECT_LOCAL_AGENT_MICRO_PACK_TEMPLATE_WRITE_IMPLEMENTATION_CREATE_ONLY_RETRY_AFTER_BODY_CONTENT_STRUCTURE_DECISION`

The recommendation is documentary only. It does not auto-start or authorize creation.

## Explicit non-authorization

This decision does not authorize implementation of the 52 templates; materialization; template creation, write, or mutation; parent-directory creation; rendered outputs; fixtures; snapshots; final placeholder values; Target-dependent filling or mapping; support-layer physical finalization; trace final schema/files; source mirrors; `/docs` dumps; checkers, validators, meters, parsers, renderers, schemas, materializers, runners, or tests; threshold enforcement; Target real; GitHub; branch, commit, or PR; productive skill; host-installed skills; Aggregator changes; a tenth check; or any automatic next phase.
