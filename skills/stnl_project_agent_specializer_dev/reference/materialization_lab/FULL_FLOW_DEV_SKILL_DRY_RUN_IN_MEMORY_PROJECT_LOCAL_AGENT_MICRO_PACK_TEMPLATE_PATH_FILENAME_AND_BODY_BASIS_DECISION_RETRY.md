# Project-Local Agent Micro-Pack Template Path, Filename, and Body-Basis Decision Retry

## 1. Verdict

`TEMPLATE_PATH_FILENAME_AND_BODY_BASIS_DECISION_RETRY_BLOCKED_PENDING_CANONICAL_BASIS`

Phase verdict:

`MATERIALIZATION_FULL_FLOW_DEV_SKILL_DRY_RUN_IN_MEMORY_PROJECT_LOCAL_AGENT_MICRO_PACK_TEMPLATE_PATH_FILENAME_AND_BODY_BASIS_DECISION_RETRY: BLOCKED`

The retry is documentation-only. Template writing, template creation, template mutation, exact-allowlist work, materialization, and rendered output remain blocked.

## 2. Executive Summary

The approved layout, naming, and source-selection specification provides the nine-family taxonomy, the symbolic layout grammar, conceptual target filenames for the two agent entrypoint surfaces and two target-level artifacts, family-level transformation constraints, placeholder categories, and the invariant matrix of 12 agents and 24 conceptual agent artifacts.

It deliberately does not select the exact dev-template root, exact target-surface segment spellings, exact dev-template filenames, a template marker or suffix, physical shard filenames, trace-shard cardinality, or complete source and activation-temperature bindings for every body-basis record. Those omissions cannot be resolved from the allowed evidence without invention. Consequently, exact candidate paths cannot be enumerated, collision freedom cannot be proved, total future package cardinality cannot be fixed, and a future exact-allowlist decision is not ready.

This record preserves all supported partial bases and names each unresolved element. It does not contain final template prose, copy-ready template fragments, filled placeholders, or write authorization.

## 3. Scope

This retry evaluates only the documentation basis for:

- the exact dev-template root and family-to-path mapping;
- deterministic dev-template filenames;
- body-basis records for the nine approved families;
- family cardinality and the 12-agent/24-agent-artifact invariant;
- support-artifact counting, collision, preexistence, source-selection, transformation, placeholder, traceability, and anti-bloat constraints; and
- readiness for a later exact-allowlist decision.

The only repository change in this phase is this decision record.

## 4. Non-Goals

This retry does not create, inspect, compare, alter, render, or authorize templates. It does not select final template bodies, final body sections, placeholder syntax, placeholder values, target-derived values, an exact write allowlist, a parser, renderer, schema, validator, checker, materializer, fixture, snapshot, report, Aggregator change, or tenth official check. It does not access a real Target, GitHub, or the productive skill.

## 5. Canonical Inputs Read

The evidence used for this decision is limited to:

1. `FULL_FLOW_DEV_SKILL_DRY_RUN_IN_MEMORY_PROJECT_LOCAL_AGENT_MICRO_PACK_TEMPLATE_LAYOUT_NAMING_AND_SOURCE_SELECTION_SPECIFICATION.md`, read completely and treated as the primary canonical input;
2. narrow, term-limited reads of `FULL_FLOW_DEV_SKILL_DRY_RUN_IN_MEMORY_PROJECT_LOCAL_AGENT_MICRO_PACK_TEMPLATE_PATH_FILENAME_AND_BODY_BASIS_DECISION.md`, used only to preserve its prior blocked state and declared missing bases; and
3. narrow, term-limited reads of `FULL_FLOW_DEV_SKILL_DRY_RUN_IN_MEMORY_PROJECT_LOCAL_AGENT_MICRO_PACK_TEMPLATE_WRITE_EXACT_ALLOWLIST_DECISION.md`, used only to preserve its prior blocked state and dependency on a canonical path/filename/body basis.

The phase instruction was read completely as an execution boundary, not as authority to invent architectural facts. A narrow top-level filename listing of `reference/materialization_lab/` was used only to locate the two prior decision records; filesystem existence and naming were not used as template-layout or filename evidence.

## 6. Prohibited Inputs Not Read

No file under `skills/stnl_project_agent_specializer_dev/reference/templates/**` was read, inspected, searched, compared, or used as evidence. The productive repository skill at `skills/stnl_project_agent_specializer/` was not accessed. No real Target, GitHub state, fixture, snapshot, rendered output, or existing template content was accessed. `.DS_Store` and `__MACOSX` content was not read or used.

## 7. Historical Blocker Handling

The historical accidental reads of limited lines from:

- `skills/stnl_project_agent_specializer_dev/reference/templates/codex/AGENTS.md`; and
- `skills/stnl_project_agent_specializer_dev/reference/templates/copilot/agent.md`

remain contaminated and invalid. Their content was not revisited, cited, reused, compared, summarized, or used to infer a root, segment, filename, extension, marker, body obligation, placeholder, convention, cardinality, or candidate path. This retry is independent of that content. Any later attempt to use it as evidence must block.

## 8. Architecture And Matrix Invariants

The architectural direction remains `PROJECT_LOCAL_AGENT_MICRO_PACK_MATRIX` with these conceptual target surfaces:

- `.github/agents/<agent>.agent.md`;
- `.codex/agents/<agent>.toml`;
- `.sentinel/agents/_shared/**`; and
- `.sentinel/agents/<agent>/**`.

The canonical roles remain, in order: `orchestrator`, `planner`, `validation-eval-designer`, `execution-package-designer`, `designer`, `coder-frontend`, `coder-backend`, `coder-ios`, `validation-runner`, `reviewer`, `finalizer`, and `resync`.

The matrix proof is fixed as `12 GitHub entrypoints + 12 Codex entrypoints = 24 conceptual agent artifacts`. `.codex/config.toml` and `AGENTS.md` are exactly two conceptual target-level artifacts and do not count as agents. Shared support, per-agent support, trace, placeholders, `_shared`, and families do not count as agents. `.sentinel/agents/**` is support architecture, not a third agent matrix and not a replacement for either entrypoint surface.

The Aggregator remains closed at exactly nine existing child checks. This retry creates or registers no check.

## 9. Family Taxonomy Reviewed

All nine approved families are retained. No family is added, removed, split, or inferred from existing templates:

1. `copilot-agent-entrypoint`;
2. `codex-agent-entrypoint`;
3. `codex-target-config`;
4. `target-agents-guide`;
5. `shared-micro-pack-start`;
6. `shared-micro-pack-manifest`;
7. `agent-micro-pack-start`;
8. `agent-micro-pack-manifest`; and
9. `trace-shard`.

## 10. Dev-Template Root Basis Decision

Status: `BLOCKED`.

The only canonical root token is `<AUTHORIZED_DEV_TEMPLATE_ROOT>`. The primary specification expressly leaves it symbolic and states that a future decision must explicitly authorize it. No allowed evidence read by this retry supplies that authorization. Directory existence, the shape of prohibited existing template directories, and conceptual target paths are not valid root evidence.

Selecting any literal root would therefore be invention. No dev-template directory is selected or created.

## 11. Filename Normalization Basis Decision

Status: `PARTIAL_BASIS_ONLY_BLOCKED_FOR_EXACT_FILENAMES`.

Supported inputs are the canonical family key, target surface, scope, exact canonical role when per-agent, artifact purpose, required target format, and an explicitly selected template marker or suffix. Canonical role identifiers are stable lower-case kebab-case tokens and must not be aliased or humanized. The conceptual output forms `<agent>.agent.md`, `<agent>.toml`, `config.toml`, and `AGENTS.md` are supported architectural inputs.

The following required dev-template normalization choices are not canonically decided:

- exact target-surface path tokens;
- role placement in a dev-template basename versus its parent path;
- case rules for target-level and support basenames;
- treatment of compound extensions in dev-template filenames;
- the exact template marker or suffix and its placement;
- exact support-artifact extensions and basenames; and
- exact trace naming.

The family keys and conceptual output names do not decide these missing elements. No exact dev-template filename is approved.

## 12. Family-To-Path Basis Records

Every record binds the known family facts to a body-basis record while preserving unresolved segments. `<ROOT?>`, `<SURFACE?>`, `<TARGET_MARKER?>`, and `<FILE?>` denote missing decisions in this record, not approved path tokens or placeholders for a template.

| Record | Family | Conceptual target output | Scope segment | Family segment | Role/shared/target basis | Purpose-file basis | Candidate cardinality | Counts in 24 | Collision status | Body basis |
| --- | --- | --- | --- | --- | --- | --- | ---: | --- | --- | --- |
| `PB-01` | `copilot-agent-entrypoint` | `.github/agents/<agent>.agent.md` | `per-agent` | family key | each of 12 exact roles | `<FILE?>`; conceptual role `.agent.md` only | 12 | Yes | Blocked: root, surface token, filename, marker | `BB-01` |
| `PB-02` | `codex-agent-entrypoint` | `.codex/agents/<agent>.toml` | `per-agent` | family key | each of 12 exact roles | `<FILE?>`; conceptual role `.toml` only | 12 | Yes | Blocked: root, surface token, filename, marker | `BB-02` |
| `PB-03` | `codex-target-config` | `.codex/config.toml` | `target-level` | family key | target marker omitted or exact token remains undecided | `<FILE?>`; conceptual `config.toml` only | 1 | No | Blocked: root, surface token, target-marker rule, filename | `BB-03` |
| `PB-04` | `target-agents-guide` | `AGENTS.md` | `target-level` | family key | target marker omitted or exact token remains undecided | `<FILE?>`; conceptual `AGENTS.md` only | 1 | No | Blocked: root, surface token, target-marker rule, filename | `BB-04` |
| `PB-05` | `shared-micro-pack-start` | `.sentinel/agents/_shared/**` support | `shared` | family key | one shared marker, exact token undecided | `<FILE?>`; START is purpose, not filename | 0..1 | No | Blocked: root, surface/shared tokens, filename, shard policy | `BB-05` |
| `PB-06` | `shared-micro-pack-manifest` | `.sentinel/agents/_shared/**` support | `shared` | family key | one shared marker, exact token undecided | `<FILE?>`; MANIFEST is purpose, not filename | 0..1 | No | Blocked: root, surface/shared tokens, filename, shard policy | `BB-06` |
| `PB-07` | `agent-micro-pack-start` | `.sentinel/agents/<agent>/**` support | `per-agent` | family key | each of 12 exact roles | `<FILE?>`; START is purpose, not filename | 12 if required by exact shard policy | No | Blocked: root, surface token, filename, shard policy | `BB-07` |
| `PB-08` | `agent-micro-pack-manifest` | `.sentinel/agents/<agent>/**` support | `per-agent` | family key | each of 12 exact roles | `<FILE?>`; MANIFEST is purpose, not filename | 12 if required by exact shard policy | No | Blocked: root, surface token, filename, shard policy | `BB-08` |
| `PB-09` | `trace-shard` | shared or per-agent trace support | scope remains shared or per-agent | family key | unresolved by trace policy | `<FILE?>` | Unknown | No | Blocked: scope, root, all physical naming, cardinality | `BB-09` |

The symbolic grammar remains `<AUTHORIZED_DEV_TEMPLATE_ROOT>/<TARGET_SURFACE>/<SCOPE>/<FAMILY>/<ROLE_OR_SHARED>/<PURPOSE_TEMPLATE_FILE>`, but it is not an exact path mapping. No row can be expanded into an allowlist entry.

## 13. Candidate Filename Basis Records

| Record | Family | Surface and scope | Role | Purpose | Target format | Template marker basis | Supported deterministic result | Exact dev filename status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `FB-01` | `copilot-agent-entrypoint` | GitHub, per-agent | one exact canonical role | small agent entrypoint | compound `.agent.md` output | Missing | conceptual example `orchestrator.agent.md` | Blocked |
| `FB-02` | `codex-agent-entrypoint` | Codex, per-agent | one exact canonical role | small agent entrypoint | `.toml` output | Missing | conceptual example `orchestrator.toml` | Blocked |
| `FB-03` | `codex-target-config` | Codex, target-level | not applicable | target configuration | `.toml` output | Missing | conceptual output `config.toml` | Blocked |
| `FB-04` | `target-agents-guide` | target guidance, target-level | not applicable | routing and operating guide | Markdown output | Missing | conceptual output `AGENTS.md` | Blocked |
| `FB-05` | `shared-micro-pack-start` | Sentinel support, shared | not applicable | shared activation entry | extension undefined | Missing | none; START is purpose only | Blocked |
| `FB-06` | `shared-micro-pack-manifest` | Sentinel support, shared | not applicable | shared inventory and routing metadata | extension undefined | Missing | none; MANIFEST is purpose only | Blocked |
| `FB-07` | `agent-micro-pack-start` | Sentinel support, per-agent | one exact canonical role | role activation entry | extension undefined | Missing | none; START is purpose only | Blocked |
| `FB-08` | `agent-micro-pack-manifest` | Sentinel support, per-agent | one exact canonical role | role inventory and routing metadata | extension undefined | Missing | none; MANIFEST is purpose only | Blocked |
| `FB-09` | `trace-shard` | Sentinel support, shared or per-agent | conditional and unresolved | obligation traceability | extension undefined | Missing | none | Blocked |

The deterministic examples are conceptual target-output facts only. They were not derived from existing templates and are not candidate dev-template filenames.

## 14. Body-Basis Records

These records state the maximum family-level basis supported without writing final prose. All records prohibit copied kernels, copied Senior Agent Profiles, copied full docs, existing-template content, filled placeholders, rendered examples, monolithic agents, unsupported obligations, and write authorization.

| Record | Family | Behavior obligations | Allowed source categories and selection boundary | Transformation and activation basis | Shared versus role-specific allocation | Trace expectation | Missing basis and status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `BB-01` | `copilot-agent-entrypoint` | minimum role identity, activation, GitHub entry, and routing to support | matrix role, future explicitly authorized GitHub surface contract, activation/routing policy, applicable shared behavior | non-literal GitHub-surface adaptation; keep support out of entrypoint; exact temperature allocation unresolved | shared rules referenced; exact role obligations selected per role | source obligation -> transformation -> family/role/temperature | exact source bindings, precedence, and temperature disposition missing; `BLOCKED` |
| `BB-02` | `codex-agent-entrypoint` | minimum role identity, configuration, Codex entry, and routing to support | matrix role, future explicitly authorized Codex surface/config contract, activation/routing policy, applicable shared behavior | non-literal Codex-surface adaptation; keep support out of entrypoint; exact temperature allocation unresolved | shared rules referenced; exact role obligations selected per role | same required chain, target-aware | exact source bindings, precedence, and temperature disposition missing; `BLOCKED` |
| `BB-03` | `codex-target-config` | target configuration and matrix registration only; no role body | future explicitly authorized Codex target configuration contract and matrix registration decision | transform documented configuration obligations only; activation temperature not applicable | target-level only | each config obligation traced to its contract | exact contract/source binding and exact required obligations missing; `BLOCKED` |
| `BB-04` | `target-agents-guide` | cross-cutting target operation, routing, shared activation, and on-demand docs policy | future explicitly authorized target operating/routing contract, on-demand docs policy, shared activation policy | summarize cross-cutting guidance; never concatenate role bodies or docs; default-loading allocation unresolved | shared cross-cutting only; role details remain in role support | each guidance obligation traced without copying | exact source bindings, precedence, and activation allocation missing; `BLOCKED` |
| `BB-05` | `shared-micro-pack-start` | shared activation entry and discovery of common support | shared-rule contracts, source inventory, activation policy, shard taxonomy, each explicitly authorized later | partition common activation concerns; allocate only genuinely shared obligations; exact temperatures/shards unresolved | shared once; no role duplication | obligation -> shared disposition -> temperature -> source | exact shard taxonomy, sources, temperatures, and artifact requirement missing; `BLOCKED` |
| `BB-06` | `shared-micro-pack-manifest` | shared shard inventory and routing metadata | same allowed shared categories, limited to inventory/routing need | inventory/routing transformation only; do not duplicate shard bodies | shared once | every listed shard/obligation must trace to source and destination | exact inventory, shard count, sources, and metadata obligations missing; `BLOCKED` |
| `BB-07` | `agent-micro-pack-start` | concise role activation and route to role/shared shards | canonical role contract, relevant profile/kernel semantics as semantic sources only, activation policy, source inventory | non-literal role preparation; temperature-aware routing; exact allocation unresolved | role-specific only, referencing shared behavior | per-role obligation/source/transformation/temperature | exact per-role source bindings, precedence, shard taxonomy, and temperatures missing; `BLOCKED` |
| `BB-08` | `agent-micro-pack-manifest` | per-role shard inventory and routing metadata | same allowed role categories, limited to inventory/routing need | inventory transformation only; no shard-body duplication | per-role inventory plus references to shared | every inventory item traced to role obligation and source | exact per-role shard inventory/count and source bindings missing; `BLOCKED` |
| `BB-09` | `trace-shard` | map selected sources to semantic obligations, transformations, destination family/scope, temperature, and shared/role disposition | traceability decisions, selected allowed source references, behavior obligations, family mapping | audit-only mapping; never reproduce source bodies | shared or role-specific placement must follow a still-missing trace policy | trace is the artifact purpose | scope, count, format, schema boundary, source set, and placement missing; `BLOCKED` |

These are decision records, not final body designs. They contain no final headings, prose, snippets, placeholder syntax, values, or implementation-ready content. Because exact authorized sources and dispositions remain missing, the family records are insufficient for later drafting without invention.

## 15. Cardinality And Counting Proof

| Family | Supported cardinality result | Agent-count effect |
| --- | --- | --- |
| `copilot-agent-entrypoint` | exactly 12 | contributes 12 agents/artifacts to the 24 |
| `codex-agent-entrypoint` | exactly 12 | contributes 12 agents/artifacts to the 24 |
| `codex-target-config` | exactly 1 | support only; zero agents |
| `target-agents-guide` | exactly 1 | support only; zero agents |
| `shared-micro-pack-start` | at most 1; exact 0-or-1 requirement unresolved | support only; zero agents |
| `shared-micro-pack-manifest` | at most 1; exact 0-or-1 requirement unresolved | support only; zero agents |
| `agent-micro-pack-start` | candidate 12 only if exact shard policy requires the family | support only; zero agents |
| `agent-micro-pack-manifest` | candidate 12 only if exact shard policy requires the family | support only; zero agents |
| `trace-shard` | unknown pending canonical trace policy | support only; zero agents |

The agent matrix is exactly 24 conceptual agent artifacts and is proved independently of support cardinality. The two target-level artifacts are exactly two and do not alter that count. Known non-trace package cardinality is not exact: depending on unresolved support-family requirements it cannot be promoted to a single total. Trace adds an unknown support count. Therefore the future template package cardinality is not determinable and the exact basis is blocked.

## 16. Collision Analysis

Canonical role identifiers are unique, so a future injective mapping that retains the exact role segment can avoid collisions among the 12 instances within a per-agent family. Distinct family keys can also distinguish families if the approved path mapping retains them.

That conditional reasoning is not an exact collision proof. The root, surface tokens, target/shared marker rule, filenames, marker/suffix, support extensions, trace scope, and normalization equivalence rules are unresolved. Case-folded, normalized, cross-family, and target-purpose collisions therefore cannot be evaluated over enumerated paths. Collision proof is `BLOCKED`.

## 17. Preexistence And Mutation Policy

This retry creates only this decision record. Every future template write remains create-only and requires a separate exact-allowlist `PASS`. A future write must narrowly prove every allowlisted target absent and fail if any intended target exists.

Overwrite, append, merge, replacement, delete-and-recreate, rename, repair, normalization, and mutation remain unauthorized. Comparison against existing templates remains blocked unless a separate later phase explicitly authorizes it. Preexistence must never become naming, layout, or body evidence. Because exact candidate paths are unavailable, no template preexistence or collision check is performed here.

## 18. Placeholder Basis

Only these categories are supported: matrix role identity, target surface, activation temperature, source reference, shard role, traceability reference, and project context hook.

No final syntax, identifier, value, placement, producer/consumer binding, or expansion is selected. Every later placeholder must have one allowed category, one producer, one consumer, validation expectations, and fail-closed missing-value behavior. Placeholders must not encode copied sources, Target or GitHub state, writes, extra agents, or unresolved architecture. Placeholder work cannot resolve the root, filename, shard-count, or body-source gaps in this decision.

## 19. Source-Selection Basis

The primary specification supports only category-level source eligibility and family-level selection constraints. Every actual future source must be explicitly authorized, read, relevant to its family, assigned an allowed category, and bound to a semantic obligation. Existing templates are always excluded.

The specification also requires explicit precedence for conflicting sources. No complete exact source set or precedence record for every family/role instance was supplied or authorized in this retry. Category eligibility is therefore preserved, but exact body-source selection is `BLOCKED`.

## 20. Transformation Basis

All later transformations must be non-literal, summarizing, project-prepared, behavior-preserving through traceability, bounded by family purpose, shard-aware, activation-temperature aware where applicable, target-surface aware, agent-role aware, shared-rule aware, and fail-closed.

Copying full kernels, full Senior Agent Profiles, complete docs, or existing templates is prohibited. Monolithic agents, final placeholder filling, rendered output, and unsupported obligations are prohibited. Family-level transformations are recorded in `BB-01` through `BB-09`, but exact obligation-to-source-to-temperature transformations remain incomplete and blocked.

## 21. No-Copy And Anti-Bloat Basis

The no-copy rule is established as a mandatory future constraint: kernels and Senior Agent Profiles are semantic authorities, not payloads; docs are consumed on demand; entrypoints remain minimal; shared behavior is represented once; per-agent material remains role-specific; manifests route rather than duplicate; and trace proves coverage without reproducing sources.

This is a sufficient documentary no-copy and anti-bloat basis for evaluating a later proposal, but it is not proof about unwritten templates. No line-count or byte-count threshold is invented. Any later source dump, duplicated shared behavior, monolithic body, or unrelated content must fail.

## 22. Traceability Basis

Every transformed obligation must bind an allowed source reference, selected semantic obligation, transformation rule, destination family and scope, activation temperature when applicable, and shared-versus-role-specific disposition. Textual identity is neither required nor sufficient.

The traceability rule is established, but the physical trace family remains unresolved: exact scope, cardinality, path, filename, format, source set, and instance bindings are absent. Therefore traceability policy is ready as a constraint while `trace-shard` exact basis is `BLOCKED`.

## 23. Gaps, Blockers, Or Warnings

Hard blockers are:

1. no authorized literal dev-template root;
2. no exact normalized target-surface segment mapping;
3. no target-level role/marker omission rule;
4. no template marker or suffix choice and placement rule;
5. no exact dev-template basename rule, including compound-extension handling;
6. no exact physical filename/extension/target-location basis for START, MANIFEST, or trace support;
7. no canonical decision whether each at-most-one shared family is required;
8. no exact per-agent shard inventory supporting the two 12-instance support families;
9. no trace-shard scope or cardinality;
10. no complete exact source bindings and conflict precedence per family/role;
11. no exact activation-temperature disposition for applicable obligations;
12. no exact total future package cardinality; and
13. no enumerable-path collision proof.

The historical accidental read is a contamination warning only; it was isolated and not used. No blocker may be bypassed by inspecting templates, the productive skill, a real Target, GitHub, or filesystem layout.

## 24. Future Exact-Allowlist Readiness

Status: `NOT_READY`.

The future phase `MATERIALIZATION_FULL_FLOW_DEV_SKILL_DRY_RUN_IN_MEMORY_PROJECT_LOCAL_AGENT_MICRO_PACK_TEMPLATE_WRITE_EXACT_ALLOWLIST_DECISION_RETRY` must not start from this blocked result. It would require exact path enumeration and approved body-basis bindings that do not yet exist.

A separate documentation-only canonical-basis phase must first resolve every hard blocker in Section 23 without reading existing templates or producing template content. After that evidence exists, this path/filename/body-basis retry must be rerun. Only its later `PASS` may enable an exact-allowlist retry.

## 25. Abort Criteria

Abort any successor phase if it needs existing template content, the historical accidental read, the productive skill, a real Target, GitHub, an existing-file edit, more files than authorized, directory creation without authorization, inferred paths or names, invented shard/cardinality/source/temperature decisions, final template prose, filled placeholders, copied full sources, rendered output, fixtures, snapshots, parser/renderer/schema/validator/checker/materializer work, an Aggregator change, or a tenth check.

Abort also if agent cardinality differs from 12 roles and 24 entrypoint artifacts, support is counted as agents, `.sentinel/agents/**` becomes a third matrix, exact paths cannot be enumerated, or collision/preexistence proof cannot be completed.

## 26. Final Status

`TEMPLATE_PATH_FILENAME_AND_BODY_BASIS_DECISION_RETRY_BLOCKED_PENDING_CANONICAL_BASIS`

Path basis: `BLOCKED`.

Filename basis: `BLOCKED`.

Body basis: `BLOCKED`, with family-level constraints recorded but exact source and transformation bindings incomplete.

Matrix invariant: `PASS` for exactly 12 roles and 24 conceptual agent entrypoint artifacts.

Exact-allowlist readiness: `BLOCKED`.

Template writing, creation, mutation, replacement, rendering, and materialization remain blocked.

