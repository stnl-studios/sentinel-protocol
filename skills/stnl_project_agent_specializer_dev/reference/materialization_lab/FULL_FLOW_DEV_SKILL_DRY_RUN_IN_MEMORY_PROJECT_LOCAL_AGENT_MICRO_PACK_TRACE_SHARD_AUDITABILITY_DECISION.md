# Full-Flow Dev-Skill Dry-Run In-Memory Project-Local Agent Micro-Pack Trace Shard Auditability Decision

## 1. Status

READY.

This is a decision-only, documentation-only, dev-only, single-document-only,
forward-only, trace-shard-auditability-decision-only,
conceptual-trace-only, auditability-vocabulary-only, non-authorizing artifact
for `stnl_project_agent_specializer_dev`.

It creates no real trace, final trace structure, final trace path, final trace
inventory, template, placeholder, rendered output, fixture, snapshot,
persisted report, checker, validator, measurement tool, parser, renderer,
schema, Target artifact, GitHub artifact, branch, commit, pull request, or
Aggregator change.

## 2. Phase

`MATERIALIZATION_FULL_FLOW_DEV_SKILL_DRY_RUN_IN_MEMORY_PROJECT_LOCAL_AGENT_MICRO_PACK_TRACE_SHARD_AUDITABILITY_DECISION`

## 3. Verdict

`MATERIALIZATION_FULL_FLOW_DEV_SKILL_DRY_RUN_IN_MEMORY_PROJECT_LOCAL_AGENT_MICRO_PACK_TRACE_SHARD_AUDITABILITY_DECISION: PASS_WITH_IMPLEMENTATION_BLOCKED`

Chosen posture:

`TRACE_SHARD_AUDITABILITY_CONCEPTUAL_ONLY_IMPLEMENTATION_BLOCKED`

`trace` may be described conceptually as future auditability support for
`PROJECT_LOCAL_AGENT_MICRO_PACK_MATRIX`. `trace` remains audit-only. It is not
a mirror, persisted report, rendered output, source dump, template,
placeholder inventory, checker, validator, measurement tool, enforcement
mechanism, audit runtime, or materialization authorization.

## 4. Objective

Formalize a conservative conceptual decision for how the `trace` shard family
may support future auditability within:

`PROJECT_LOCAL_AGENT_MICRO_PACK_MATRIX`

This decision records only vocabulary, boundaries, inherited blockers, and the
next safe phase. It does not create trace content, define final trace files,
define final trace structure, define final trace inventory, create audit
implementation, or authorize materialization.

## 5. Scope

Scope is limited to one new documentation artifact under:

`skills/stnl_project_agent_specializer_dev/reference/materialization_lab/`

This document may preserve and relate:

- `PROJECT_LOCAL_AGENT_MICRO_PACK_MATRIX`;
- `PROJECT_LOCAL_AGENT_MICRO_PACK_CONTRACT_PLAN`;
- placeholder categories and gates;
- placeholder final syntax;
- template specification forward stabilization;
- anti-bloat thresholds deferred;
- audit approach deferred;
- template creation gates review;
- support-layer template families;
- placeholder support-layer inventory;
- Aggregator 9-check boundary;
- validation/static contract boundaries.

This document is the only artifact created by this phase.

## 6. Non-goals

This phase does not:

- create real trace;
- define final trace structure;
- define final trace file paths;
- define final trace inventory;
- define final trace schema;
- create source mirror;
- copy kernels, Senior Agent Profiles, `/docs`, complete shards, complete
  support packs, or full instruction bodies;
- create templates;
- alter templates;
- define final template bodies;
- define final template paths;
- define final template inventory;
- define final placeholder inventory;
- create placeholder names, values, namespaces, or executable replacement
  rules;
- create rendered outputs;
- assume rendered outputs;
- create or alter scripts;
- create or alter fixtures;
- create snapshots;
- create persisted reports;
- create checker, validator, measurement tool, parser, renderer, or schema;
- create threshold enforcement;
- define numeric thresholds, trace size limits, trace budgets, trace counts,
  line counts, token budgets, file counts, shard counts, or duplication
  percentages;
- access a real Target;
- infer Target docs maps, stack, commands, ownership, runtime, tooling, or
  project surfaces;
- access GitHub;
- create branch, commit, pull request, issue, or remote artifact;
- access, compare, use as source, or mutate the productive
  `skills/stnl_project_agent_specializer/` skill;
- create `.sentinel/agents/**`;
- create `.github/agents/**`;
- create `.codex/agents/**`;
- create `AGENTS.md`;
- create `.codex/config.toml`;
- alter official child checks;
- alter the Aggregator;
- create a tenth check;
- register mapper, consumer, smoke, test, auxiliary audit, or documentation as
  an official check;
- alter the skill per project.

## 7. Inherited baseline

The inherited architecture remains:

`PROJECT_LOCAL_AGENT_MICRO_PACK_MATRIX`

The architecture exists to materialize light agents in a future authorized
phase, not heavy agents and not source mirrors. It preserves:

- small platform entrypoints:
  - `.github/agents/<agent>.agent.md`;
  - `.codex/agents/<agent>.toml`;
- project-local micro-packs in a future target project:
  - `.sentinel/agents/_shared/**`;
  - `.sentinel/agents/<agent>/**`;
- shard families by temperature:
  - `always`;
  - `hot`;
  - `warm`;
  - `cold`;
  - `trace`;
- `START.md` as the minimal entry;
- `MANIFEST.md` as the activation controller;
- `_shared` as centralization for common rules;
- kernels and Senior Agent Profiles as strong sources, not literal payloads;
- `/docs` consumption on demand, without dump;
- behavior parity through traceability;
- skill immutability per project.

This decision does not reopen the inherited architecture.

## 8. Relation to anti-bloat thresholds deferred

Inherited result:

`MATERIALIZATION_FULL_FLOW_DEV_SKILL_DRY_RUN_IN_MEMORY_PROJECT_LOCAL_AGENT_MICRO_PACK_ANTI_BLOAT_THRESHOLDS_DEFERRED_DOCUMENTATION_AUDIT: EXCELLENT_PASS`

Preserved posture:

`MIXED_QUALITATIVE_WITH_DEFERRED_NUMERIC`

Interpretation:

- qualitative anti-bloat criteria are normative now;
- numeric thresholds remain deferred;
- conceptual bands may exist only as risk language;
- conceptual bands are not enforcement;
- there is no safe basis for numeric values now;
- there is no real Target;
- support-layer templates are not specified;
- representative outputs do not exist;
- measurement policy does not exist;
- no checker, validator, measurement tool, or enforcement is authorized;
- `trace` receives no size limit, budget, count, line count, token budget,
  file count, shard count, or duplication percentage in this phase.

`trace` must remain anti-bloat by design through non-copy boundaries, not
through numeric enforcement.

## 9. Relation to audit approach deferred

Inherited result:

`MATERIALIZATION_FULL_FLOW_DEV_SKILL_DRY_RUN_IN_MEMORY_PROJECT_LOCAL_AGENT_MICRO_PACK_AUDIT_APPROACH_DEFERRED_DECISION_AUDIT: EXCELLENT_PASS`

Preserved posture:

`AUDIT_APPROACH_DEFERRED_WITH_CONCEPTUAL_DIMENSIONS_ONLY`

Interpretation:

- audit approach remains deferred;
- conceptual dimensions may exist only as future vocabulary;
- no audit method is defined;
- no metric is defined;
- no threshold is defined;
- no checker is created;
- no validator is created;
- no measurement tool is created;
- no fixture, snapshot, or persisted report is created;
- no enforcement is created;
- `trace` does not become audit implementation.

This decision defines conceptual auditability vocabulary only.

## 10. Relation to template creation gates review

Inherited result:

`MATERIALIZATION_FULL_FLOW_DEV_SKILL_DRY_RUN_IN_MEMORY_PROJECT_LOCAL_AGENT_MICRO_PACK_TEMPLATE_CREATION_GATES_REVIEW_DECISION_AUDIT: EXCELLENT_PASS`

Preserved posture:

`TEMPLATE_CREATION_GATES_REVIEWED_BUT_CREATION_BLOCKED`

Interpretation:

- gates may be reviewed conceptually;
- template creation remains blocked;
- support-layer template families are not final;
- template paths are not final;
- placeholders are not final;
- rendered outputs are not assumed;
- Target-dependent placeholders are not filled;
- audit approach does not permit template creation;
- numeric anti-bloat thresholds remain deferred;
- no template is created or altered.

`trace` does not relax template creation gates.

## 11. Relation to support-layer template families

Inherited result:

`MATERIALIZATION_FULL_FLOW_DEV_SKILL_DRY_RUN_IN_MEMORY_PROJECT_LOCAL_AGENT_MICRO_PACK_SUPPORT_LAYER_TEMPLATE_FAMILIES_DECISION_AUDIT: EXCELLENT_PASS`

Preserved posture:

`SUPPORT_LAYER_TEMPLATE_FAMILIES_CONCEPTUAL_ONLY_CREATION_BLOCKED`

Interpretation:

- support-layer families may be described conceptually;
- no family is final;
- no family becomes a real file;
- no family becomes a final path;
- no family becomes a template body;
- no family creates final placeholder inventory;
- no family creates rendered output;
- no family authorizes `.sentinel/agents/**`;
- no family counts as an agent;
- no family replaces `.github/agents/**`;
- no family replaces `.codex/agents/**`;
- template creation remains blocked.

`trace` remains a conceptual family relationship, not a final support-layer
structure.

## 12. Relation to placeholder support-layer inventory

Inherited result:

`MATERIALIZATION_FULL_FLOW_DEV_SKILL_DRY_RUN_IN_MEMORY_PROJECT_LOCAL_AGENT_MICRO_PACK_PLACEHOLDER_SUPPORT_LAYER_INVENTORY_DECISION_AUDIT: EXCELLENT_PASS`

Preserved posture:

`PLACEHOLDER_SUPPORT_LAYER_INVENTORY_CONCEPTUAL_ONLY_FINAL_INVENTORY_BLOCKED`

Interpretation:

- conceptual placeholder categories may be described;
- no placeholder is real;
- no placeholder is final;
- no placeholder inventory is final;
- no placeholder becomes an executable contract;
- no placeholder becomes parser behavior;
- no placeholder becomes renderer behavior;
- no placeholder becomes schema;
- no placeholder is filled;
- no Target-dependent placeholder is resolved;
- no placeholder authorizes template creation;
- no placeholder authorizes rendered output;
- `{{AGENT_BODY}}` remains prohibited as a monolithic support-layer body.

`trace` does not create placeholder names, values, namespaces, replacement
rules, or final inventory.

## 13. Canonical invariants

The following invariants remain mandatory:

- `PROJECT_LOCAL_AGENT_MICRO_PACK_MATRIX` is canonical;
- the architecture must materialize light agents, not heavy agents;
- entrypoints remain small and point to local activation;
- `_shared` centralizes common rules;
- per-agent micro-packs remain specific to canonical agents;
- `START.md` stays minimal;
- `MANIFEST.md` controls activation;
- `always` remains minimal;
- `hot`, `warm`, and `cold` remain trigger-governed;
- `trace` is audit-only and not normal operational reading;
- kernels are not copied literally;
- Senior Agent Profiles are not copied literally;
- `/docs` is not dumped;
- behavior parity is traceable through references, summaries, source anchors,
  and transformation notes;
- `.sentinel/agents/**` is not a third matrix of agents;
- `.sentinel/agents/**` does not replace `.github/agents/**`;
- `.sentinel/agents/**` does not replace `.codex/agents/**`;
- the skill remains immutable per project.

## 14. Matrix 12/24/2

The canonical agent matrix remains exactly 12 agents:

1. `orchestrator`
2. `planner`
3. `validation-eval-designer`
4. `execution-package-designer`
5. `designer`
6. `coder-frontend`
7. `coder-backend`
8. `coder-ios`
9. `validation-runner`
10. `reviewer`
11. `finalizer`
12. `resync`

Agent artifacts remain exactly:

- 12 x `.github/agents/<agent>.agent.md`;
- 12 x `.codex/agents/<agent>.toml`.

Target-level artifacts remain exactly:

- `.codex/config.toml`;
- `AGENTS.md`.

Counting restrictions:

- `.codex/config.toml` does not count as an agent;
- `AGENTS.md` does not count as an agent;
- support packs do not count as agents;
- `_shared` does not count as an agent;
- per-agent micro-packs do not count as agents;
- `.sentinel/agents/**` is not a third matrix of agents;
- `.sentinel/agents/**` does not replace `.github/agents/**`;
- `.sentinel/agents/**` does not replace `.codex/agents/**`;
- `trace` does not count as an agent;
- `trace` does not alter the matrix `12/24/2`.

## 15. Aggregator 9-check boundary

The official Validation Harness Aggregator remains closed over exactly 9 child
checks:

1. `scripts/materialization_lab/check-static.mjs`
2. `scripts/materialization_lab/check-source-inventory.mjs`
3. `scripts/materialization_lab/check-template-coverage.mjs`
4. `scripts/materialization_lab/check-fixture-boundary.mjs`
5. `scripts/materialization_lab/check-lazy-load-fixtures.mjs`
6. `scripts/materialization_lab/check-project-scenarios.mjs`
7. `scripts/materialization_lab/check-render-context.mjs`
8. `scripts/materialization_lab/check-dry-run-plan.mjs`
9. `scripts/materialization_lab/check-fixture-render-dry-run-integration.mjs`

Boundary:

- wrapper Aggregator is not a tenth check;
- this phase creates no checker;
- this phase creates no tenth check;
- this phase alters no official child check;
- this phase alters no Aggregator;
- this phase registers no mapper, consumer, test, smoke, auxiliary audit, or
  documentation as an official check;
- trace auditability vocabulary does not become a child check.

## 16. Trace shard auditability posture

Chosen posture:

`TRACE_SHARD_AUDITABILITY_CONCEPTUAL_ONLY_IMPLEMENTATION_BLOCKED`

Meaning:

- `trace` may be described conceptually as auditability support;
- `trace` remains audit-only;
- `trace` is not a mirror;
- `trace` is not a persisted report;
- `trace` is not a rendered output;
- `trace` is not a source dump;
- `trace` is not a template;
- `trace` is not a placeholder inventory;
- `trace` is not a checker;
- `trace` is not a validator;
- `trace` is not a measurement tool;
- `trace` is not enforcement;
- `trace` creates no audit implementation;
- `trace` creates no audit runtime;
- `trace` alters no Aggregator;
- `trace` authorizes no materialization.

## 17. Trace audit-only boundary

`trace` exists here only as a conceptual future auditability category.

Preserved rules:

- `trace` is not normal operational reading;
- `trace` is not loaded by default;
- `trace` does not replace `always`, `hot`, `warm`, or `cold`;
- `trace` does not define workflow runtime;
- `trace` does not authorize executable audit;
- `trace` does not authorize executable validation;
- `trace` does not authorize executable measurement.

## 18. Trace no-mirror boundary

`trace` must not become a mirror of:

- kernels;
- Senior Agent Profiles;
- `/docs`;
- templates;
- rendered outputs;
- Target files;
- source files;
- support packs;
- complete shards;
- full instruction bodies.

`trace` may support references, summaries, source anchors, and transformation
notes as future vocabulary. It must not copy source material wholesale or
become a rewrapped source.

## 19. Trace no-persisted-report boundary

`trace` is not:

- a persisted report;
- a generated audit report;
- a snapshot;
- a fixture;
- a validation output;
- a checker output;
- a CI artifact;
- an evidence bundle;
- a report strategy;
- a report schema.

No report persistence, report generator, report format, report schema, or
report artifact is authorized here.

## 20. Trace source reference boundary

`trace` may use source references as conceptual future vocabulary.

Preserved rules:

- source references are not literal copies;
- source references are not repackaged source bodies;
- source references do not authorize broad `/docs` reading;
- source references do not authorize repo-wide reading;
- source references do not authorize productive skill access;
- source references do not authorize Target access;
- source references do not become rendered output;
- source references do not become final trace structure.

## 21. Trace summary boundary

`trace` may use summaries as conceptual future vocabulary.

Preserved rules:

- summaries are short derivatives, not copies;
- summaries do not replace sources;
- summaries do not carry entire kernels;
- summaries do not carry entire Senior Agent Profiles;
- summaries do not carry `/docs` dumps;
- summaries do not carry all shards;
- summaries do not become template bodies;
- summaries do not become final placeholder values.

## 22. Trace source anchor boundary

`trace` may use source anchors as conceptual future vocabulary.

Preserved rules:

- source anchors are location or connection references, not payload;
- anchors do not authorize broad scraping;
- anchors do not authorize repo-wide reading;
- anchors do not authorize `/docs` dumps;
- anchors do not authorize Target-dependent inference;
- anchors do not become parser rules;
- anchors do not become rendered output;
- anchors do not become audit checks.

## 23. Trace transformation notes boundary

`trace` may use transformation notes as conceptual future vocabulary.

Preserved rules:

- transformation notes explain how a source was condensed or adapted;
- transformation notes are not implementation plans;
- transformation notes are not final template specifications;
- transformation notes are not final placeholder inventories;
- transformation notes are not audit metrics;
- transformation notes are not checker logic;
- transformation notes are not validation rules;
- transformation notes authorize no materialization.

## 24. Behavior parity reference boundary

Behavior parity comes through traceability, not literal copy.

Preserved rules:

- behavior parity does not require mirroring source material;
- behavior parity may be discussed through references, summaries, source
  anchors, and transformation notes;
- behavior parity does not authorize rendered outputs;
- behavior parity does not authorize execution;
- behavior parity does not authorize automatic measurement;
- behavior parity does not authorize enforcement.

## 25. No-copy source material boundary

No-copy policy applies to:

- kernels;
- Senior Agent Profiles;
- `/docs`;
- source documentation;
- full instruction bodies;
- complete shard contents;
- complete support packs;
- complete trace.

`trace` must avoid literal payload and support only conceptual traceability
through references, summaries, source anchors, and transformation notes.

## 26. Trace placeholder relationship boundary

`trace` does not:

- create a real placeholder;
- define a final placeholder;
- define a final placeholder name;
- define a final namespace;
- define a final placeholder value;
- define a final placeholder inventory;
- authorize parser behavior;
- authorize renderer behavior;
- authorize schema;
- authorize placeholder replacement;
- authorize executable placeholder validation;
- authorize `{{AGENT_BODY}}`;
- authorize a monolithic placeholder carrying complete trace.

Placeholder work remains blocked until a separate explicit phase authorizes it.

## 27. Trace support-layer relationship boundary

`trace` is a conceptual family inside the future support layer.

Preserved rules:

- `trace` is not a final support-layer structure;
- `trace` is not a final template family;
- `trace` is not a final file path;
- `trace` is not a final template body;
- `trace` is not a final rendered output;
- `trace` creates no `.sentinel/agents/**`;
- `trace` does not replace `_shared`;
- `trace` does not replace per-agent micro-packs;
- `trace` does not replace entrypoints;
- `trace` does not count as an agent.

## 28. Trace target-dependent boundary

Target posture:

- real Target access is not authorized;
- Target-dependent facts are not available;
- Target-dependent trace is not created;
- Target-dependent anchors are not inferred;
- Target docs map is not inferred;
- Target project surfaces are not inferred;
- Target stack, commands, ownership, runtime, and tooling are not inferred;
- Target validation does not exist;
- Target write does not exist;
- Target rendered outputs do not exist.

Any Target-dependent trace discussion requires a separate explicit phase.

## 29. Trace anti-bloat boundary

`trace` must be anti-bloat by design.

Preserved rules:

- `trace` does not copy whole sources;
- `trace` does not mirror `/docs`;
- `trace` does not mirror kernels;
- `trace` does not mirror Senior Agent Profiles;
- `trace` does not carry all shards;
- `trace` does not become a full report;
- trace size is not defined;
- trace count is not defined;
- trace budget is not defined;
- numeric thresholds remain deferred;
- `trace` creates no enforcement.

## 30. Trace audit readiness boundary

Audit readiness posture:

- audit approach remains deferred;
- trace auditability is conceptual vocabulary;
- trace auditability is not an audit method;
- trace auditability is not a metric;
- trace auditability is not a checker;
- trace auditability is not a validator;
- trace auditability is not a measurement tool;
- trace auditability is not a fixture;
- trace auditability is not a snapshot;
- trace auditability is not a persisted report;
- trace auditability is not an Aggregator child;
- trace auditability is not enforcement.

## 31. Trace finalization blockers

Trace finalization remains blocked because:

- final support-layer structure does not exist;
- final template families do not exist;
- final template paths do not exist;
- final placeholder inventory does not exist;
- rendered outputs do not exist;
- parser is not authorized;
- renderer is not authorized;
- schema is not authorized;
- real Target access is not authorized;
- Target-dependent anchors are unavailable;
- audit approach is deferred;
- numeric thresholds are deferred;
- measurement policy does not exist;
- fixture, snapshot, and report strategy does not exist;
- validation strategy does not exist;
- materialization is not authorized.

## 32. What cannot be finalized yet

The following cannot be finalized in this phase:

- final trace structure;
- final trace file paths;
- final trace inventory;
- final trace schema;
- final trace placeholder names;
- final trace placeholder values;
- final trace source map;
- final trace anchor map;
- final transformation note format;
- final behavior parity method;
- final audit method;
- final measurement policy;
- final report strategy;
- final fixture strategy;
- final snapshot strategy;
- final validation strategy;
- final rendered trace output;
- final Target-dependent trace behavior;
- final materialization behavior.

## 33. Forbidden inferences

This decision does not permit inferring:

- trace implementation;
- trace runtime;
- trace materialization;
- final trace structure;
- final trace path;
- final trace inventory;
- final trace schema;
- persisted trace report;
- audit report;
- fixture;
- snapshot;
- checker;
- validator;
- measurement tool;
- threshold enforcement;
- Aggregator change;
- tenth check;
- source mirror;
- `/docs` dump;
- kernel copy;
- Senior Agent Profile copy;
- complete shard copy;
- support pack copy;
- final placeholder;
- final placeholder inventory;
- parser;
- renderer;
- schema;
- template creation;
- template mutation;
- rendered outputs;
- `.sentinel/agents/**` materialized;
- `.github/agents/**` materialized;
- `.codex/agents/**` materialized;
- `AGENTS.md` materialized;
- `.codex/config.toml` materialized;
- Target access;
- GitHub write;
- productive skill access;
- support layer as a third matrix;
- support packs as agents;
- alteration of matrix `12/24/2`;
- alteration of the Aggregator 9-check boundary.

## 34. Allowed future decision paths

The following future paths may be considered only in separate explicit phases:

- `TRACE_SHARD_AUDITABILITY_DECISION_AUDIT`;
- `SUPPORT_LAYER_STRUCTURE_DECISION`;
- `TEMPLATE_SPECIFICATION_DOCUMENTATION_PLAN_DECISION`;
- `TARGETLESS_RENDERING_CONTRACT_DECISION`;
- `PLACEHOLDER_TARGET_DEPENDENCY_CLASSIFICATION_DECISION`;
- `TRACE_SOURCE_REFERENCE_MODEL_DECISION`.

Primary next path:

`TRACE_SHARD_AUDITABILITY_DECISION_AUDIT`

This list does not authorize any implementation, template, trace output,
report, parser, renderer, schema, checker, validator, measurement tool,
Target access, GitHub write, or materialization.

## 35. Recommended safe next phase

Recommended immediate next phase:

`MATERIALIZATION_FULL_FLOW_DEV_SKILL_DRY_RUN_IN_MEMORY_PROJECT_LOCAL_AGENT_MICRO_PACK_TRACE_SHARD_AUDITABILITY_DECISION_AUDIT`

The next phase should be:

- read-only;
- path-limited;
- documentation-only audit;
- single-document-focused;
- without writes;
- without GitHub;
- without real Target;
- without productive skill access;
- without scripts;
- without templates;
- without fixtures;
- without snapshots;
- without persisted reports;
- without Aggregator change;
- without checker change;
- without validator;
- without measurement tool;
- without threshold enforcement;
- without parser;
- without renderer;
- without schema;
- without final trace structure;
- without final trace paths;
- without final trace inventory;
- without source mirror;
- without `/docs` dump;
- without rendered outputs.

## 36. Explicit non-authorization

This document authorizes no action beyond preserving this single documentary
decision.

Explicitly not authorized:

- implementation;
- materialization;
- runtime behavior;
- trace output;
- final trace structure;
- final trace path;
- final trace inventory;
- source mirror;
- source dump;
- template creation;
- template mutation;
- placeholder creation;
- final placeholder inventory;
- parser;
- renderer;
- schema;
- checker;
- validator;
- measurement tool;
- threshold enforcement;
- fixture;
- snapshot;
- persisted report;
- audit implementation;
- audit runtime;
- Aggregator change;
- official child-check change;
- tenth check;
- Target access;
- GitHub write;
- branch;
- commit;
- pull request;
- productive skill access;
- `.sentinel/agents/**` creation;
- `.github/agents/**` creation;
- `.codex/agents/**` creation;
- `AGENTS.md` creation;
- `.codex/config.toml` creation.

The decision remains conceptual, documentary, dev-only, forward-only, and
non-authorizing.
