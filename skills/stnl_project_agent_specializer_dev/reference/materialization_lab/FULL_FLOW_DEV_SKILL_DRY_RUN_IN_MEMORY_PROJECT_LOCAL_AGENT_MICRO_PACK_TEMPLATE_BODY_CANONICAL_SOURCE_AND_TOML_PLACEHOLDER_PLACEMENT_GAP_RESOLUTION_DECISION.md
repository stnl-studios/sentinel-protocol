# Project-Local Agent Micro-Pack Template Body Canonical Source And TOML Placeholder Placement Gap Resolution Decision

## Status

`BLOCKED_PENDING_BODY_CANONICAL_SOURCE_AND_TOML_PLACEHOLDER_PLACEMENT_GAP_RESOLUTION`

## Phase

`MATERIALIZATION_FULL_FLOW_DEV_SKILL_DRY_RUN_IN_MEMORY_PROJECT_LOCAL_AGENT_MICRO_PACK_TEMPLATE_BODY_CANONICAL_SOURCE_AND_TOML_PLACEHOLDER_PLACEMENT_GAP_RESOLUTION_DECISION`

## Verdict

`BLOCKED_WITHOUT_BODY_CANONICAL_SOURCE_AND_TOML_PLACEHOLDER_PLACEMENT_GAP_RESOLUTION`

Posture:

`TEMPLATE_BODY_CANONICAL_SOURCE_AND_TOML_PLACEHOLDER_PLACEMENT_GAP_RESOLUTION_BLOCKED_PENDING_CANONICAL_GAP_INPUTS`

## Objective

Determine, without reading or writing templates, whether the existing documentary basis safely defines the canonical source for final bodies, the complete Markdown and TOML structures, generated-notice handling, exact placeholder placement, and format-aware escaping needed before a body-content-structure retry. The source policy can be bounded, but the complete gap resolution cannot be defined without inventing missing canonical inputs.

## Scope

This phase is documentation-only and limited to this single new decision document under `skills/stnl_project_agent_specializer_dev/reference/materialization_lab/`. It evaluates inherited records and consolidated audits. It does not create templates, candidate parent directories, rendered outputs, fixtures, snapshots, executable mechanisms, Target artifacts, or any second file.

## Non-goals

This phase does not draft final bodies; choose final Markdown headings, prose, or slots; choose complete TOML keys, tables, order, types, comments, or escaping; define generated-notice wording or placement; place or fill placeholders; create the 52 candidates; finalize the physical support layer or trace schema; access a real Target, GitHub, or the repository productive-skill path; or alter the Aggregator. A later read-hygiene section records an unauthorized host-installed skill-instruction read that deviated from this intended boundary.

## Inherited baseline

The inherited direction is `PROJECT_LOCAL_AGENT_MICRO_PACK_MATRIX`. The canonical matrix remains 12 agents, 24 conceptual agent entrypoints, and 2 target-level artifacts. The exact allowlist remains documentary with 52 conceptual create-only candidates. Body-basis records, the closed placeholder inventory and syntax, bounded support-layer decisions, audit-only trace posture, qualitative anti-bloat posture, and the nine-check Aggregator boundary remain available but do not collectively define final body serialization.

## Relation to blocked body content structure decision

The immediately preceding decision recorded status `BLOCKED_PENDING_BODY_CONTENT_STRUCTURE_AND_PLACEHOLDER_POLICY` and verdict `BLOCKED_WITHOUT_BODY_CONTENT_STRUCTURE_AND_PLACEHOLDER_POLICY`. It identified incomplete Markdown structures, incomplete Codex entrypoint mapping, absent target-config structure, unresolved generated notice, unresolved exact placement, and unresolved format-aware escaping. This decision confirms that those gaps are not closed by the current documentary basis.

## Relation to template read contamination

The prior phase's unauthorized template reads remain a historical blocking fact only. Their content was discarded and is not evidence here. No existing template, no path under `reference/templates/**`, and no template listing or search was read in this phase. Therefore, there is no new template-read contamination; the historical contamination is not repeated or cured by inference.

## Relation to exact allowlist decision retry

The exact allowlist continues to establish 52 unique conceptual paths and create-only intent. It does not authorize current writing, parent-directory creation, final body content, placeholder placement, or reuse of existing templates.

## Relation to path filename body basis decision

The body-basis records remain semantic evidence for purpose, obligations, source classes, exclusions, and non-literal transformation. They are not final bodies and do not specify complete format structure, exact headings, final prose slots, complete TOML serialization, generated notices, or exact placeholder placement.

## Relation to canonical basis gap resolution

Canonical roots, candidate identity, cardinality, naming grammar, collision posture, and trace disposition remain inherited. Those facts bound candidate identity but do not resolve body serialization.

## Relation to layout naming and source selection

Layout and naming remain documentary and fixed for candidate identity. Source selection forbids deriving body content from filenames, conceptual Target paths, filesystem observation, or existing template content. A resolved path is not evidence for a body shape.

## Relation to placeholder final inventory

The closed inventory remains the nominal authority for existing placeholder names and namespaces. It provides no values and does not establish exact per-family, per-file, per-heading, per-field, or per-TOML-value placement.

## Relation to placeholder final syntax

The token form and negative syntax constraints remain inherited. Unknown, renamed, nested, monolithic, and multiline identifiers remain prohibited. The syntax decision does not provide context-specific Markdown or TOML escaping and does not authorize placement or filling.

## Relation to support layer finalization

The support layer remains bounded, project-local, non-agent, and not physically finalized. Common rules belong in `_shared`; per-agent support remains role-specific; `START.md` remains activation-oriented; `MANIFEST.md` remains selective-loading metadata; and `/docs` remains on-demand.

## Relation to support-layer template families

Shared support, per-agent support, activation control, temperature support, and trace support remain conceptual families. Family membership does not provide final headings, slots, notice placement, placeholder placement, or escaping.

## Relation to trace audit closeout

Trace remains audit-only, non-operational, non-mirroring, and represented by zero template files. Trace records do not authorize a trace body, schema, report, loader, persisted file, fixture, or snapshot.

## Relation to anti-bloat posture

Entrypoints and activation controls remain small and routing-oriented; manifests remain inventory-oriented; shared rules remain centralized; shards remain selectively loaded; and kernels, Senior Agent Profiles, and `/docs` remain semantic sources rather than literal payloads. Numeric thresholds remain deferred.

## Relation to Aggregator boundary

The official Aggregator remains closed at exactly nine child checks. Its wrapper is not a tenth check. This document is not a checker, validator, schema, gate implementation, or Aggregator child, and it does not alter any official check.

## Canonical invariants

- The 12 canonical agent identities remain unchanged.
- The 24 conceptual agent entrypoints and 2 target-level artifacts remain conceptual.
- The 52 candidates remain unwritten and create-only in any future authorized write phase.
- Templates, support packs, `_shared`, per-agent micro-packs, families, placeholders, shards, and trace are not agents.
- `.sentinel/agents/**` is not a third agent matrix.
- Placeholder values and Target-dependent filling remain blocked.
- Existing templates and filesystem observations are forbidden body sources.
- No-copy, lazy-load, docs-on-demand, fail-closed, and audit-only trace postures remain mandatory.

## Matrix 12/24/2 preservation

This decision preserves exactly 12 canonical agents, 24 conceptual platform agent artifacts, and 2 conceptual target-level artifacts. `.codex/config.toml` and `AGENTS.md` do not count as agents. The 26 conceptual support candidates complete the documentary 52-candidate set without creating another agent matrix.

## Gap resolution posture

The body canonical source policy is defined only as a policy boundary. The overall gap resolution is blocked because the canonical inputs needed to define final Markdown structures, complete TOML structures, generated notices, exact placeholder placement, and context-aware escaping are absent. It is additionally blocked by the unauthorized host-installed skill-instruction read recorded below. Template writing and the body-content-structure retry remain blocked.

## Decision model

`BASIS_READY` means the documentary evidence safely supports the stated fact or negative boundary. `DEFERRED` means a future documentary decision is required but the item is not itself evidence of present readiness. `BLOCKED` means adopting the item now would require invention or violate a boundary. These labels are documentary only and are not executable gates, schema values, checker output, or validator output.

The consolidated audit results are: body canonical source policy-only `BASIS_READY`; TOML entrypoint `BLOCKED`; target config `BLOCKED`; all six Markdown families `BLOCKED`; generated notice `BLOCKED`; placeholder placement `BLOCKED`; escaping `BLOCKED`; no-copy `BASIS_READY`; family coverage overall `BLOCKED`; create-only future posture `BASIS_READY`; and matrix/Aggregator preservation `BASIS_READY`.

## Decision criteria

The decision evaluates all 49 required criteria against the explicitly permitted documentary basis and the clean-read boundary. A partial policy result cannot be promoted to an overall pass: every serialization-sensitive criterion must be supported without relying on templates, platform assumptions, filesystem observation, Target facts, GitHub facts, or the productive skill.

## Decision result

| Criterion | Status | Evidence basis | Decision meaning | Still blocked |
| --- | --- | --- | --- | --- |
| Previous body structure blocked result understood | `BASIS_READY` | Prior blocked decision | The prior result and its gaps are understood | Gap closure |
| Previous template-read contamination recorded but not repeated | `BASIS_READY` | Prior contamination record and current read hygiene | Historical contamination is excluded; no new contamination occurred | Historical result remains blocked |
| Read hygiene preserved | `BLOCKED` | A writer subagent read `/Users/ajfiumanee/.agents/skills/stnl_project_agent_specializer/SKILL.md`, outside the allowed documentary paths | No template content was read, but the phase was not fully path-clean | Clean rerun without the host-installed skill read |
| Exact allowlist remains documented | `BASIS_READY` | Exact-allowlist decision | 52 entries remain documentary | Writing and parent creation |
| 52 candidate files remain conceptual | `BASIS_READY` | Scope and allowlist | No candidate is materialized | All 52 files |
| Body-basis records available | `BASIS_READY` | Body-basis decision | Semantic records exist | Final serialization |
| Body canonical source can be defined | `BASIS_READY` | Body-basis and source-selection records | A policy-only source boundary can be defined | Final bodies and structures |
| Forbidden body sources can be defined | `BASIS_READY` | Source-selection and no-copy boundaries | Templates, filesystem, Target, GitHub, and productive skill are excluded | Any prohibited source use |
| Markdown family headings can be defined | `BLOCKED` | Markdown audit | No canonical heading sets exist for all six families | Required and optional headings and order |
| Markdown slots can be defined | `BLOCKED` | Markdown audit | No canonical slot map exists | Purpose and placeholder slots |
| Markdown prose boundary can be defined | `BLOCKED` | Markdown audit and qualitative anti-bloat evidence | Negative boundaries exist, but final prose contract does not | Family prose rules |
| TOML Codex entrypoint keys can be defined | `BLOCKED` | TOML entrypoint audit | Six names are documented but completeness and mapping are unresolved | Canonical key set |
| TOML Codex entrypoint ordering can be defined | `BLOCKED` | TOML entrypoint audit | No canonical order is supported | Key order |
| TOML Codex entrypoint value types can be defined | `BLOCKED` | TOML entrypoint audit | No canonical per-key type contract is supported | Value types |
| TOML target config keys can be defined | `BLOCKED` | Target-config audit | No canonical key set is available | Config keys |
| TOML target config ordering can be defined | `BLOCKED` | Target-config audit | No canonical order is available | Config order |
| TOML target config value types can be defined | `BLOCKED` | Target-config audit | No canonical types are available | Config types |
| TOML comments policy can be defined | `BLOCKED` | TOML and notice audits | Comment allowance and placement are unresolved | Comment policy |
| Generated notice policy can be defined | `BLOCKED` | Generated-notice audit | Requirement, wording class, placement, style, and duplication are unresolved | Entire notice policy |
| Placeholder namespaces can be placed | `BLOCKED` | Placement audit | Namespaces are known; exact eligible locations are not | Family, heading, field, and value placement |
| Placeholder required/optional/deferred status can be defined | `BLOCKED` | Placement audit | Per-location status is unavailable | Required/optional classification |
| Placeholder values remain blocked | `BASIS_READY` | Inventory boundary | No value is supplied | Every value |
| Target-dependent filling remains blocked | `BASIS_READY` | Inventory and Target boundary | No Target fact may be inferred or filled | All Target-dependent filling |
| Placeholders excluded from paths | `BASIS_READY` | Fixed candidate identity | Paths remain literal and documentary | Any token in a path |
| Placeholders excluded from filenames | `BASIS_READY` | Fixed naming grammar | Filenames remain literal | Any token in a filename |
| Placeholders excluded from TOML keys | `BASIS_READY` | Format-safety negative boundary | Keys cannot be tokenized | Any token in a key or table name |
| Placeholders excluded from Markdown headings | `BASIS_READY` | Format-safety negative boundary | Headings cannot be tokenized | Any token in a heading |
| Forbidden placeholders preserved | `BASIS_READY` | Inventory and syntax decisions | `{{AGENT_BODY}}`, unknown, renamed, nested, multiline, and monolithic tokens remain forbidden | Any forbidden token use |
| Markdown escaping can be defined | `BLOCKED` | Escaping audit | No context-specific canonical escaping is available | Markdown escaping |
| TOML escaping can be defined | `BLOCKED` | Escaping audit | Quotes, backslashes, newlines, comments, and token preservation are unresolved | TOML escaping |
| No-copy policy preserved | `BASIS_READY` | Body-basis exclusions and anti-bloat decisions | Sources remain semantic, not payload | Literal or near-literal source copying |
| Anti-bloat posture preserved | `BASIS_READY` | Anti-bloat decision | Small and selective structure remains required | Numeric thresholds |
| Trace audit-only posture preserved | `BASIS_READY` | Trace closeout | Trace remains non-operational | Trace implementation |
| Trace template files remain zero | `BASIS_READY` | Allowlist and trace closeout | No trace template is required | Any trace template file |
| Create-only can remain future default | `BASIS_READY` | Exact allowlist and create-vs-mutate audit | A future write may only reconsider create-only/all-or-nothing | Current write authorization |
| Mutation remains blocked | `BASIS_READY` | Create-vs-mutate audit | Overwrite, append, merge, repair, rename, and replacement remain prohibited | Every mutation mode |
| No templates read | `BASIS_READY` | Current read-hygiene record | No template was read in this phase | Any later template read |
| No templates created | `BASIS_READY` | Current scope | No template was created | Template creation |
| No rendered outputs | `BASIS_READY` | Current scope | No rendered output was created | Rendering |
| No fixtures | `BASIS_READY` | Current scope | No fixture was created | Fixture creation |
| No snapshots | `BASIS_READY` | Current scope | No snapshot was created | Snapshot creation |
| No parser renderer schema | `BASIS_READY` | Current scope | No parser, renderer, or schema was created | All three mechanisms |
| No validator checker materializer | `BASIS_READY` | Current scope | No validator, checker, or materializer was created | All three mechanisms |
| Matrix 12/24/2 preserved | `BASIS_READY` | Matrix audit | Cardinality and classifications are unchanged | Any matrix change |
| Aggregator 9-check boundary preserved | `BASIS_READY` | Aggregator audit | No child check or wrapper semantics changed | Aggregator change or tenth check |
| Target real remains blocked | `BASIS_READY` | Scope boundary | No real Target is evidence or output | Target access and mapping |
| GitHub remains blocked | `BASIS_READY` | Scope boundary | No GitHub operation is evidence or output | GitHub access |
| Productive skill remains blocked | `BLOCKED` | The repository path `skills/stnl_project_agent_specializer/` was not accessed, but the same-named host-installed skill instruction was read | The read was discarded as canonical evidence and is a scope violation | Clean rerun with no productive or same-named installed skill read |
| Current phase writes limited to one document | `BASIS_READY` | Explicit write permission | This is the only authorized new file | Any second write |

This table is not a schema, checker, validator, executable gate, or template, and it does not authorize template writing.

## Read hygiene and contamination prevention

Documentary evidence reads were restricted to explicitly permitted paths and consolidated audit results. No broad search, recursive listing, or content read crossed into `reference/templates/**`. No existing template content was opened, returned, compared, summarized, or used. The prior phase's contamination was recorded without reopening templates. New template contamination: `NONE`.

The writer subagent nevertheless read `/Users/ajfiumanee/.agents/skills/stnl_project_agent_specializer/SKILL.md`. This was not the forbidden repository path `skills/stnl_project_agent_specializer/`, was not a template, and was not used as canonical evidence, but it was outside the phase's allowed read set. The content is discarded. This scope deviation independently requires `BLOCKED` and a clean rerun.

## Body canonical source policy

The following policy boundary is adopted:

- body-basis records may supply semantic purpose, obligations, exclusions, and non-literal transformation requirements;
- the placeholder inventory may supply nominal placeholder identity only;
- the placeholder syntax decision may supply token form and negative syntax constraints only;
- support-layer decisions may supply responsibility and selective-loading boundaries only;
- the matrix decision may supply agent identity and cardinality only;
- trace decisions may supply audit-only and zero-file posture only;
- anti-bloat decisions may supply qualitative small-body and no-copy boundaries only;
- applicable materialization-lab contracts may supply boundaries only;
- a future canonical serialization specification must supply the missing Markdown, TOML, notice, placement, and escaping decisions before any final body can be derived.

No permitted source class alone is a final body. This policy defines source eligibility, not final content or serialization.

## Forbidden body sources

Existing templates; template listings; filesystem observation; nearby files; conceptual Target paths; filenames; real Target facts; capability discovery; GitHub facts; the productive skill; runtime defaults; platform recollection; legacy placeholders; unapproved examples; kernels or Senior Agent Profiles as literal payload; `/docs` dumps; source mirrors; and any inferred body, heading, key, order, type, notice, placement, or escaping rule are forbidden sources.

## Allowed body source evidence

Allowed evidence is limited to the explicitly permitted materialization-lab decisions and contracts: exact allowlist records, body-basis records, canonical basis and layout/naming/source-selection records, placeholder inventory and syntax records, support-layer decisions, matrix records, trace decisions, qualitative anti-bloat decisions, and applicable boundary contracts. Their use is constrained to the fact each record actually resolves. None may be stretched into an absent serialization decision.

## Body derivation model

A future body may be derived only from the intersection of: one exact candidate record, its semantic body-basis, the applicable matrix/support/trace/anti-bloat boundaries, the closed placeholder inventory and syntax, and a future passing canonical serialization specification. Because that final specification does not exist, derivation stops before headings, prose, TOML serialization, notice insertion, placeholder placement, or escaping. No final body is derivable now.

## Markdown family structure resolution

| Family | Required headings | Optional headings | Heading order | Slots and purpose | Prose boundary | Generated notice | Resolution |
| --- | --- | --- | --- | --- | --- | --- | --- |
| GitHub platform entrypoint | Not canonically specified | Not canonically specified | Not canonically specified | Not canonically specified | Negative no-copy/anti-bloat rules only | Not canonically specified | `BLOCKED` |
| Target-level AGENTS guide | Not canonically specified | Not canonically specified | Not canonically specified | Not canonically specified | Negative no-copy/docs-on-demand rules only | Not canonically specified | `BLOCKED` |
| Shared START | Not canonically specified | Not canonically specified | Not canonically specified | Not canonically specified | Minimal activation intent only | Not canonically specified | `BLOCKED` |
| Shared MANIFEST | Not canonically specified | Not canonically specified | Not canonically specified | Not canonically specified | Inventory intent only | Not canonically specified | `BLOCKED` |
| Per-agent START | Not canonically specified | Not canonically specified | Not canonically specified | Not canonically specified | Role-scoped activation intent only | Not canonically specified | `BLOCKED` |
| Per-agent MANIFEST | Not canonically specified | Not canonically specified | Not canonically specified | Not canonically specified | Selective role inventory intent only | Not canonically specified | `BLOCKED` |

For every family, body-monolithic content, repo-wide reading by default, `/docs` full dumps, source copying, and load-all behavior remain forbidden. Those negative rules do not define a final structure.

## Markdown heading and slot policy

No required heading, optional heading, heading order, list/table form, section purpose, prose slot, placeholder slot, reference slot, or notice slot is adopted. Placeholders remain forbidden in headings. Exact heading and slot policy requires the recommended intermediate canonical specification phase.

## Markdown prose boundary

Only negative and qualitative boundaries are ready: prose must not copy sources, mirror a kernel or Senior Agent Profile, dump `/docs`, embed whole shards, duplicate common rules, create a monolithic body, or require repo-wide reading by default. Exact family wording, mandatory statements, maximum qualitative detail, list policy, table policy, and section prose remain blocked.

## TOML entrypoint structure resolution

The documentary basis mentions six Codex agent field names: `name`, `description`, `model`, `model_reasoning_effort`, `sandbox_mode`, and `developer_instructions`. This mention is not adopted as a complete or ordered canonical template structure. Completeness, required-versus-optional status, key order, per-key types, namespaced-versus-legacy placeholder mapping, allowed placeholders by value, forbidden value contexts, developer-instructions representation, comments, generated notice, multiline handling, and escaping remain unresolved. No `.codex/agents/*.toml` file is created or authorized.

## TOML target config structure resolution

No canonical target-config keys, tables, dotted keys, ordering, required/optional status, value types, comments, notice placement, placeholder placement, string form, or multiline handling are available. Defining any of them now would be an unsupported inference. Real Target values, capability discovery, runtime mutation, and creation of `.codex/config.toml` remain prohibited.

## TOML key ordering policy

No TOML key order is adopted for either Codex agent entrypoints or the target-level config. Ordering cannot be inferred from partial key mentions, platform knowledge, examples, templates, runtime conventions, or filesystem observation.

## TOML value type policy

No final per-key TOML value type is adopted. Strings, multiline strings, arrays, booleans, integers, tables, inline tables, dotted keys, and comments may not be authorized by implication. A future canonical specification must explicitly bind every permitted key to one type and multiline policy.

## TOML comment policy

Whether comments are required, optional, or prohibited; where they may appear; whether a generated notice may be a comment; and how comment characters interact with values and placeholders remain blocked. No comment wording or placement is adopted.

## Generated notice policy

Generated-notice requirement, wording class, Markdown placement, TOML placement, TOML comment style, placeholder eligibility, shared-versus-family-specific form, duplication boundary, and escaping implications are all blocked. No generated notice is invented, adopted, rendered, or inserted.

## Placeholder placement policy

| Namespace | Allowed families | Allowed placement | Forbidden placement | Required optional or deferred | Value status | Escaping context | Target dependency handling | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `shared` | Not canonically resolved | Not canonically resolved | Paths, filenames, Markdown headings, TOML keys/table names, comments, monolithic body slots | `DEFERRED` | Unfilled and blocked | Not canonically resolved | No Target inference or filling | Nominal inventory only |
| `agent` | Not canonically resolved | Not canonically resolved | Paths, filenames, Markdown headings, TOML keys/table names, comments, `{{AGENT_BODY}}`, monolithic body slots | `DEFERRED` | Unfilled and blocked | Not canonically resolved | No Target inference or filling | Identity does not imply placement |
| `shard` | Not canonically resolved | Not canonically resolved | Paths, filenames, Markdown headings, TOML keys/table names, comments, physical shard bodies, loader definitions | `DEFERRED` | Unfilled and blocked | Not canonically resolved | No Target discovery or filling | Zero direct shard candidates |
| `trace` | No template family is authorized for populated use | None currently authorized | All template bodies, paths, filenames, headings, TOML keys/table names, comments, reports, schemas, persisted trace files | `DEFERRED` | Unfilled and blocked | Not canonically resolved | No source-path or Target filling | Audit vocabulary only; zero trace files |
| `platform` | Not canonically resolved | Not canonically resolved | Paths, filenames, Markdown headings, TOML keys/table names, comments, capability discovery | `DEFERRED` | Unfilled and blocked | Not canonically resolved | No platform or Target inference | Platform identity does not imply placement |
| `target` | No populated use is currently authorized | None currently authorized | Paths, filenames, Markdown headings, TOML keys/table names, comments, current bodies, defaults, samples | `DEFERRED` | Unfilled and blocked | Not canonically resolved | Explicit future Target-dependent phase required | Reserved; no current filling |

This table preserves namespace and negative-context boundaries only. It does not create, remove, rename, fill, or finally place any placeholder.

## Placeholder namespace placement matrix

| Namespace | Platform entrypoint | Target-level artifacts | Shared support | Per-agent support | Activation control | Temperature support | Trace support |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `shared` | `BLOCKED` | `BLOCKED` | `BLOCKED` | `BLOCKED` | `BLOCKED` | `BLOCKED` | `BLOCKED` |
| `agent` | `BLOCKED` | `BLOCKED` | `BLOCKED` | `BLOCKED` | `BLOCKED` | `BLOCKED` | `BLOCKED` |
| `shard` | `BLOCKED` | `BLOCKED` | `BLOCKED` | `BLOCKED` | `BLOCKED` | `BLOCKED` | `BLOCKED` |
| `trace` | Prohibited for populated use | Prohibited for populated use | Prohibited for populated use | Prohibited for populated use | Prohibited for populated use | Prohibited for populated use | Audit-only; zero files |
| `platform` | `BLOCKED` | `BLOCKED` | `BLOCKED` | `BLOCKED` | `BLOCKED` | `BLOCKED` | `BLOCKED` |
| `target` | Prohibited for populated use | Prohibited for populated use | Prohibited for populated use | Prohibited for populated use | Prohibited for populated use | Prohibited for populated use | Prohibited for populated use |

`BLOCKED` means nominal eligibility and exact placement are not adopted, not that a token must appear later.

## Markdown placeholder placement

No placeholder is assigned to a Markdown family, heading, paragraph, list item, table cell, code block, link, or comment. Placeholders are forbidden in headings, paths, and filenames. Required, optional, repeated, singular, and omitted status by Markdown slot remains blocked.

## TOML placeholder placement

No placeholder is assigned to a TOML value. Placeholders are forbidden in keys, dotted keys, table headers, paths, filenames, and comments. Exact mapping to quoted, multiline, array, boolean, integer, or other value contexts remains blocked. No raw interpolation is authorized.

## Placeholder escaping policy

Tokens must remain nominally intact until a future format-specific policy exists. Nested tokens, unknown names, renamed names, multiline identifiers, monolithic placeholders, and `{{AGENT_BODY}}` remain forbidden. No Markdown or TOML escape sequence, quoting rule, replacement order, newline conversion, or literal-token mechanism is adopted.

## Forbidden placeholder contexts

Placeholders are forbidden in paths, directory names, filenames, Markdown headings, TOML keys, dotted keys, table headers, comments, generated-notice wording, and monolithic body slots. They are also forbidden in any context requiring a current value, default, sample, Target fact, platform capability, GitHub fact, source copy, nested token, multiline identifier, unsafe raw interpolation, or physical trace output.

## Format safety and escaping policy

Exact Markdown escaping, TOML string escaping, quote handling, backslash handling, newline handling, comment handling, invalid-character handling, and placeholder-token preservation are blocked. The only adopted safety boundaries are negative: no nested placeholders, no multiline identifiers, no raw unsafe interpolation, no tokenized keys/headings/paths/filenames/comments, and no invention of an escape transform. No parser, renderer, schema, validator, or executable escaping mechanism is created.

## Template family coverage

| Family | Covered categories | Canonical source status | Markdown structure status | TOML structure status | Placeholder placement status | Future write meaning | Still blocked |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Platform entrypoint templates | GitHub platform entrypoint; Codex platform entrypoint | `POLICY_ONLY` | `BLOCKED` | `BLOCKED` | `BLOCKED` | Not body-ready | Headings, slots, complete TOML, notice, placement, escaping |
| Target-level artifact templates | Codex target-level config; Target-level agent guidance | `POLICY_ONLY` | `BLOCKED` | `BLOCKED` | `BLOCKED` | Not body-ready | AGENTS structure and complete config serialization |
| Shared support templates | Shared support | `POLICY_ONLY` | `BLOCKED` | `NOT_APPLICABLE` | `BLOCKED` | Not body-ready | Shared START/MANIFEST headings, slots, placement, notice |
| Per-agent support templates | Per-agent support | `POLICY_ONLY` | `BLOCKED` | `NOT_APPLICABLE` | `BLOCKED` | Not body-ready | Per-agent START/MANIFEST headings, slots, placement, notice |
| Activation control templates | Activation entry control; activation manifest control | `POLICY_ONLY` | `BLOCKED` | `NOT_APPLICABLE` | `BLOCKED` | No additional files implied; overlapping controls remain unready | Exact control representation |
| Temperature shard templates | Temperature support | `POLICY_ONLY` | `BLOCKED` | `BLOCKED` | `BLOCKED` | Zero direct candidates remain; no shard write authorized | Any physical structure, loader, body, placement |
| Trace support templates | Trace support | `POLICY_ONLY_AUDIT_ONLY` | `BLOCKED` | `BLOCKED` | Populated placement prohibited | Zero template files remain required | Schema, report, persistence, runtime use |

Overall family coverage is `BLOCKED`: policy-only source coverage does not establish body readiness.

## Trace support posture

Trace remains audit-only with zero template files. No `trace` placeholder is placed or filled. No trace body, schema, report, source mirror, loader, persisted artifact, fixture, snapshot, or final format is authorized.

## No-copy boundary

No final body may copy or closely mirror an existing template, kernel, Senior Agent Profile, `/docs`, source group, shard, or nearby file. Permitted source records may contribute semantic obligations only. Behavior parity must be preserved through traceable non-literal derivation, not payload copying.

## Anti-bloat boundary

No monolithic body, `{{AGENT_BODY}}`, source mirror, embedded full kernel/profile, `/docs` dump, load-all behavior, duplicated shared governance, or physical trace payload is allowed. Entrypoints and START candidates remain small; MANIFEST candidates remain selective metadata. Final numeric thresholds are not defined.

## Target dependency boundary

Real Target access, capability discovery, paths, tools, model values, docs maps, project facts, configuration values, placeholder filling, Target-dependent mapping, adapters, runtime behavior, and materialization remain blocked. Missing Target facts must stay missing.

## Create-vs-mutate decision

A future write may only be reconsidered as exact allowlist, create-only, fail-if-exists, all-or-nothing, with explicit parent-directory authorization and immediate preflight. Mutation remains prohibited: no overwrite, append, merge, repair, rename, normalization, replacement, delete-and-recreate, or mixed create/mutate mode. Any preexisting candidate must abort the future write.

## Future body content structure retry criteria

The phase `MATERIALIZATION_FULL_FLOW_DEV_SKILL_DRY_RUN_IN_MEMORY_PROJECT_LOCAL_AGENT_MICRO_PACK_TEMPLATE_BODY_CONTENT_STRUCTURE_AND_PLACEHOLDER_POLICY_DECISION_RETRY_AFTER_GAP_RESOLUTION` is not ready. Before it can run:

- a clean intermediate canonical specification must define complete Markdown structures for all six families;
- it must define complete Codex entrypoint and target-config TOML structures, including key set, tables where applicable, order, types, required/optional status, comments, and multiline policy;
- it must define generated-notice requirement, wording class, placement, comment style if any, duplication, and placeholder relationship;
- it must define exact placeholder eligibility and required/optional/deferred placement by family, Markdown slot, and TOML value;
- it must define context-specific Markdown and TOML escaping and token preservation;
- this gap-resolution decision must then be retried cleanly and receive `PASS_WITH_BODY_CANONICAL_SOURCE_AND_TOML_PLACEHOLDER_PLACEMENT_GAP_RESOLUTION_DEFINED_TEMPLATE_WRITE_STILL_BLOCKED`;
- no template read may occur, and no values, Target-dependent filling, trace files, copying, or materialization may be introduced.

## Future create-only write readiness criteria

A future create-only write remains blocked until the intermediate canonical specification passes, this gap-resolution decision passes on retry, the body-content-structure retry passes, the exact 52-entry allowlist remains valid, all 52 paths remain absent, exact parent creation is explicitly authorized, every body is derivable without guessing or template reads, no placeholder value or Target-dependent filling is required, trace remains zero files, and create-only/all-or-nothing execution remains possible.

## Abort criteria for future template write

Abort before writing if the phase would need to invent a body source, heading, prose slot, TOML key, table, ordering, value type, comment policy, escaping rule, generated notice, placeholder placement, value, default, sample, or Target fact; place a token in a path, filename, heading, TOML key/table, or comment; use `{{AGENT_BODY}}`, a monolithic token, unknown token, nested token, or multiline identifier; read, copy, compare, reuse, mutate, overwrite, append, merge, repair, rename, or replace an existing template; create rendered outputs, fixtures, snapshots, parsers, renderers, schemas, validators, checkers, meters, materializers, runners, or a tenth check; alter the Aggregator or matrix; access Target, GitHub, or productive skill; copy kernels, Senior Agent Profiles, or `/docs`; treat support as a third matrix; or materialize real Target artifacts.

## What is decided now

- The phase is blocked and the complete gap resolution is not defined.
- A policy-only canonical source boundary and forbidden-source boundary are defined.
- Historical template contamination is recorded and no new template contamination occurred; a separate unauthorized host-installed skill-instruction read is recorded as a blocking scope deviation.
- Negative placeholder contexts, no-copy, anti-bloat, trace-zero-files, create-only future posture, matrix `12/24/2`, and Aggregator nine-check boundary are preserved.
- No final structure, notice, placement, escaping, body, or output is adopted.

## What remains blocked

Complete TOML entrypoint and target-config structures; all six Markdown family structures; final prose and slots; generated notices; exact placeholder placement and status; Markdown and TOML escaping; a clean read-hygiene rerun; body-content-structure retry; implementation of all 52 candidates; parent-directory creation; materialization; mutation; rendered outputs; fixtures; snapshots; placeholder values; Target-dependent filling and mapping; physical support finalization; trace schema/files; source mirrors; `/docs` dumps; executable mechanisms; Target; GitHub; productive skill and same-named installed skill reads; Aggregator changes; and a tenth check remain blocked.

## What cannot be finalized yet

Canonical Markdown headings, order, purposes, slots, lists/tables, and prose; complete TOML keys, tables, ordering, types, required/optional status, comments, developer-instructions representation, multiline form, and escaping; generated-notice semantics and placement; placeholder-to-family, heading, slot, field, or TOML-value mapping; required/optional/deferred placeholder status; rendered-output and fixture/snapshot strategies; validation implementation; numeric anti-bloat thresholds; physical support structure; Target adapter/mapping; and final trace schema cannot be finalized.

## Forbidden inferences

Do not infer missing details from partial key mentions, filenames, conceptual paths, template names, existing templates, nearby files, filesystem state, platform knowledge, runtime conventions, examples, Target facts, GitHub facts, productive-skill behavior, source prose, legacy tokens, or desired output shape. Do not convert qualitative boundaries into exact headings, keys, order, types, wording, placement, or escaping.

## Allowed future decision paths

The only safe next path is a clean, one-document, documentation-only canonical specification phase that closes TOML, Markdown, notice, placement, and escaping without reading or writing templates. After that passes, this gap-resolution decision may be retried. Only its pass may unblock the separate body-content-structure retry, and only a later passing retry may recommend a separately authorized create-only implementation phase.

## Recommended safe next phase

`MATERIALIZATION_FULL_FLOW_DEV_SKILL_DRY_RUN_IN_MEMORY_PROJECT_LOCAL_AGENT_MICRO_PACK_TEMPLATE_TOML_MARKDOWN_NOTICE_PLACEHOLDER_PLACEMENT_AND_ESCAPING_CANONICAL_SPECIFICATION_DECISION`

That intermediate phase must remain fail-closed, documentation-only, one-document-only, path-limited, clean of template reads and any productive or same-named installed skill reads, and value-free. It must explicitly define the two TOML families, six Markdown families, generated-notice policy, exact namespace/token placement, and context-aware escaping before this gap-resolution decision is retried.

## Explicit non-authorization

This decision does not authorize implementation or creation of the 52 templates; any materialization; template creation, writing, mutation, parent-directory creation, reading, copying, comparison, reuse, overwrite, append, merge, repair, rename, replacement, or delete-and-recreate; rendered outputs; fixtures; snapshots; final placeholder values; Target-dependent placeholder filling or mapping; support-layer physical finalization; final trace schema or files; source mirrors; `/docs` dumps; persisted extra reports; checkers; validators; meters; parsers; renderers; schemas; materializers; runners; threshold enforcement; final numeric thresholds; Target access; capability discovery; GitHub; branch, commit, or PR operations; productive-skill access; Aggregator changes; a tenth check; runtime or CLI behavior; Target adapters; or real `.sentinel/agents/**`, `.github/agents/**`, `.codex/agents/**`, `AGENTS.md`, or `.codex/config.toml` artifacts.

The body-content-structure retry and `TEMPLATE_WRITE_IMPLEMENTATION_CREATE_ONLY` remain blocked.
