# Full-Flow Dev-Skill Dry-Run In-Memory Project-Local Agent Micro-Pack Audit Approach Deferred Decision

## 1. Status

READY.

This is a decision-only, documentation-only, dev-only,
single-document-only, forward-only, audit-approach-deferred-only,
non-authorizing artifact for `stnl_project_agent_specializer_dev`.

It creates no implementation, runtime, materialization path, template, script,
fixture, snapshot, persisted report, checker, validator, measurement tool,
threshold enforcement, renderer, parser, schema, Target artifact, GitHub
artifact, branch, commit, pull request, or Aggregator change.

## 2. Phase

`MATERIALIZATION_FULL_FLOW_DEV_SKILL_DRY_RUN_IN_MEMORY_PROJECT_LOCAL_AGENT_MICRO_PACK_AUDIT_APPROACH_DEFERRED_DECISION`

## 3. Verdict

`MATERIALIZATION_FULL_FLOW_DEV_SKILL_DRY_RUN_IN_MEMORY_PROJECT_LOCAL_AGENT_MICRO_PACK_AUDIT_APPROACH_DEFERRED_DECISION: PASS`

The audit approach posture for `PROJECT_LOCAL_AGENT_MICRO_PACK_MATRIX` is:

`AUDIT_APPROACH_DEFERRED_WITH_CONCEPTUAL_DIMENSIONS_ONLY`

The audit approach remains deferred. Conceptual dimensions may be recorded as
future review vocabulary only. No audit method, metric, threshold, checker,
validator, measurement tool, fixture, snapshot, persisted report, or
enforcement is defined or authorized.

## 4. Objective

Formalize the conservative decision that anti-bloat audit approach for
`PROJECT_LOCAL_AGENT_MICRO_PACK_MATRIX` must remain deferred until the
architecture has enough documentary, template, Target, sample, and measurement
basis for a separate future decision.

This document records:

- why audit approach remains deferred;
- what dimensions may be discussed conceptually later;
- what cannot be audited executably yet;
- which gates must exist before audit implementation;
- which risks remain blocked;
- which prerequisites must exist before any checker, validator, measurement
  tool, fixture, snapshot, report, or enforcement;
- which next phase is safe.

## 5. Scope

Scope is limited to one documentation artifact under:

`skills/stnl_project_agent_specializer_dev/reference/materialization_lab/`

This document may preserve inherited documentary decisions about:

- `PROJECT_LOCAL_AGENT_MICRO_PACK_MATRIX`;
- anti-bloat thresholds deferred;
- placeholder categories and gates;
- placeholder final syntax;
- template specification forward stabilization;
- trace shards;
- Aggregator 9-check boundary;
- validation/static contract boundaries.

This document is the only artifact created by this phase.

## 6. Non-goals

This phase does not:

- implement materialization;
- define an executable audit method;
- create or alter templates;
- create or alter scripts;
- create or alter fixtures;
- create or alter snapshots;
- create or alter reports;
- create or alter schemas;
- create or alter parsers;
- create or alter renderers;
- create or alter validators;
- create or alter measurement tooling;
- create threshold enforcement;
- define numeric thresholds;
- create a checker;
- create a tenth check;
- alter official child checks;
- alter the Validation Harness Aggregator;
- register mapper, consumer, test, smoke, auxiliary audit, or documentation
  review as an official check;
- access a real Target;
- access GitHub;
- access, compare, use as source, or mutate the productive
  `skills/stnl_project_agent_specializer/` skill;
- materialize `.sentinel/agents/**`;
- create `.github/agents/**`;
- create `.codex/agents/**`;
- create `AGENTS.md`;
- create `.codex/config.toml`;
- alter the skill per project;
- create a branch, commit, pull request, issue, remote artifact, or GitHub
  write.

## 7. Inherited baseline

The inherited architectural baseline is accepted and not reopened:

- `PROJECT_LOCAL_AGENT_MICRO_PACK_MATRIX` is canonical;
- the architecture materializes light agents, not heavy agents;
- platform entrypoints stay small;
- project-local support lives under `.sentinel/agents/**`;
- support packs do not count as agents;
- support packs do not replace platform entrypoints;
- shards remain temperature-classified as `always`, `hot`, `warm`, `cold`,
  and `trace`;
- `START.md` is the minimal entry;
- `MANIFEST.md` controls activation;
- `_shared` centralizes common rules;
- kernels and Senior Agent Profiles are strong sources, not literal payloads;
- `/docs` is consumed on demand and never dumped wholesale;
- behavior parity depends on traceability;
- the skill remains immutable per project.

This document follows the project-local micro-pack matrix decision, contract
plan, placeholder categories and gates plan, placeholder final syntax decision,
template specification forward stabilization plan, and anti-bloat thresholds
deferred plan. It does not revise those artifacts.

## 8. Relation to anti-bloat thresholds deferred plan

This decision inherits the result:

`MATERIALIZATION_FULL_FLOW_DEV_SKILL_DRY_RUN_IN_MEMORY_PROJECT_LOCAL_AGENT_MICRO_PACK_ANTI_BLOAT_THRESHOLDS_DEFERRED_DOCUMENTATION_AUDIT: EXCELLENT_PASS`

The preserved anti-bloat threshold posture is:

`MIXED_QUALITATIVE_WITH_DEFERRED_NUMERIC`

Interpretation:

- qualitative anti-bloat criteria are normative now;
- numeric thresholds remain deferred;
- conceptual bands may exist only as risk language;
- conceptual bands are not enforcement;
- there is no safe basis for numeric values now;
- there is no real Target;
- support-layer templates are not specified;
- representative outputs do not exist;
- measurement policy does not exist;
- audit approach is not sufficiently defined;
- no checker, validator, measurement tool, or enforcement is authorized.

This phase decides that the audit approach remains deferred because the
required basis for a future audit approach plan is still incomplete.

## 9. Canonical invariants

Preserved invariants:

- `.github/agents/<agent>.agent.md` remains the GitHub entrypoint shape;
- `.codex/agents/<agent>.toml` remains the Codex agent entrypoint shape;
- entrypoints must be small and point to local activation;
- `.sentinel/agents/_shared/**` remains the project-local shared support
  layer;
- `.sentinel/agents/<agent>/**` remains the per-agent project-local
  micro-pack;
- `_shared` centralizes common rules;
- per-agent micro-packs are specific to canonical agents;
- `always`, `hot`, `warm`, `cold`, and `trace` remain the shard families;
- `START.md` remains the minimal entry;
- `MANIFEST.md` remains the activation controller;
- kernels remain strong sources without literal copy;
- Senior Agent Profiles remain strong sources without literal copy;
- `/docs` remains on-demand input and is not dumped;
- behavior parity is traceable through summaries, anchors, and transformation
  notes;
- the support layer is not a third matrix of agents;
- the skill is immutable per project.

## 10. Matrix 12/24/2

The canonical agent matrix remains exactly 12 agents:

1. `orchestrator`
2. `planner`
3. `validation-eval-designer`
4. `execution-package-designer`
5. `designer`
6. `coder-frontend`
7. `coder-backend`
8. `coder-ios`
9. `validation-runner`
10. `reviewer`
11. `finalizer`
12. `resync`

Agent artifacts remain exactly:

- 12 x `.github/agents/<agent>.agent.md`;
- 12 x `.codex/agents/<agent>.toml`.

Target-level artifacts remain exactly:

- `.codex/config.toml`;
- `AGENTS.md`.

Counting restrictions:

- `.codex/config.toml` does not count as an agent;
- `AGENTS.md` does not count as an agent;
- support packs do not count as agents;
- `_shared` does not count as an agent;
- per-agent micro-packs do not count as agents;
- `.sentinel/agents/**` is not a third matrix of agents;
- `.sentinel/agents/**` does not replace `.github/agents/**`;
- `.sentinel/agents/**` does not replace `.codex/agents/**`.

## 11. Aggregator 9-check boundary

The official Validation Harness Aggregator remains closed over exactly these 9
child checks:

1. `scripts/materialization_lab/check-static.mjs`
2. `scripts/materialization_lab/check-source-inventory.mjs`
3. `scripts/materialization_lab/check-template-coverage.mjs`
4. `scripts/materialization_lab/check-fixture-boundary.mjs`
5. `scripts/materialization_lab/check-lazy-load-fixtures.mjs`
6. `scripts/materialization_lab/check-project-scenarios.mjs`
7. `scripts/materialization_lab/check-render-context.mjs`
8. `scripts/materialization_lab/check-dry-run-plan.mjs`
9. `scripts/materialization_lab/check-fixture-render-dry-run-integration.mjs`

The Aggregator wrapper is not a tenth child check.

This document does not:

- create a checker;
- create a tenth check;
- alter official child checks;
- alter the Aggregator;
- register mapper, consumer, tests, smokes, auxiliary audits, or documentation
  reviews as official checks.

## 12. Audit approach posture

Chosen posture:

`AUDIT_APPROACH_DEFERRED_WITH_CONCEPTUAL_DIMENSIONS_ONLY`

Meaning:

- the audit approach remains deferred;
- conceptual dimensions may be recorded;
- no method of audit is defined;
- no metric is defined;
- no threshold is defined;
- no checker is created;
- no validator is created;
- no measurement tool is created;
- no fixture, snapshot, or report is created;
- no enforcement is created.

The alternative posture
`AUDIT_APPROACH_DOCUMENTATION_PLAN_READY_BUT_NOT_AUTHORIZED` is not selected,
because the required basis for audit planning remains incomplete.

## 13. Why audit approach remains deferred

The audit approach remains deferred because:

- there is no real Target;
- support-layer template families are not final;
- support-layer templates are not specified;
- placeholder categories and syntax do not yet authorize rendering;
- representative outputs do not exist;
- real `.sentinel/agents/**` outputs do not exist;
- real `.github/agents/**` outputs do not exist;
- real `.codex/agents/**` outputs do not exist;
- real `AGENTS.md` and `.codex/config.toml` outputs do not exist;
- measurement policy does not exist;
- exception policy does not exist;
- fixture, snapshot, and report strategy does not exist;
- numeric thresholds remain deferred;
- audit implementation is not authorized;
- Aggregator expansion is blocked by default;
- direct template creation remains blocked.

These conditions block audit implementation and also block any claim that a
documentation-plan phase is already ready.

## 14. What can be audited conceptually later

Future conceptual audit vocabulary may include:

- entrypoint compactness;
- local activation clarity;
- `START.md` minimality;
- `MANIFEST.md` activation discipline;
- `_shared` common-rule centralization;
- per-agent shard specificity;
- `hot`, `warm`, and `cold` trigger discipline;
- `trace` audit-only discipline;
- trace compactness;
- source anchor quality;
- transformation note quality;
- no-copy discipline for kernels;
- no-copy discipline for Senior Agent Profiles;
- no-dump discipline for `/docs`;
- support-layer non-agent discipline;
- matrix 12/24/2 preservation;
- Aggregator 9-check preservation.

These are conceptual dimensions only. They are not executable checks, metrics,
thresholds, fixtures, snapshots, reports, validators, or enforcement rules.

## 15. What cannot be audited yet

The following cannot be audited executably yet:

- size thresholds;
- token budgets;
- line counts;
- duplication percentages;
- per-family numeric budgets;
- real Target materialization outputs;
- support-layer template outputs;
- real `.sentinel/agents/**`;
- real `.github/agents/**`;
- real `.codex/agents/**`;
- real `AGENTS.md`;
- real `.codex/config.toml`;
- runtime behavior;
- checker output;
- validator output;
- measurement output;
- enforcement output.

Reasons:

- there is no real Target;
- support-layer templates are not final;
- representative outputs do not exist;
- measurement policy does not exist;
- numeric thresholds remain deferred;
- audit implementation is not authorized.

## 16. Audit dimensions without enforcement

Audit dimensions may be used only as language for future risk discussion.

Allowed conceptual dimensions:

- compactness;
- activation clarity;
- source anchoring;
- transformation quality;
- non-copy discipline;
- lazy-load discipline;
- trace audit-only discipline;
- support-layer non-agent discipline;
- matrix preservation;
- Aggregator boundary preservation.

These dimensions do not authorize:

- scoring;
- numeric bands;
- executable checks;
- file scanning;
- fixture comparison;
- snapshot comparison;
- persisted reporting;
- validation tooling;
- threshold enforcement.

## 17. Audit implementation gates

Before any future audit implementation, all of the following gates must be
handled in a separate, explicit, auditable phase:

- specific decision-only phase approved;
- specific documentation-only plan phase approved;
- template families defined;
- support-layer templates specified;
- placeholders stabilized for support-layer;
- anti-bloat qualitative criteria documented;
- numeric thresholds posture resolved or explicitly kept deferred;
- representative samples available;
- measurement policy defined;
- exception policy defined;
- fixture and snapshot strategy defined;
- report strategy defined;
- boundary with Aggregator defined;
- explicit decision about whether to create a checker;
- explicit decision about whether to create a validator;
- explicit decision about whether to create a measurement tool;
- explicit decision about Target real access;
- explicit decision about GitHub access;
- confirmation that the productive skill will not be used without its own
  phase.

These gates do not authorize implementation in this phase.

## 18. Checker/validator/measurement boundaries

This phase explicitly does not authorize:

- checker creation;
- tenth check creation;
- Aggregator expansion;
- official child-check registration;
- validator creation;
- measurement tooling;
- threshold enforcement;
- lint helper;
- audit helper;
- persisted report generation;
- fixture creation;
- snapshot creation.

If a future phase wants any of these, it must be separate, explicit,
documentation-first, path-scoped, and auditable.

## 19. Fixture/snapshot/report boundaries

This phase does not create or authorize:

- fixture files;
- fixture schemas;
- fixture outputs;
- fixture comparison rules;
- snapshots;
- snapshot directories;
- snapshot update policy;
- persisted Markdown reports;
- persisted JSON reports;
- dry-run reports;
- target reports;
- caches;
- temporary output artifacts.

Future fixture, snapshot, or report work must not be inferred from conceptual
audit dimensions.

## 20. Relation to numeric thresholds

Numeric thresholds remain deferred.

This document defines no:

- size threshold;
- line-count threshold;
- token-budget threshold;
- shard-count threshold;
- file-count threshold;
- duplication percentage;
- per-family numeric budget;
- trace-size limit;
- pack-size limit;
- enforcement band.

Conceptual bands may be discussed only as qualitative risk language in a
future decision. They are not measurement policy, validation policy, checker
policy, or enforcement policy.

## 21. Relation to template creation gates

Template creation remains blocked.

A future audit approach must not assume:

- final support-layer template families;
- final support-layer template paths;
- final placeholder inventory;
- rendered support-layer outputs;
- pack sizes;
- shard sizes;
- trace sizes.

Future template creation still requires a separate explicit phase after the
required gates are satisfied. This audit approach decision does not create or
mutate templates.

## 22. Relation to placeholder syntax/categories/gates

This document preserves the placeholder boundary:

- `{{...}}` is the conceptual delimiter;
- current flat placeholders remain restricted to legacy platform-entrypoint
  and target-level templates;
- future support-layer placeholders require semantically namespaced names;
- future names use lowercase dot-separated syntax;
- underscore is allowed inside segments;
- nested placeholders are forbidden;
- unknown placeholders fail closed;
- unused placeholders fail closed;
- multiline identifiers are forbidden;
- multiline values are forbidden by default;
- Target-dependent placeholders may be classified, but never filled without
  Target authorization;
- `{{AGENT_BODY}}` is prohibited as a monolithic support-layer body.

This phase creates no parser, renderer, schema, executable placeholder rule, or
real placeholder.

## 23. Relation to trace shards

`trace` remains:

- audit-only;
- non-mirror;
- based on references, summaries, source anchors, and transformation notes;
- dependent on a future audit approach;
- without numeric size definition;
- without enforcement;
- without literal copy of kernels, Senior Agent Profiles, or `/docs`.

`trace` must not become:

- a payload complete enough to replace sources;
- normal operational reading;
- a persisted audit report;
- a copy of source text;
- a bypass around anti-bloat criteria;
- a substitute for `MANIFEST.md` activation.

## 24. Target/template/audit-dependent restrictions

Restrictions:

- Target-dependent facts remain unavailable;
- Target-dependent placeholders are not filled;
- Target-dependent docs maps are not inferred;
- Target-dependent project surfaces are not inferred;
- support-layer templates remain unspecified;
- template-dependent support-pack size cannot be inferred;
- representative outputs do not exist;
- audit-dependent trace size cannot be inferred;
- audit-dependent compactness rules cannot become enforcement;
- no Target read or write is authorized;
- no template creation or mutation is authorized;
- no audit implementation is authorized.

If a future task requires Target facts, template facts, or audit facts, it must
use a separate explicitly authorized phase.

## 25. Forbidden inferences

This decision does not permit inferring:

- executable audit;
- checker;
- tenth check;
- validator;
- measurement tool;
- threshold enforcement;
- fixture;
- snapshot;
- persisted report;
- template creation;
- template mutation;
- Target access;
- GitHub write;
- productive skill access;
- parser;
- renderer;
- schema;
- numeric thresholds;
- materialized output;
- support layer as a third matrix;
- alteration of matrix 12/24/2;
- alteration of the Aggregator 9-check boundary.

Absent items remain absent. Deferred items remain deferred.

## 26. Allowed future decision paths

Allowed future decision paths, all separate and not authorized by this phase:

- `AUDIT_APPROACH_DEFERRED_DOCUMENTATION_AUDIT`;
- `AUDIT_APPROACH_DOCUMENTATION_PLAN_DECISION`;
- `TEMPLATE_CREATION_GATES_REVIEW_DECISION`;
- `SUPPORT_LAYER_TEMPLATE_FAMILIES_DECISION`;
- `TRACE_SHARD_AUDITABILITY_DECISION`.

The primary recommended path is a read-only audit of this document, not audit
planning or implementation.

## 27. Recommended safe next phase

Recommended immediate next phase:

`MATERIALIZATION_FULL_FLOW_DEV_SKILL_DRY_RUN_IN_MEMORY_PROJECT_LOCAL_AGENT_MICRO_PACK_AUDIT_APPROACH_DEFERRED_DECISION_AUDIT`

That next phase should be:

- read-only;
- path-limited;
- documentation-only audit;
- single-document-focused;
- without writes;
- without GitHub;
- without real Target;
- without productive skill access;
- without scripts;
- without templates;
- without fixtures;
- without snapshots;
- without persisted reports;
- without Aggregator change;
- without checker change;
- without validator;
- without measurement tooling;
- without threshold enforcement.

## 28. Explicit non-authorization

This document explicitly does not authorize:

- implementation;
- runtime;
- materialization;
- Target access;
- GitHub write;
- productive skill access;
- productive skill mutation;
- template creation;
- template mutation;
- fixture creation;
- snapshot creation;
- persisted report creation;
- renderer creation;
- parser creation;
- schema creation;
- validator creation;
- measurement tooling;
- threshold enforcement;
- Aggregator expansion;
- checker creation;
- official check registration;
- numeric threshold definition;
- audit implementation;
- audit runtime.

This document is decision-only, documentation-only, dev-only,
single-document-only, forward-only, audit-approach-deferred-only, and
non-authorizing.
