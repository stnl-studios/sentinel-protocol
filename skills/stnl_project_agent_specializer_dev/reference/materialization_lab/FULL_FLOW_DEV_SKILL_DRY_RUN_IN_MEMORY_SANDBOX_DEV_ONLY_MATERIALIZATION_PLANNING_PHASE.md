# FULL FLOW DEV SKILL DRY RUN IN MEMORY — SANDBOX/DEV-ONLY MATERIALIZATION PLANNING PHASE

Phase:

`MATERIALIZATION_FULL_FLOW_DEV_SKILL_DRY_RUN_IN_MEMORY_SANDBOX_DEV_ONLY_MATERIALIZATION_PLANNING_PHASE`

This document is documentation-only, planning-only, dev-only,
non-authorizing, sem execução, sem materialização, sem sandbox materialization
executada, sem Target real, sem GitHub, sem skill produtiva, sem
Aggregator/checker/décimo check, sem renderer/writer/loader, sem Target
Adapter/Write Approval, and sem runtime/CLI/schema oficial/stdout contract.

## 1. Status

`MATERIALIZATION_FULL_FLOW_DEV_SKILL_DRY_RUN_IN_MEMORY_SANDBOX_DEV_ONLY_MATERIALIZATION_PLANNING_PHASE: READY`

This document persists the sandbox/dev-only plan previously approved in
conversational context and corrects only the versionable traceability gap.

It does not create authorization to execute, materialize, write, inspect a real
Target, access GitHub, access the productive skill, alter the Aggregator, create
a checker, or create a tenth check.

## 2. Purpose

The purpose of this document is to plan, at documentary level only, a possible
future sandbox/dev-only materialization phase.

This document does not authorize execution, operational writes,
materialization, sandbox execution, Target real access, GitHub access, or
productive-skill access.

## 3. Scope

The scope is only the dev skill:

`skills/stnl_project_agent_specializer_dev/`

The productive skill is outside absolute scope:

`skills/stnl_project_agent_specializer/`

The productive skill must not be used as source, reference, baseline, fallback,
comparison, fixture, template source, or conceptual reference.

## 4. Canonical Agent Model

The canonical agent model contains exactly 12 agents:

1. orchestrator
2. planner
3. validation-eval-designer
4. execution-package-designer
5. designer
6. coder-frontend
7. coder-backend
8. coder-ios
9. validation-runner
10. reviewer
11. finalizer
12. resync

Any increase above 12 agents is a blocker.

## 5. Planned Artifact Matrix

The planned conceptual matrix is:

- 12 x copilot: `.github/agents/<agent>.agent.md`
- 12 x codex: `.codex/agents/<agent>.toml`
- Codex target-level: `.codex/config.toml`
- Codex target-level: `AGENTS.md`

`.codex/config.toml` and `AGENTS.md` are target-level artifacts, not agents.

These artifacts do not increase the canonical agent count.

## 6. Explicit Template Sources

The only permitted explicit templates are:

- `reference/templates/copilot/agent.md`
- `reference/templates/codex/agent.toml`
- `reference/templates/codex/config.toml`
- `reference/templates/codex/AGENTS.md`

Templates must not be inferred by:

- path symmetry;
- output path;
- runtime naming;
- productive skill;
- real Target;
- naming convention;
- absence of blocker.

## 7. Target Strategy

Copilot documentary strategy:

- planned agent-level artifacts in `.github/agents/<agent>.agent.md`;
- exactly 12 agents;
- no write in this phase;
- no real Target;
- no GitHub.

Codex documentary strategy:

- planned agent-level artifacts in `.codex/agents/<agent>.toml`;
- `.codex/config.toml` as target-level;
- `AGENTS.md` as target-level;
- do not transform target-level artifacts into agents;
- no write in this phase;
- no real Target;
- no GitHub.

## 8. Anti-Bloat And Deduplication

Future documentary planning must avoid:

- content duplication between agents;
- excessive repetition of global policies;
- expansion above 12 agents;
- improper mixing between agent-level and target-level artifacts;
- Copilot/Codex divergence without justification;
- unnecessary prompt growth.

Shared policy belongs at the narrowest safe shared level. Agent artifacts should
carry role-specific content and should not become copies of target-level policy.

## 9. Ownership And Managed Artifacts

Ownership and managed artifacts are only documentary concepts in this phase.

This document does not create a real managed block, real marker, official
schema, writer, or operational overwrite policy.

## 10. Conflict, Overwrite And Fail-Closed

Any future conflict must be handled as fail-closed.

There is no authorization for:

- overwrite;
- automatic merge;
- patch;
- diff application;
- write;
- Target read/write/stat/list/content.

## 11. Future Idempotency Criteria

Future documentary idempotency criteria:

- the same input must generate the same conceptual plan;
- target-level artifacts do not vary by agent;
- agent-level artifacts do not invade target-level artifacts;
- the order of the 12 agents must remain stable;
- absence of explicit templates blocks;
- absence of required evidence blocks.

## 12. Required No-Write Evidence

Future no-write evidence must include exactly these required fields and values:

```text
target_read_attempted: false
target_write_attempted: false
filesystem_stat_attempted: false
directory_listing_attempted: false
file_content_read_attempted: false
files_written: []
persistent_report_written: false
github_write_attempted: false
productive_skill_mutation_attempted: false
approval_token_issued: false
write_executed: false
patch_applied: false
commit_created: false
branch_created: false
pull_request_created: false
```

Any absent, true, renamed, softened, or contradictory field blocks a future
pass.

## 13. Required Non-Authorization Evidence

Future non-authorization evidence must explicitly deny:

- real materialization;
- sandbox/dev-only materialization executada;
- real Target read;
- real Target write;
- filesystem access against real Target;
- real writer;
- real renderer;
- real loader;
- real scenario selector;
- real Target Adapter;
- real Write Approval;
- approval token;
- approval registry;
- approval signature;
- signer;
- persistent report;
- generated output;
- materialized output;
- patch or diff application;
- GitHub write;
- productive skill access;
- productive skill read;
- productive skill comparison;
- productive skill as source;
- productive skill mutation;
- commit;
- branch;
- pull request;
- Aggregator change;
- checker creation;
- tenth check.

Any absent, true, renamed, softened, or contradictory non-authorization signal
blocks a future pass.

## 14. Relationship To Previous Pass Verdicts

- `REVIEW_PASS` does not authorize materialization.
- `DRY_RUN_PASS` does not authorize writing.
- Documentary readiness does not authorize implementation.
- Planning sandbox/dev-only does not authorize execution.
- This document does not authorize implementation.
- This document does not authorize materialization.
- This document does not authorize sandbox execution.

## 15. Aggregator Boundary

The official Aggregator remains closed at exactly 9 child checks:

1. `scripts/materialization_lab/check-static.mjs`
2. `scripts/materialization_lab/check-source-inventory.mjs`
3. `scripts/materialization_lab/check-template-coverage.mjs`
4. `scripts/materialization_lab/check-fixture-boundary.mjs`
5. `scripts/materialization_lab/check-lazy-load-fixtures.mjs`
6. `scripts/materialization_lab/check-project-scenarios.mjs`
7. `scripts/materialization_lab/check-render-context.mjs`
8. `scripts/materialization_lab/check-dry-run-plan.mjs`
9. `scripts/materialization_lab/check-fixture-render-dry-run-integration.mjs`

The wrapper Aggregator is not a tenth check.

This document registers nothing in the Aggregator.

## 16. Future Decision Gate

Any future phase beyond this document requires a separate decision.

The next possible phase after this correction pack must be only an audit of the
correction pack or a decision about auditing the correction pack.

This document does not recommend implementation, materialization, sandbox
execution, renderer/writer/loader work, Target Adapter work, or Write Approval
work.

## 17. Absolute Blockers

Absolute blockers:

- any use of the productive skill as source or conceptual reference;
- any real Target;
- any GitHub write;
- any branch, commit, or PR;
- any template inference;
- any increase above 12 agents;
- any treatment of `.codex/config.toml` or `AGENTS.md` as an agent;
- any authorization of execution;
- any authorization of materialization;
- any authorization of sandbox execution;
- any renderer/writer/loader implementation;
- any Target Adapter/Write Approval implementation;
- any runtime/CLI/schema/stdout contract;
- any Aggregator change;
- any new checker;
- any tenth check;
- any softening of no-write evidence;
- any softening of non-authorization evidence.
