# Project-local agent micro-pack template path filename and body basis decision

## 1. Status

`BLOCKED_PENDING_PATH_FILENAME_BODY_BASIS`

Posture: `TEMPLATE_PATH_FILENAME_AND_BODY_BASIS_BLOCKED_PENDING_CANONICAL_SPECIFICATION`.

This is a documentation-only, dev-only and fail-closed decision. It is not a
template, executable inventory, schema, checker, validator or write approval.

## 2. Phase

`MATERIALIZATION_FULL_FLOW_DEV_SKILL_DRY_RUN_IN_MEMORY_PROJECT_LOCAL_AGENT_MICRO_PACK_TEMPLATE_PATH_FILENAME_AND_BODY_BASIS_DECISION`

## 3. Verdict

`BLOCKED_WITHOUT_PATH_FILENAME_BODY_BASIS`

The inherited documentation does not safely determine literal candidate
filenames, literal candidate paths inside the dev skill or a canonical body
basis per candidate file. Defining any of them now would invent a convention
that the inherited decisions explicitly left open.

## 4. Objective

Decide whether the inherited documentary record is sufficient to establish a
canonical basis for candidate template filenames, paths and bodies without
writing templates, reading existing template bodies, using a real Target or
converting conceptual Target shapes into real artifacts.

The result is negative and fail-closed. This document records the missing
specification and the boundaries that a future documentary phase must preserve.

## 5. Scope

The decision is limited to
`skills/stnl_project_agent_specializer_dev/` and to creation of this document.
No Target, GitHub operation or productive skill is in scope.

During the broader orchestrated execution, a subagent used an overly broad
`rg` query and accidentally read limited lines from two existing templates:
`reference/templates/codex/AGENTS.md` and
`reference/templates/copilot/agent.md`. That read had no explicit documentary
basis, violates the no-read boundary and is a blocking finding. The observed
content was discarded and is not quoted, compared, reused or treated as
evidence for any filename, path, body, family, category or style decision.
This decision writer did not read either template.

## 6. Non-goals

This phase does not create or mutate templates; define candidate or final
filenames, paths, bodies or executable inventory; populate placeholder values;
create rendered outputs, fixtures or snapshots; create a parser, renderer,
schema, validator, checker, meter or materializer; define runtime or CLI
behavior; access a real Target, GitHub or the productive skill; alter the
Aggregator; create a tenth check; or materialize any agent or support pack.

## 7. Inherited baseline

The inherited baseline establishes:

- the exact allowlist is `BLOCKED_WITHOUT_EXACT_ALLOWLIST` under posture
  `TEMPLATE_WRITE_EXACT_ALLOWLIST_BLOCKED_PENDING_CANONICAL_PATH_BASIS`;
- the missing central bases are exact filenames, exact paths and canonical
  body basis per file;
- template write authorization is
  `PASS_WITH_SEPARATE_TEMPLATE_WRITE_PHASE_AUTHORIZED`, but is separate,
  conditional and non-transitive;
- the implementation plan is `PASS_WITH_TEMPLATE_WRITE_BLOCKED`;
- readiness is `PASS_WITH_TEMPLATE_CREATION_BLOCKED`;
- the final placeholder inventory contains 57 unfilled canonical names in six
  namespaces;
- Target-dependent filling and mapping remain blocked;
- support-layer finalization is `PASS_WITH_IMPLEMENTATION_BLOCKED`;
- the seven template families are conceptual-only and bounded;
- template creation gates are reviewed but creation remains blocked;
- trace closeout is `CLOSED_WITH_EXCELLENT_PASS`, while trace remains
  audit-only and conceptual-only;
- final audit approach and numeric anti-bloat thresholds remain deferred;
- the canonical matrix remains `12/24/2`; and
- the official Aggregator remains closed at nine child checks.

## 8. Relation to exact allowlist decision

The exact allowlist decision remains authoritative. It contains no file or path
entries and identifies the lack of literal filename, literal path and
file-specific canonical body basis as blockers. This phase does not resolve
those blockers because the inherited record supplies conceptual shapes and
taxonomies, not a literal layout and naming specification.

The exact allowlist and `TEMPLATE_WRITE_IMPLEMENTATION` therefore remain
blocked. No exact allowlist retry is safe yet.

## 9. Relation to template write authorization

`PASS_WITH_SEPARATE_TEMPLATE_WRITE_PHASE_AUTHORIZED` authorizes only the
possibility of a separately prompted, exact-allowlisted future phase. It is not
current write approval and does not select files, paths, bodies or values.
Failing this basis decision does not revoke that documentary authorization, but
its preconditions remain unsatisfied.

## 10. Relation to template creation implementation plan

The implementation plan remains `PASS_WITH_TEMPLATE_WRITE_BLOCKED`. Its seven
families, ten artifact categories, six namespaces and review categories are
planning taxonomy only. They do not determine a filesystem layout, filename
convention, source-selection rule or body transformation per file.

## 11. Relation to template readiness

`PASS_WITH_TEMPLATE_CREATION_BLOCKED` means the architecture is ready for
further documentary decisions, not that template files are specified. The
readiness record explicitly leaves files, paths, bodies and executable
inventory unresolved. That boundary is preserved.

## 12. Relation to placeholder final inventory

The 57 canonical placeholders remain unfilled and distributed across
`shared`, `agent`, `shard`, `trace`, `platform` and `target`. The inventory is
documentary vocabulary, not a filename generator, path grammar, body schema or
mapping algorithm. It cannot resolve the missing bases.

## 13. Relation to support layer finalization

The support layer remains a conceptual auxiliary architecture under
`PASS_WITH_IMPLEMENTATION_BLOCKED`. `_shared`, per-agent support, activation,
temperature shards, trace, `START.md` and `MANIFEST.md` are conceptual
responsibilities or shapes, not approved template files or dev-skill paths.

## 14. Relation to support-layer template families

The seven families remain conceptual-only and creation-blocked. Their names
classify possible responsibilities but do not establish literal filenames,
directories, extensions, cardinality, body sources or source-to-body
transformations.

## 15. Relation to template creation gates

The gates remain reviewed but creation-blocked. The inherited gate vocabulary
supports fail-closed review and abort criteria only. It does not supply the
missing canonical layout, naming or source-selection specification.

## 16. Relation to trace audit closeout

The trace audit closeout remains `CLOSED_WITH_EXCELLENT_PASS`. Trace remains
audit-only, conceptual-only, non-mirror, non-report, non-runtime and
non-materialization-authorizing. No final trace structure, path, inventory or
schema is established here.

## 17. Relation to audit approach deferred

The final audit approach remains deferred. Conceptual review dimensions do not
become an audit method, test, fixture, snapshot, persisted report, validator,
checker or enforcement mechanism in this phase.

## 18. Relation to anti-bloat thresholds deferred

Qualitative anti-bloat rules remain normative: small entrypoints, focused
shards, centralized common rules, on-demand documentation consumption and no
source dumps. Numeric thresholds, budgets, measurements and enforcement remain
deferred; no numeric value is introduced.

## 19. Canonical invariants

- Agents remain lightweight; platform entrypoints remain small.
- Common rules are centralized conceptually rather than repeated per agent.
- Kernels and Senior Agent Profiles are strong sources, never literal payloads.
- `/docs` is consumed narrowly and on demand, never copied wholesale.
- Prepared project-local shards preserve behavior parity through traceability.
- Source anchors, summaries, transformation notes and provenance are required
  concepts, but their file-specific selection remains unspecified.
- The skill remains immutable per project.
- `{{AGENT_BODY}}`, source mirrors and monolithic payloads are forbidden.
- Templates, families, support packs, `_shared`, per-agent micro-packs,
  placeholders and trace do not count as agents.

## 20. Matrix 12/24/2 preservation

The canonical matrix remains exactly 12 agents:

1. `orchestrator`
2. `planner`
3. `validation-eval-designer`
4. `execution-package-designer`
5. `designer`
6. `coder-frontend`
7. `coder-backend`
8. `coder-ios`
9. `validation-runner`
10. `reviewer`
11. `finalizer`
12. `resync`

Agent artifacts remain 12 x `.github/agents/<agent>.agent.md` and 12 x
`.codex/agents/<agent>.toml`, for 24 agent artifacts. Target-level artifacts
remain `.codex/config.toml` and `AGENTS.md`, for 2 target-level artifacts. The
two target-level artifacts do not count as agents. The support layer is not a
third matrix and does not replace either agent-artifact surface.

## 21. Aggregator 9-check boundary

The official Aggregator remains closed in this exact order:

1. `scripts/materialization_lab/check-static.mjs`
2. `scripts/materialization_lab/check-source-inventory.mjs`
3. `scripts/materialization_lab/check-template-coverage.mjs`
4. `scripts/materialization_lab/check-fixture-boundary.mjs`
5. `scripts/materialization_lab/check-lazy-load-fixtures.mjs`
6. `scripts/materialization_lab/check-project-scenarios.mjs`
7. `scripts/materialization_lab/check-render-context.mjs`
8. `scripts/materialization_lab/check-dry-run-plan.mjs`
9. `scripts/materialization_lab/check-fixture-render-dry-run-integration.mjs`

The Aggregator wrapper is not a tenth check. This document is not a check and
does not register, create, edit or authorize a checker, child check or
Aggregator change.

## 22. Path filename body basis posture

`TEMPLATE_PATH_FILENAME_AND_BODY_BASIS_BLOCKED_PENDING_CANONICAL_SPECIFICATION`

No candidate filename, path or body basis is established. The safe posture is
to require a separate documentary specification that has explicit authority to
define a literal dev-template layout, literal naming convention and
file-specific source-selection/transformation rules without reading or deriving
from existing template bodies.

## 23. Decision model

A pass would require every candidate entry to have a literal filename, literal
path inside the dev skill, explicit future `create-only` mode, mapped family and
category, canonical non-final body basis, allowed placeholder namespaces,
forbidden content and collision behavior. Paths could contain no glob,
placeholder, `<agent>` or `**`; per-agent candidates would require all 12
literal expansions.

The inherited record does not supply those elements. The model therefore fails
closed and produces no candidate entries.

## 24. Decision criteria

`BASIS_READY` means only that a documentary boundary is established.
`DEFERRED` means a separate decision is required. `BLOCKED` means the missing
basis or a scope violation prevents this decision from passing. None of these
states authorizes template write.

## 25. Decision result

| Criterion | Status | Evidence basis | Decision meaning | Still blocked |
| --- | --- | --- | --- | --- |
| Exact allowlist blocker identified | `BASIS_READY` | Prior exact allowlist decision names three missing bases. | The gap is explicit. | Exact allowlist and write. |
| Template write authorization exists | `BASIS_READY` | Separate conditional authorization exists. | A future phase may be considered only after all gates pass. | Current and future write without allowlist. |
| Implementation plan exists | `BASIS_READY` | Planning-only document is active. | Taxonomies and boundaries may be inherited. | Files, paths, bodies and execution. |
| Candidate families exist | `BASIS_READY` | Seven conceptual families are documented. | Family vocabulary is available. | File-level assignment. |
| Candidate artifact categories exist | `BASIS_READY` | Ten conceptual categories are documented. | Category vocabulary is available. | File-level assignment and real artifacts. |
| Filename basis can be established | `BLOCKED` | No canonical literal naming convention exists. | Any filename would be invented. | All candidate filenames. |
| Path basis can be established | `BLOCKED` | No canonical literal dev-template layout exists. | Any candidate path would be invented. | All candidate paths. |
| Body basis can be established | `BLOCKED` | No file-specific source-selection and transformation specification exists. | Any body basis would be inferred. | All file-specific body bases and final bodies. |
| Paths remain inside skill dev | `BLOCKED` | No candidate paths can be defined to evaluate. | Confinement is a rule, not a demonstrated candidate property. | Candidate path allowlist. |
| Paths are not Target artifacts | `BLOCKED` | Conceptual shapes cannot be converted to dev paths without a layout specification. | No safe candidate distinction exists yet. | Candidate path allowlist and Target artifacts. |
| No globs or unresolved tokens in paths | `BLOCKED` | No literal paths exist; inherited shapes contain conceptual tokens. | Literal expansion cannot be demonstrated. | Every candidate path. |
| Per-agent expansion, if used, is literal | `BLOCKED` | Cardinality is known but file layout and naming are not. | Twelve literal entries cannot be safely produced. | Per-agent candidate entries. |
| Create-only can be preserved | `BASIS_READY` | Inherited future default is `create-only`. | A future specification must preserve it. | Operational use without exact entries. |
| Mutation remains blocked | `BASIS_READY` | `BLOCKED_PENDING_EXPLICIT_MUTATION_BASIS`. | No mutate mode is available. | Reading, comparing, replacing or editing templates. |
| Existing templates not read | `BLOCKED` | An inherited subagent accidentally read limited lines from two templates without explicit basis. | The no-read gate was violated; content is discarded. | Reliance on those reads and phase pass. |
| Existing templates not used as body basis | `BASIS_READY` | Accidentally observed content was discarded and is not evidence. | No existing body supplies basis. | Copy, reuse, comparison and derivation. |
| Placeholder namespaces mapped | `DEFERRED` | Six namespaces exist, but no candidate files exist for mapping. | Namespace vocabulary is ready; per-file mapping is not. | Per-file and per-family operational mapping. |
| Placeholder values remain blocked | `BASIS_READY` | Final inventory contains no values. | No value may be populated or inferred. | All values. |
| No placeholders in paths | `BASIS_READY` | Canonical boundary forbids placeholder-driven paths. | A future specification must use literals only. | Any placeholder path and all current candidates. |
| Target-dependent filling remains blocked | `BASIS_READY` | Target boundary is fail-closed. | No Target fact can resolve content. | Filling, mapping, adapter and Target access. |
| No-copy policy preserved | `BASIS_READY` | Whole-source copies and monolithic bodies are forbidden. | Future basis must be selective and traceable. | Source mirror, dumps and `{{AGENT_BODY}}`. |
| Traceability basis described | `DEFERRED` | Source anchors, summaries, transformation notes and provenance are known only as concepts. | File-specific source selection remains necessary. | Final trace structure and file-specific basis. |
| Anti-bloat qualitative posture preserved | `BASIS_READY` | Qualitative rules are inherited. | Future proposals must remain small and focused. | Numeric thresholds and enforcement. |
| Abort criteria can be updated | `BASIS_READY` | Existing gates support documentary abort rules. | A future phase can fail before write. | Executable gate or write. |
| Rendered outputs remain blocked | `BASIS_READY` | No-output boundary is explicit. | Outputs are outside this decision. | Renders, fixtures, snapshots and reports. |
| Renderer/parser/schema remain blocked | `BASIS_READY` | Mechanism boundary is explicit. | Decision remains documentary. | Parser, renderer and schema. |
| Implementation/materialization remain blocked | `BASIS_READY` | All inherited decisions preserve these blocks. | No operational artifact may be produced. | Implementation and materialization. |
| Matrix 12/24/2 preserved | `BASIS_READY` | Canonical counts and exclusions are fixed. | Templates and support do not alter the matrix. | New agents, re-counting and third matrix. |
| Aggregator 9-check boundary preserved | `BASIS_READY` | Official ordered allowlist has nine child checks. | This document is not a check. | Aggregator change, checker or tenth check. |
| Target real remains blocked | `BASIS_READY` | Scope is dev-only and Target-independent. | No Target-derived fact is allowed. | Target read, write, discovery and mapping. |
| GitHub remains blocked | `BASIS_READY` | Remote operations are excluded. | Platform shapes remain conceptual. | GitHub access, branch, commit and PR. |
| Productive skill remains blocked | `BASIS_READY` | Hard scope excludes the productive skill. | No basis may be derived from it. | Read, comparison and mutation. |
| Current phase writes limited to one document | `BASIS_READY` | Write permission names only this document. | No other write is permitted. | Any second file or existing-file edit. |

The three central basis criteria are blocked, and the no-read criterion was
violated by an inherited subagent. Boundary readiness cannot compensate for
either condition.

## 26. Filename basis

No filename basis is defined. A future filename must be literal, independent of
Target and explicitly related to one candidate family and category. It may not
contain `<agent>`, `**`, a glob or placeholder; may not derive from an existing
template; and must be proposed only for future `create-only` review.

Those are constraints, not a naming convention. The inherited documents do not
choose directory-local naming, prefixes, suffixes, extensions, agent-name
encoding or control-file naming. No candidate filename is introduced here.

## 27. Path basis

No path basis is defined. A future candidate path must be literal, remain
inside `skills/stnl_project_agent_specializer_dev/`, contain no glob,
placeholder, `<agent>` or `**`, and have an explicit documentary reason. It
must not be a real Target destination or point to a preexisting template in a
future `create-only` phase.

The Target shapes `.github/agents/**`, `.codex/agents/**`,
`.sentinel/agents/**`, `AGENTS.md` and `.codex/config.toml` are not candidate
dev-template paths. No transformation from those shapes to a dev layout is
authorized here, so no candidate path is introduced.

## 28. Body basis

No file-specific body basis is defined. The inherited qualitative boundary
requires kernels and Senior Agent Profiles to remain strong sources rather than
payloads; `/docs` to be consumed on demand; and future transformations to use
source anchors, summaries, transformation notes and provenance. It forbids
`{{AGENT_BODY}}`, monolithic bodies, source mirrors and existing template bodies.

These constraints do not select sources per candidate file, define source
precedence, specify transformation responsibility or delimit the minimum
content of a file. A future source-selection specification is required. No body
final, placeholder value, parser, renderer or schema is created here.

## 29. Candidate file basis table

| Candidate file | Candidate path | Write mode for future phase | Candidate family | Candidate artifact category | Filename basis | Path basis | Body basis | Allowed placeholder namespaces | Forbidden content | Current phase status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| No candidate entry defined | No candidate entry defined | Not available | Not assigned | Not assigned | Missing canonical naming convention | Missing canonical dev layout | Missing file-specific source-selection specification | Not mapped | Existing-template content, source copies, monolithic body and populated values | `BLOCKED` |

The row is a blocking declaration, not a candidate file, template inventory or
allowlist entry. It defines no filename, path or body.

## 30. Candidate family and category mapping

| Candidate family | Candidate artifact category | Status | Missing basis |
| --- | --- | --- | --- |
| platform entrypoint templates | GitHub platform entrypoint | `BLOCKED` | Literal filename, dev path and file-specific body basis. |
| platform entrypoint templates | Codex platform entrypoint | `BLOCKED` | Literal filename, dev path and file-specific body basis. |
| target-level artifact templates | Codex target-level config | `BLOCKED` | Literal filename, dev path and Target-independent body basis. |
| target-level artifact templates | Target-level agent guidance | `BLOCKED` | Literal filename, dev path and Target-independent body basis. |
| shared support templates | Shared support | `BLOCKED` | Physical responsibility boundary, literal filename/path and source selection. |
| per-agent support templates | Per-agent support | `BLOCKED` | Literal twelve-agent expansion, filenames/paths and per-file source selection. |
| activation control templates | Activation entry control | `BLOCKED` | Literal control filename/path and non-runtime body basis. |
| activation control templates | Activation manifest control | `BLOCKED` | Literal control filename/path and non-schema body basis. |
| temperature shard templates | Temperature support | `BLOCKED` | Literal shard inventory, filename/path and focused source selection. |
| trace support templates | Trace support | `BLOCKED` | Literal filename/path and audit-only source-selection basis without schema. |

All seven families and all ten categories remain documentary taxonomy. This
table neither defines files nor authorizes their creation.

## 31. Create-vs-mutate decision

No creation or mutation is authorized. The preferred mode for a possible later
phase remains `create-only`, but cannot operate without an exact allowlist.
Mutation remains `BLOCKED_PENDING_EXPLICIT_MUTATION_BASIS` and would require a
separate phase with explicit read authority, collision analysis and safety
justification.

## 32. Preexistence and collision boundary

Because no candidate paths exist, preexistence and collision checks cannot be
performed. A future `create-only` phase must abort before any write if an
allowlisted path already exists, ownership is ambiguous, atomicity cannot be
shown or an adjacent file would need mutation.

The two accidentally read template paths are not candidate paths and their
existence supplies no naming, layout, body or mutation basis.

## 33. Placeholder usage boundary

| Namespace | Candidate basis use | Blocked current use | Value status | Target dependency handling | Notes |
| --- | --- | --- | --- | --- | --- |
| `shared` | Documentary vocabulary for common-rule responsibility only. | File mapping, body insertion, path use and rendering. | Unfilled; no value. | No Target inference. | 9 inherited names; no new name. |
| `agent` | Documentary vocabulary for canonical-agent responsibility only. | Per-file mapping, twelve-file generation and body insertion. | Unfilled; no value. | Agent facts cannot be inferred from Target. | 11 inherited names; no new name. |
| `shard` | Documentary vocabulary for focused shard responsibility only. | Inventory, loader, filename/path or body generation. | Unfilled; no value. | Target-dependent shard facts remain blocked. | 13 inherited names; no new name. |
| `trace` | Documentary vocabulary for provenance concepts only. | Trace file, schema, persisted report or body generation. | Unfilled; no value. | Real source paths require separately authorized evidence. | 10 inherited names; no new name. |
| `platform` | Documentary vocabulary for platform concepts only. | Platform detection, path generation, escaping implementation or body insertion. | Unfilled; no value. | Capability facts remain blocked. | 6 inherited names; no new name. |
| `target` | Reserved documentary vocabulary for a future authorized Target phase. | Every current use, mapping, filling, path use and body insertion. | Unfilled; no value. | Fully blocked pending narrow Target authorization. | 8 inherited names; no new name. |

## 34. Forbidden placeholder usage

Placeholders may not appear in filenames or paths, receive values, infer Target
facts, create a schema or parser contract, select files, generate inventory,
carry copied source payloads or substitute for a monolithic body.
`{{AGENT_BODY}}` is expressly prohibited. No placeholder is created, removed or
renamed here.

## 35. No-copy boundary

Existing template bodies are not valid sources. Kernels, Senior Agent Profiles
and `/docs` may never be copied wholesale. Source mirrors, `/docs` dumps,
monolithic support payloads and body reuse are forbidden. Future work must
transform narrowly selected sources into focused project-prepared content with
documented provenance; no such transformation is selected in this phase.

## 36. Traceability boundary

A future body basis must identify source anchors, summarize relevance, record a
transformation note and preserve provenance and behavior-parity rationale
without copying payloads. This phase defines no trace file, path, inventory,
schema, report or persisted record and does not select source anchors per file.

## 37. Anti-bloat boundary

Future entrypoints must remain small; common rules must remain centralized;
per-agent and temperature responsibilities must remain focused; documentation
must be consumed on demand; and repeated global content, source dumps and
monolithic placeholders are forbidden. Numeric limits, budgets, measurements
and threshold enforcement remain deferred.

## 38. Target dependency boundary

No Target is read, inferred, detected or mapped. Candidate filenames, paths and
body bases may not depend on Target facts. Target-dependent placeholder filling,
Target mapping, Target adapter, real artifact paths and real artifact writes
remain blocked pending separate explicit authorization.

## 39. Validation and review boundary

Future documentary review may discuss literal exactness, family/category
mapping, create-only preexistence, placeholder boundaries, no-copy,
traceability, anti-bloat, matrix preservation, Aggregator preservation and
no-Target/no-output compliance. This phase creates no test, fixture, snapshot,
rendered output, validator, checker, schema, threshold or executable gate.

## 40. Abort criteria for future exact allowlist

The next exact allowlist review must abort if it needs to:

- invent a filename, path or body basis;
- use `<agent>` without twelve literal expansions, `**` as a glob, any glob or
  any placeholder in a path;
- use a Target path or treat `.github/agents/**`, `.codex/agents/**`,
  `.sentinel/agents/**`, `AGENTS.md` or `.codex/config.toml` as a real
  destination;
- read an existing template without explicit authority or use the accidental
  reads recorded here;
- copy, reuse, compare or derive from an existing template body;
- mutate an existing template or proceed when a `create-only` path preexists;
- define a final body or executable template inventory;
- populate placeholder values or infer Target-dependent values;
- access a real Target, GitHub or the productive skill;
- copy a kernel, Senior Agent Profile or `/docs` wholesale;
- use `{{AGENT_BODY}}`, a source mirror, `/docs` dump or monolithic payload;
- create rendered outputs, fixtures, snapshots or persisted reports;
- create a parser, renderer, schema, validator, checker, meter or materializer;
- alter the Aggregator, any official child check or create a tenth check;
- alter matrix `12/24/2`, create an agent, treat support as a third matrix or
  treat templates, families, support packs or trace as agents;
- materialize any real Target artifact; or
- proceed without explicit `create-only`, collision, ownership, rollback and
  post-write review boundaries.

Since no literal convention or source-selection specification exists, an exact
allowlist retry now must abort before producing entries.

## 41. No rendered output boundary

No rendered output, preview, sample, fixture output, snapshot output, temporary
artifact, cache or persisted report is created or authorized. Output strategy
remains a separate future decision.

## 42. No parser renderer schema boundary

No parser, renderer, schema, tokenizer, grammar, compiler, serializer, escaping
engine or equivalent contract is created or implied. Documentary tables in
this file are not machine-readable contracts.

## 43. No validation enforcement boundary

No validator, checker, meter, threshold enforcement, test harness, executable
gate or official check is created. Review categories remain vocabulary only and
do not alter the official nine-check Aggregator.

## 44. No materialization boundary

No agent, entrypoint, target-level artifact, support pack, `_shared` content,
per-agent micro-pack, activation control, shard or trace artifact is
materialized. No runtime, CLI, writer, adapter or materialization strategy is
authorized.

## 45. What is decided now

- Status is `BLOCKED_PENDING_PATH_FILENAME_BODY_BASIS`.
- Verdict is `BLOCKED_WITHOUT_PATH_FILENAME_BODY_BASIS`.
- Posture is
  `TEMPLATE_PATH_FILENAME_AND_BODY_BASIS_BLOCKED_PENDING_CANONICAL_SPECIFICATION`.
- The inherited record does not determine literal filenames, literal paths or
  file-specific canonical body bases without invention.
- No candidate entry, allowlist entry, template body or executable inventory is
  defined.
- The accidental no-read violation is recorded; its content is discarded and
  cannot be used.
- Exact allowlist retry and template write remain blocked.

## 46. What remains blocked

Implementation; materialization; template creation, write and mutation;
reading existing templates by implication; candidate and final filenames and
paths; file-specific and final bodies; executable inventory; rendered outputs;
fixtures; snapshots; support-layer physical finalization; placeholder values;
Target-dependent filling and mapping; Target adapter; final trace structure,
path, inventory and schema; source mirrors; `/docs` dumps; persisted reports;
checker; validator; meter; parser; renderer; schema; materializer; threshold
enforcement; numeric thresholds; final audit approach; real Target; GitHub;
branch; commit; pull request; productive skill; Aggregator change; official
child-check change; and tenth check.

## 47. What cannot be finalized yet

- A literal dev-template directory layout.
- Literal filename and extension conventions.
- Cardinality and literal expansion rules per family.
- File-to-family and file-to-category assignments.
- File-specific source selection, precedence and transformation responsibility.
- Allowed namespace mapping per file.
- Final template bodies and executable inventory.
- Physical support-layer and final trace structure.
- Render, fixture, snapshot, parser, renderer, schema and validation strategies.
- Target mapping, adapter, filling, implementation and materialization strategy.
- Any mutation basis for existing templates.

## 48. Forbidden inferences

It is forbidden to infer that conceptual shapes are dev paths; family names are
filenames; `START.md` or `MANIFEST.md` are approved template files; `<agent>` or
`**` authorizes expansion; 57 placeholders determine bodies, mappings or
values; write authorization is current approval; existing templates determine
style, names, paths or bodies; accidental observations are evidence; Target
shapes authorize Target access; traceability defines a trace schema; qualitative
anti-bloat defines numbers; documentation is a checker; or support forms a
third agent matrix.

## 49. Allowed future decision paths

Only separately prompted documentary paths are allowed:

1. define a literal dev-template layout, naming convention and file-specific
   source-selection/transformation basis without creating templates;
2. audit that specification for no-copy, no-Target, literal expansion,
   collision, matrix and Aggregator preservation;
3. retry the exact allowlist only if the specification is complete and the
   no-read violation is not used as evidence;
4. consider a dev-only `create-only` write phase only after an exact allowlist
   passes and is repeated in full in its prompt; and
5. use separate future phases for mutation, outputs, mechanisms, validation,
   audit method, thresholds, Target mapping or materialization.

Skipping any stage preserves all blocks.

## 50. Recommended safe next phase

The required intermediate documentary phase is:

`MATERIALIZATION_FULL_FLOW_DEV_SKILL_DRY_RUN_IN_MEMORY_PROJECT_LOCAL_AGENT_MICRO_PACK_TEMPLATE_LAYOUT_NAMING_AND_SOURCE_SELECTION_SPECIFICATION`

It must receive explicit authority to establish a literal convention for
dev-skill template layout and naming plus file-specific documentary source
selection and transformation rules. It must remain documentation-only,
fail-closed, no-template-write, no-Target, no-GitHub, no-productive-skill,
no-output and no-mechanism. Exact allowlist retry remains blocked until that
specification is completed and separately reviewed.

## 51. Explicit non-authorization

This decision does not authorize implementation; materialization; template
creation, write, read, comparison or mutation; candidate or final filename/path
selection; final bodies or executable inventory; rendered outputs; fixtures;
snapshots; placeholder values; Target-dependent filling or mapping; Target
adapter; support-layer physical finalization; final trace
structure/path/inventory/schema; source mirror; `/docs` dump; persisted report;
checker; validator; meter; parser; renderer; schema; materializer; threshold
enforcement; numeric thresholds; final audit approach; runtime; CLI; filesystem
writer; real Target; GitHub; branch; commit; pull request; productive skill;
Aggregator or child-check change; tenth check; agent-count change; or third
matrix.

The only authorized effect of this phase is creation of this blocked decision
document.
