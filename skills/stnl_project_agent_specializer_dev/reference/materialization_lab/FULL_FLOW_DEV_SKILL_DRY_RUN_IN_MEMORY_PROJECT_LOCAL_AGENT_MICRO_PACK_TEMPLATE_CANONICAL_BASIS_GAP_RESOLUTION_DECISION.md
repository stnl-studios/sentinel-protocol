# Project-Local Agent Micro-Pack Template Canonical-Basis Gap-Resolution Decision

## 1. Verdict

`TEMPLATE_CANONICAL_BASIS_GAP_RESOLUTION_DECISION_PASS_WITH_TEMPLATE_WRITE_BLOCKED`

Phase verdict:

`MATERIALIZATION_FULL_FLOW_DEV_SKILL_DRY_RUN_IN_MEMORY_PROJECT_LOCAL_AGENT_MICRO_PACK_TEMPLATE_CANONICAL_BASIS_GAP_RESOLUTION_DECISION: PASS`

This is a documentation-only decision. It authorizes only a future retry of `MATERIALIZATION_FULL_FLOW_DEV_SKILL_DRY_RUN_IN_MEMORY_PROJECT_LOCAL_AGENT_MICRO_PACK_TEMPLATE_PATH_FILENAME_AND_BODY_BASIS_DECISION_RETRY`. Template writing, template creation, template mutation, exact-allowlist work, materialization, and rendered output remain blocked.

## 2. Executive Summary

This decision resolves the 21 canonical-policy gaps reported by the previous path/filename/body-basis retry. It establishes a dev-only root convention, exact segment vocabularies, marker and filename rules, required support-family cardinalities, a non-materialized trace disposition, source-binding and precedence policies, activation-temperature and semantic body-basis policies, a package cardinality of 52 future template candidates, and prerequisites for enumerable collision proof.

The decisions are forward documentation conventions authorized by this phase. They were not inferred from existing templates, filesystem layout, the productive skill, a real Target, or GitHub. No final template body, copy-ready template prose, placeholder value, exact path allowlist, or write authorization is present.

All 21 policy gaps are `RESOLVED`. Resolution of a policy does not assert that future source identifiers or semantic obligations already exist. The next retry must bind those items explicitly and must fail closed if an exact required source reference or obligation is absent.

## 3. Scope

This document decides only:

- the canonical dev-template basis root, path segment, scope, marker, and filename conventions;
- START, MANIFEST, trace, and package cardinality policies;
- source selection, precedence, activation-temperature, and semantic body-basis policies;
- collision-proof prerequisites; and
- readiness and fail-closed rules for the next documentation-only retry.

The literal root and filenames below are decision inputs for a future retry. They are not created paths, an enumerated write allowlist, or authorization to write.

## 4. Non-Goals

This phase does not create or modify templates or template directories; define final template bodies, sections, prose, placeholder syntax, or placeholder values; enumerate an exact write allowlist; inspect template preexistence; materialize target paths; access a real Target or GitHub; access the productive skill; create a fixture, snapshot, parser, renderer, schema, validator, checker, materializer, report, or generated output; change the Aggregator; or add a tenth check.

## 5. Canonical Inputs Read

The following canonical inputs were read completely:

1. the phase instruction `MATERIALIZATION_FULL_FLOW_DEV_SKILL_DRY_RUN_IN_MEMORY_PROJECT_LOCAL_AGENT_MICRO_PACK_TEMPLATE_CANONICAL_BASIS_GAP_RESOLUTION_DECISION`;
2. `FULL_FLOW_DEV_SKILL_DRY_RUN_IN_MEMORY_PROJECT_LOCAL_AGENT_MICRO_PACK_TEMPLATE_LAYOUT_NAMING_AND_SOURCE_SELECTION_SPECIFICATION.md`; and
3. `FULL_FLOW_DEV_SKILL_DRY_RUN_IN_MEMORY_PROJECT_LOCAL_AGENT_MICRO_PACK_TEMPLATE_PATH_FILENAME_AND_BODY_BASIS_DECISION_RETRY.md`.

Only narrow existence checks for the allowed directory, this exact decision-document path, and the two named canonical inputs were performed. Filesystem shape was not used as architectural or naming evidence.

## 6. Prohibited Inputs Not Read

No path under `skills/stnl_project_agent_specializer_dev/reference/templates/**` was read, inspected, searched, compared, or used as evidence. The productive skill under `skills/stnl_project_agent_specializer/` was not accessed. No real Target, GitHub state, existing template body, fixture, snapshot, or rendered output was accessed. `__MACOSX` and `.DS_Store` were ignored.

## 7. Historical Blocker Handling

The historical accidental reads of limited lines from:

- `skills/stnl_project_agent_specializer_dev/reference/templates/codex/AGENTS.md`; and
- `skills/stnl_project_agent_specializer_dev/reference/templates/copilot/agent.md`

remain contaminated and invalid. Their contents were not revisited, cited, reused, compared, summarized, or used to infer any root, segment, marker, filename, extension, body basis, placeholder, source, cardinality, or convention in this decision.

This note records contamination only. Any successor that uses or cannot demonstrate independence from that content must return `BLOCKED`.

## 8. Architecture And Matrix Invariants

The architecture remains `PROJECT_LOCAL_AGENT_MICRO_PACK_MATRIX`, with conceptual target shapes:

- `.github/agents/<agent>.agent.md`;
- `.codex/agents/<agent>.toml`;
- `.sentinel/agents/_shared/**`; and
- `.sentinel/agents/<agent>/**`.

The canonical roles remain exactly, and in order: `orchestrator`, `planner`, `validation-eval-designer`, `execution-package-designer`, `designer`, `coder-frontend`, `coder-backend`, `coder-ios`, `validation-runner`, `reviewer`, `finalizer`, and `resync`.

The matrix remains exactly 12 agents and 24 conceptual agent artifacts: 12 GitHub entrypoints plus 12 Codex entrypoints. `.codex/config.toml` and `AGENTS.md` are exactly two conceptual target-level artifacts. Target-level artifacts, `_shared`, START, MANIFEST, per-agent support, trace, placeholders, and template families are not agents. `.sentinel/agents/**` is support architecture, not a third matrix and not a replacement for either entrypoint surface.

The nine-family taxonomy remains unchanged:

1. `copilot-agent-entrypoint`;
2. `codex-agent-entrypoint`;
3. `codex-target-config`;
4. `target-agents-guide`;
5. `shared-micro-pack-start`;
6. `shared-micro-pack-manifest`;
7. `agent-micro-pack-start`;
8. `agent-micro-pack-manifest`; and
9. `trace-shard`.

The official Aggregator remains closed at exactly nine child checks. No checker or Aggregator change is authorized.

## 9. Prior Retry Blockers

The prior retry was blocked by a symbolic root; unresolved surface, scope, and marker tokens; incomplete filename normalization and template-marker rules; undefined support filenames and family requirements; undefined trace scope, count, format, and placement; incomplete source bindings and precedence; incomplete temperature and body-basis bindings; indeterminate package cardinality; and lack of an enumerable collision proof.

This phase resolves those policy choices as forward documentation conventions. It does not pretend that future semantic source references have been read or that body obligations have been authored.

## 10. Decision Authority And Invention Boundary

This phase uses its explicit authority to establish forward conventions internal to the dev-skill documentation layer. A convention is valid here only when it is consistent with the approved architecture, independent of existing templates and external state, and neither final template content nor an exact write allowlist.

The decisions below establish deterministic inputs for the next retry. They do not establish final bodies, body sections, copy-ready prose, placeholder values, rendered output, target-specific values, preexistence facts, or permission to write. Missing semantic source evidence remains a blocker at the body-basis-record level and may not be replaced by guesswork.

## 11. Gap Resolution Summary

| Gap | Subject | Status | Canonical result |
| --- | --- | --- | --- |
| `GAP-01` | Dev-template root | `RESOLVED` | `skills/stnl_project_agent_specializer_dev/reference/materialization_lab/template_basis/` |
| `GAP-02` | Target-surface segments | `RESOLVED` | Seven exact surface tokens |
| `GAP-03` | Scope segments | `RESOLVED` | `target-level`, `shared`, `per-agent`, `trace` |
| `GAP-04` | Role/shared/target markers | `RESOLVED` | role identifier, `_shared`, `_target`, `_trace` |
| `GAP-05` | Filename normalization | `RESOLVED` | Stable ASCII lower-kebab with one explicit target-basename exception |
| `GAP-06` | Template marker/suffix | `RESOLVED` | `.template.md` and `.template.toml` |
| `GAP-07` | Compound extensions | `RESOLVED` | Preserve semantic tokens before `.template.<format>` |
| `GAP-08` | Support filenames/extensions | `RESOLVED` | `start.template.md`, `manifest.template.md`; trace has no file |
| `GAP-09` | Shared START | `RESOLVED` | Exactly 1 |
| `GAP-10` | Shared MANIFEST | `RESOLVED` | Exactly 1 |
| `GAP-11` | Per-agent START | `RESOLVED` | Exactly 12 |
| `GAP-12` | Per-agent MANIFEST | `RESOLVED` | Exactly 12 |
| `GAP-13` | Trace scope | `RESOLVED` | Package-wide audit-only body-basis record set |
| `GAP-14` | Trace cardinality | `RESOLVED` | Zero future template files |
| `GAP-15` | Trace filename/format | `RESOLVED` | No filename; structured decision records in the retry document |
| `GAP-16` | Source binding | `RESOLVED` | Exact reference plus category and obligation binding |
| `GAP-17` | Source precedence | `RESOLVED` | Six-level precedence, with existing templates excluded |
| `GAP-18` | Activation temperature | `RESOLVED` | Entrypoint/support/trace/docs disposition policy |
| `GAP-19` | Body-basis binding | `RESOLVED` | Required semantic record fields; never final prose |
| `GAP-20` | Package cardinality | `RESOLVED` | Exactly 52 future template candidates; 12 agents remain |
| `GAP-21` | Collision prerequisites | `RESOLVED` | Complete enumeration plus exact and normalized uniqueness checks |

## 12. GAP-01 Dev-Template Root

- **Gap identifier:** `GAP-01`.
- **Prior blocked basis:** `<AUTHORIZED_DEV_TEMPLATE_ROOT>` was symbolic and no literal root had been authorized.
- **Decision status:** `RESOLVED`.
- **Canonical decision:** The dev-only root for future template-basis candidates is `skills/stnl_project_agent_specializer_dev/reference/materialization_lab/template_basis/`. This phase does not create it.
- **Evidence used:** The primary specification's symbolic root requirement, the approved dev-only documentation boundary, and this phase's explicit authority to establish a forward root convention.
- **Evidence explicitly not used:** Existing template paths, filesystem layout, productive-skill layout, Target real, GitHub, and the historical accidental read.
- **Downstream effect:** The next retry may use this literal root when deriving and enumerating candidate paths.
- **Fail-closed rule:** If use of this root would require directory creation, file writing, inspection of an existing template, or mutation during the retry, the retry must return `BLOCKED`.

## 13. GAP-02 Target-Surface Segment Mapping

- **Gap identifier:** `GAP-02`.
- **Prior blocked basis:** Exact normalized target-surface path tokens were absent.
- **Decision status:** `RESOLVED`.
- **Canonical decision:** Map families to surface tokens as follows: `copilot-agent-entrypoint` -> `github-agent`; `codex-agent-entrypoint` -> `codex-agent`; `codex-target-config` -> `codex-target`; `target-agents-guide` -> `target-guide`; both shared micro-pack families -> `sentinel-shared`; both per-agent micro-pack families -> `sentinel-agent`; `trace-shard` -> logical token `sentinel-trace`, used only in body-basis trace records because trace has no future template file.
- **Evidence used:** The nine-family taxonomy, conceptual target surfaces, and candidate tokens authorized for evaluation by this phase.
- **Evidence explicitly not used:** Existing directory names, template names, external platform state, and contaminated reads.
- **Downstream effect:** Every materialized family has one exact surface token; trace records have one logical surface token.
- **Fail-closed rule:** No alias, pluralization, abbreviation, or additional surface token may be introduced by the next retry.

## 14. GAP-03 Scope Segment Mapping

- **Gap identifier:** `GAP-03`.
- **Prior blocked basis:** Trace scope was unresolved and the symbolic grammar did not fully bind all family instances.
- **Decision status:** `RESOLVED`.
- **Canonical decision:** The exact scope tokens are `target-level`, `shared`, `per-agent`, and `trace`. The first eight materialized families use the scope declared by their taxonomy. `trace-shard` uses logical scope `trace` only in audit/body-basis records and produces no template candidate.
- **Evidence used:** The symbolic scope taxonomy, trace-policy requirements, and this phase's authority to choose a non-materialized trace disposition.
- **Evidence explicitly not used:** Existing template layout and physical trace artifacts.
- **Downstream effect:** Scope is deterministic for every family and cannot collide through implicit omission.
- **Fail-closed rule:** A future proposal using an unlisted scope or treating `trace` as a third agent matrix must return `BLOCKED`.

## 15. GAP-04 Role Shared Target Marker Rule

- **Gap identifier:** `GAP-04`.
- **Prior blocked basis:** Target markers could be omitted or named, and shared/trace markers were not exact.
- **Decision status:** `RESOLVED`.
- **Canonical decision:** Every hierarchy retains the marker segment. Per-agent scope uses the exact canonical role identifier. Shared scope uses `_shared`. Target-level scope uses `_target`. Logical trace scope uses `_trace`. Marker omission is prohibited.
- **Evidence used:** The canonical role matrix, the requirement for a single shared marker, and the need for injective paths.
- **Evidence explicitly not used:** Existing marker directories and target filesystem shape.
- **Downstream effect:** The next retry can expand every family instance without optional segments.
- **Fail-closed rule:** Any noncanonical role, missing marker, alias, or marker reuse across scopes blocks the retry.

## 16. GAP-05 Filename Normalization

- **Gap identifier:** `GAP-05`.
- **Prior blocked basis:** Case, separators, role placement, support basenames, and normalization equivalence were unresolved.
- **Decision status:** `RESOLVED`.
- **Canonical decision:** Dev-template filenames use stable ASCII. Generated tokens are lowercase kebab-case, with no spaces, display labels, inferred abbreviations, source-derived names, Unicode substitutions, or aliases. Exact canonical role identifiers are preserved. The role remains both the per-agent marker segment and the basename input only for entrypoint families; support files use purpose basenames because the parent marker already identifies scope/role. The sole case-preserving exception is `AGENTS` in `AGENTS.template.md`, required to preserve the fixed conceptual target basename `AGENTS.md`.
- **Evidence used:** Canonical roles, conceptual target basenames, deterministic naming inputs, and the candidate normalization policy.
- **Evidence explicitly not used:** Existing template filenames and hidden filesystem conventions.
- **Downstream effect:** The next retry has one normalization function per declared family and can enumerate deterministic names.
- **Fail-closed rule:** Lossy normalization, locale-sensitive case conversion, undeclared exceptions, or two distinct inputs normalizing to one token blocks the retry.

## 17. GAP-06 Template Marker Or Suffix

- **Gap identifier:** `GAP-06`.
- **Prior blocked basis:** No exact marker, suffix, or placement rule existed.
- **Decision status:** `RESOLVED`.
- **Canonical decision:** Markdown-oriented candidates end in `.template.md`; TOML-oriented candidates end in `.template.toml`. `template` is inserted immediately before the final format extension and after any semantic basename token.
- **Evidence used:** The required target formats, candidate marker policy, and the need to distinguish basis files from conceptual outputs.
- **Evidence explicitly not used:** Existing suffixes and filenames.
- **Downstream effect:** All 52 materialized candidates have a deterministic marker and final extension.
- **Fail-closed rule:** No alternate marker, doubled marker, marker omission, or trace-specific physical suffix is allowed.

## 18. GAP-07 Compound Extension Handling

- **Gap identifier:** `GAP-07`.
- **Prior blocked basis:** The treatment of `.agent.md` and marker placement was undefined.
- **Decision status:** `RESOLVED`.
- **Canonical decision:** A semantic token preceding the format extension is preserved before the template marker. Therefore a GitHub role candidate uses `<role>.agent.template.md`; `.agent` remains semantic and `.md` remains the format. A Codex role candidate uses `<role>.template.toml`. Target candidates use `config.template.toml` and `AGENTS.template.md`. Compound semantic tokens are not collapsed, reordered, or treated as an opaque extension.
- **Evidence used:** The fixed conceptual output forms and the canonical marker rule.
- **Evidence explicitly not used:** Existing compound filenames.
- **Downstream effect:** Entrypoint and target-level filename derivation is exact without consulting templates.
- **Fail-closed rule:** Any derivation that drops `.agent`, changes the final format, or inserts `template` at another position blocks the retry.

## 19. GAP-08 Support Artifact Filename And Extension Rule

- **Gap identifier:** `GAP-08`.
- **Prior blocked basis:** START, MANIFEST, and trace were purposes only, without physical names or extensions.
- **Decision status:** `RESOLVED`.
- **Canonical decision:** Shared and per-agent START candidates use `start.template.md`. Shared and per-agent MANIFEST candidates use `manifest.template.md`. Markdown is selected because both purposes carry human-readable activation, discovery, inventory, and routing semantics rather than target configuration. Trace is non-materialized under `GAP-13` through `GAP-15`, so it has no template filename or extension.
- **Evidence used:** The family purposes, candidate support-artifact names, and audit-only trace option.
- **Evidence explicitly not used:** Existing support artifacts or target shard names.
- **Downstream effect:** The four support families have exact purpose filenames; trace creates no candidate-path ambiguity.
- **Fail-closed rule:** A future retry may not invent another support basename, extension, or physical trace file.

## 20. GAP-09 Shared START Cardinality

- **Gap identifier:** `GAP-09`.
- **Prior blocked basis:** The shared START family was only `0..1`.
- **Decision status:** `RESOLVED`.
- **Canonical decision:** `shared-micro-pack-start` is required exactly once for the package.
- **Evidence used:** The approved shared activation-entry purpose, centralization under `_shared`, and the candidate cardinality.
- **Evidence explicitly not used:** Existing shard inventory or directory presence.
- **Downstream effect:** The next retry must enumerate one shared START candidate.
- **Fail-closed rule:** Zero, duplicate, or per-role copies block the retry.

## 21. GAP-10 Shared MANIFEST Cardinality

- **Gap identifier:** `GAP-10`.
- **Prior blocked basis:** The shared MANIFEST family was only `0..1`.
- **Decision status:** `RESOLVED`.
- **Canonical decision:** `shared-micro-pack-manifest` is required exactly once for the package.
- **Evidence used:** The approved shared inventory/routing purpose, anti-bloat policy, and the candidate cardinality.
- **Evidence explicitly not used:** Existing manifests or inferred shard lists.
- **Downstream effect:** The next retry must enumerate one shared MANIFEST candidate.
- **Fail-closed rule:** Zero, duplicate, or role-specific copies of the shared manifest block the retry.

## 22. GAP-11 Per-Agent START Cardinality

- **Gap identifier:** `GAP-11`.
- **Prior blocked basis:** Twelve instances were conditional on a missing shard policy.
- **Decision status:** `RESOLVED`.
- **Canonical decision:** `agent-micro-pack-start` is required exactly once for each of the 12 canonical roles, for exactly 12 candidates.
- **Evidence used:** The approved per-agent activation purpose, exact role matrix, and candidate cardinality.
- **Evidence explicitly not used:** Existing per-agent packs or inferred role content.
- **Downstream effect:** The next retry must bind one START candidate to each exact role.
- **Fail-closed rule:** Missing, duplicate, aliased, or noncanonical roles block the retry.

## 23. GAP-12 Per-Agent MANIFEST Cardinality

- **Gap identifier:** `GAP-12`.
- **Prior blocked basis:** Twelve instances were conditional on a missing exact per-agent shard inventory.
- **Decision status:** `RESOLVED`.
- **Canonical decision:** `agent-micro-pack-manifest` is required exactly once for each of the 12 canonical roles, for exactly 12 candidates. This decision requires a routing/inventory artifact per role but does not invent the inventory contents.
- **Evidence used:** The approved per-agent inventory/routing purpose, exact role matrix, and candidate cardinality.
- **Evidence explicitly not used:** Existing manifests, shard bodies, or an inferred inventory.
- **Downstream effect:** The next retry must bind one MANIFEST candidate to each role and must separately prove its semantic body basis.
- **Fail-closed rule:** An absent exact semantic inventory for a role blocks that role's body-basis record and therefore blocks an overall retry `PASS`; it does not reduce cardinality.

## 24. GAP-13 Trace Shard Scope

- **Gap identifier:** `GAP-13`.
- **Prior blocked basis:** Trace could be shared or per-agent, with no placement policy.
- **Decision status:** `RESOLVED`.
- **Canonical decision:** Adopt trace option D. `trace-shard` remains a taxonomy and audit responsibility but is represented only by package-wide structured body-basis trace records inside the future retry decision document. It is not a future template or target artifact. Each trace record still binds its destination family, scope, and canonical role where applicable.
- **Evidence used:** The candidate trace options, traceability policy, anti-bloat requirement, and prohibition against a third matrix.
- **Evidence explicitly not used:** Existing trace files, target layout, or template content.
- **Downstream effect:** Traceability remains mandatory while no physical shared or per-agent trace candidate is generated.
- **Fail-closed rule:** Missing trace coverage blocks the next retry; creating a trace template, treating trace as default payload, or counting trace as an agent also blocks it.

## 25. GAP-14 Trace Shard Cardinality

- **Gap identifier:** `GAP-14`.
- **Prior blocked basis:** Candidate counts ranged from one to thirteen or could remain audit-only.
- **Decision status:** `RESOLVED`.
- **Canonical decision:** Future trace template-file cardinality is exactly zero. Logical trace-record cardinality is obligation-driven and must be sufficient to cover every selected transformed obligation; it is not package-file cardinality and is not fixed by this phase.
- **Evidence used:** Audit-only option D and the rule that traceability establishes parity without source duplication.
- **Evidence explicitly not used:** Existing traces, arbitrary per-family counts, or arbitrary per-role counts.
- **Downstream effect:** Package file count is determinate; the next retry must prove trace completeness rather than enumerate trace files.
- **Fail-closed rule:** Any untraced selected obligation blocks the retry. Adding one or more trace template candidates contradicts this decision and blocks it.

## 26. GAP-15 Trace Shard Filename Format Basis

- **Gap identifier:** `GAP-15`.
- **Prior blocked basis:** Trace filename, format, schema boundary, and placement were absent.
- **Decision status:** `RESOLVED`.
- **Canonical decision:** No trace filename, extension, or filesystem placement exists because trace is non-materialized. The next retry must represent trace as structured documentation records with these semantic fields: source reference, selected obligation, transformation rule, destination family, destination scope, canonical role when applicable, activation temperature when applicable, and shared-versus-role-specific disposition. Table or equivalent structured Markdown in the decision record is acceptable; no machine schema is authorized.
- **Evidence used:** The canonical traceability fields and audit-only trace disposition.
- **Evidence explicitly not used:** Existing trace formats, schemas, parsers, or checkers.
- **Downstream effect:** The retry has an exact semantic trace-record basis without a physical candidate filename.
- **Fail-closed rule:** Copying source bodies, omitting a required field, creating a schema/tool, or writing a trace template blocks the retry.

## 27. GAP-16 Source Selection Binding Policy

- **Gap identifier:** `GAP-16`.
- **Prior blocked basis:** Only source categories were eligible; exact per-family and per-role bindings were absent.
- **Decision status:** `RESOLVED`.
- **Canonical decision:** Every future body-basis record must bind each selected semantic obligation to an allowed source category and an exact source identifier. If the exact identifier is not available, the record must name it as missing and remain `BLOCKED`; a category alone is not a binding. Binding copies no content. Allowed categories are materialization-lab architecture decisions, explicit materialization-lab contracts, source-inventory decisions, canonical matrix decisions, kernels and Senior Agent Profiles as semantic authorities only, and project context/foundation documents only when a future phase explicitly scopes them.
- **Evidence used:** The primary source-selection constraints and the candidate exact-reference policy.
- **Evidence explicitly not used:** Existing templates, implicit repository knowledge, unscoped project docs, Target real, GitHub, and recollection.
- **Downstream effect:** The next retry can distinguish a resolved source binding from a clearly blocked missing source and cannot pass on category-level eligibility alone.
- **Fail-closed rule:** Missing, ambiguous, prohibited, circular, or unscoped source references block the affected record and the overall retry.

## 28. GAP-17 Source Precedence Policy

- **Gap identifier:** `GAP-17`.
- **Prior blocked basis:** Conflicts required explicit precedence but no order existed.
- **Decision status:** `RESOLVED`.
- **Canonical decision:** Apply this descending precedence: (1) project-local documented architecture decisions in `materialization_lab`; (2) explicit contracts in `materialization_lab/contracts`; (3) source-inventory decisions; (4) canonical matrix decisions; (5) kernels and Senior Agent Profiles as semantic authorities only; and (6) project context/foundation documents only when explicitly scoped by a future phase. A more-specific record within the same level wins only when it explicitly declares its scope and does not contradict a higher level; otherwise the conflict remains blocked. Existing templates are not in the chain.
- **Evidence used:** The candidate precedence order and the primary requirement for explicit conflict handling.
- **Evidence explicitly not used:** File modification time, filesystem proximity, template content, Target state, and GitHub state.
- **Downstream effect:** The next retry has deterministic conflict handling for exact source bindings.
- **Fail-closed rule:** Same-level unresolved conflict, silent override, or reliance on a source outside the chain blocks the record and overall retry.

## 29. GAP-18 Activation Temperature Disposition Policy

- **Gap identifier:** `GAP-18`.
- **Prior blocked basis:** Applicable obligations lacked exact `always`/`hot`/`warm`/`cold`/`trace` disposition rules.
- **Decision status:** `RESOLVED`.
- **Canonical decision:** Entrypoints use `always` only for minimum identity, activation, target-surface entry, and routing. Shared START/MANIFEST use `always` only for discovery and routing metadata; `hot`, `warm`, and `cold` are references to separately governed semantic shards, not embedded bodies. Per-agent START/MANIFEST use `always` only for role activation and routing; `hot`, `warm`, and `cold` are shard references. Trace obligations use `trace` only and are never default behavioral payload. Project docs are on-demand only and never default-loaded. Target config uses `always` only for documented target configuration obligations; activation temperatures otherwise do not apply to configuration values.
- **Evidence used:** The approved activation taxonomy, anti-bloat constraints, and candidate disposition policy.
- **Evidence explicitly not used:** Existing loading behavior, rendered output, or target runtime state.
- **Downstream effect:** Each semantic obligation can be assigned or declared not applicable without inventing a body.
- **Fail-closed rule:** Unassigned applicable obligations, default-loaded docs, embedded cold material, or trace as runtime payload blocks the retry.

## 30. GAP-19 Body Basis Binding Policy

- **Gap identifier:** `GAP-19`.
- **Prior blocked basis:** Family-level constraints existed, but exact source/obligation/transformation/temperature bindings were incomplete.
- **Decision status:** `RESOLVED`.
- **Canonical decision:** A body basis remains semantic and never final prose. Every instance record, or provably uniform group record, must bind: record identifier; family; target surface; scope; canonical role when applicable; artifact purpose; allowed source category; exact source reference or explicit missing-source blocker; selected semantic obligation; non-literal transformation rule; activation temperature or explicit non-applicability; destination family; shared-versus-role-specific disposition; exclusions; and trace expectation. Uniform grouping is permitted only when every bound field except canonical role is identical and role expansion is exact.
- **Evidence used:** The primary body-basis derivation sequence and the candidate binding policy.
- **Evidence explicitly not used:** Final prose, existing body structure, placeholder syntax/values, copied kernels/profiles/docs, and rendered examples.
- **Downstream effect:** The next retry can produce complete semantic records and determine precisely which records remain blocked.
- **Fail-closed rule:** If drafting would still require inventing an obligation, source, transformation, temperature, or allocation, the record and overall retry remain `BLOCKED`.

## 31. GAP-20 Package Cardinality Policy

- **Gap identifier:** `GAP-20`.
- **Prior blocked basis:** Optional support families and unknown trace count prevented a total.
- **Decision status:** `RESOLVED`.
- **Canonical decision:** The future template candidate package count is exactly 52: 12 GitHub agent entrypoints + 12 Codex agent entrypoints + 1 Codex target config + 1 target AGENTS guide + 1 shared START + 1 shared MANIFEST + 12 per-agent START + 12 per-agent MANIFEST + 0 trace template files. The agent count remains exactly 12 and the conceptual agent-artifact count remains exactly 24. The two target-level and 26 support candidates are not agents.
- **Evidence used:** The canonical matrix and resolved cardinalities `GAP-09` through `GAP-15`.
- **Evidence explicitly not used:** Existing file counts, directory contents, or an inferred shard inventory.
- **Downstream effect:** The next retry must enumerate exactly 52 candidate paths and can detect omissions or extras arithmetically.
- **Fail-closed rule:** Any count other than 52, any trace file, any missing role instance, or counting support as agents blocks the retry.

## 32. GAP-21 Collision Proof Prerequisite Policy

- **Gap identifier:** `GAP-21`.
- **Prior blocked basis:** Exact paths could not be enumerated and normalization equivalence could not be tested.
- **Decision status:** `RESOLVED`.
- **Canonical decision:** A valid next-retry collision proof must enumerate all 52 exact candidate paths from the canonical tuple `(root, surface, scope, family, marker, filename)` and demonstrate: exact-string uniqueness; ASCII case-folded uniqueness; separator-normalized uniqueness; Unicode-normalized uniqueness even though generated tokens are ASCII; unique family-instance coverage; no conflicting conceptual target purpose; exact cardinality by family; and no role aliasing. Distinct family segments must be retained, so identical support basenames in distinct families cannot collide.
- **Evidence used:** The symbolic grammar, exact conventions resolved here, and the primary collision policy.
- **Evidence explicitly not used:** Existing-file inspection, preexistence, or hidden filesystem conventions.
- **Downstream effect:** The next retry has a complete proof procedure; it must provide enumeration and results, not patterns alone.
- **Fail-closed rule:** A duplicate, normalized equivalent, unexplained exception, missing enumeration row, or inability to prove all checks blocks the retry.

## 33. Consolidated Canonical Basis For Next Retry

The next retry must derive candidates with this grammar:

`skills/stnl_project_agent_specializer_dev/reference/materialization_lab/template_basis/<surface>/<scope>/<family>/<marker>/<filename>`

The consolidated family basis is:

| Family | Surface | Scope | Marker basis | Filename basis | Count |
| --- | --- | --- | --- | --- | ---: |
| `copilot-agent-entrypoint` | `github-agent` | `per-agent` | each exact role | `<role>.agent.template.md` | 12 |
| `codex-agent-entrypoint` | `codex-agent` | `per-agent` | each exact role | `<role>.template.toml` | 12 |
| `codex-target-config` | `codex-target` | `target-level` | `_target` | `config.template.toml` | 1 |
| `target-agents-guide` | `target-guide` | `target-level` | `_target` | `AGENTS.template.md` | 1 |
| `shared-micro-pack-start` | `sentinel-shared` | `shared` | `_shared` | `start.template.md` | 1 |
| `shared-micro-pack-manifest` | `sentinel-shared` | `shared` | `_shared` | `manifest.template.md` | 1 |
| `agent-micro-pack-start` | `sentinel-agent` | `per-agent` | each exact role | `start.template.md` | 12 |
| `agent-micro-pack-manifest` | `sentinel-agent` | `per-agent` | each exact role | `manifest.template.md` | 12 |
| `trace-shard` | `sentinel-trace` logical only | `trace` logical only | `_trace` logical only | no file | 0 |

This table is a canonical derivation basis, not a 52-path enumeration and not an exact write allowlist. The next retry must expand the 12 roles, attach complete semantic body-basis and trace records, and perform the collision proof. It must not create the root or any candidate.

## 34. Remaining Blockers Or Warnings

No canonical-policy gap from `GAP-01` through `GAP-21` remains blocked in this phase.

The following are successor-phase warnings, not unresolved policy gaps:

- exact semantic source references and obligations have not been read, selected, or bound by this phase;
- the next retry must mark any missing exact source identifier as `BLOCKED` and may not substitute category-level eligibility;
- the 52 candidate paths have not been enumerated or checked for preexistence;
- collision proof has been specified but not executed;
- trace completeness has not been demonstrated for unwritten body-basis records;
- the historical accidental reads remain permanently invalid evidence; and
- template writing, exact allowlisting, and all materialization remain blocked.

These warnings preserve fail-closed behavior while leaving the next path/filename/body-basis retry meaningful.

## 35. Future Retry Rules

The only next recommended phase is:

`MATERIALIZATION_FULL_FLOW_DEV_SKILL_DRY_RUN_IN_MEMORY_PROJECT_LOCAL_AGENT_MICRO_PACK_TEMPLATE_PATH_FILENAME_AND_BODY_BASIS_DECISION_RETRY`

That retry may use this document as canonical input. It must remain documentation-only, must not read `reference/templates/**`, and must not create templates or an exact write allowlist. It must enumerate exactly 52 candidate paths, create complete semantic body-basis and audit-trace records, and execute the collision proof in its decision document.

Only if that retry returns `PASS` may `MATERIALIZATION_FULL_FLOW_DEV_SKILL_DRY_RUN_IN_MEMORY_PROJECT_LOCAL_AGENT_MICRO_PACK_TEMPLATE_WRITE_EXACT_ALLOWLIST_DECISION_RETRY` be considered. No automatic progression is authorized.

## 36. Abort Criteria

Any successor must return `BLOCKED` if it needs existing template content; uses the historical accidental read; accesses the productive skill, a real Target, or GitHub; edits an existing file; creates a directory or template; invents a path, filename, source, semantic obligation, shard, body, placeholder, or temperature; cannot enumerate exactly 52 candidate paths; cannot prove collision freedom; lacks complete trace coverage; copies full kernels, profiles, or docs; produces rendered output; creates a fixture, snapshot, parser, renderer, schema, validator, checker, materializer, Aggregator change, or tenth check; changes the 12-agent/24-agent-artifact matrix; or counts support as agents.

## 37. Boundary And Posture Confirmation

This phase creates exactly this one decision document and no other file. It edits no existing file. It creates no directory, template, rendered output, fixture, snapshot, parser, renderer, schema, validator, checker, materializer, Aggregator change, or tenth check. It does not access prohibited sources or external state.

The preserved posture is:

`TEMPLATE_CANONICAL_BASIS_GAP_RESOLUTION_DECISION_PASS_WITH_TEMPLATE_WRITE_BLOCKED`

PASS resolves the documentation conventions only. It does not authorize template writing.

## 38. Final Status

`MATERIALIZATION_FULL_FLOW_DEV_SKILL_DRY_RUN_IN_MEMORY_PROJECT_LOCAL_AGENT_MICRO_PACK_TEMPLATE_CANONICAL_BASIS_GAP_RESOLUTION_DECISION: PASS`

All 21 canonical-policy gaps are resolved. Package cardinality is exactly 52 future template candidates, with exactly 12 agents and 24 conceptual agent artifacts. Trace template cardinality is zero under audit-only option D. The next path/filename/body-basis retry is meaningful and authorized for consideration; template writing and exact-allowlist work remain blocked.
